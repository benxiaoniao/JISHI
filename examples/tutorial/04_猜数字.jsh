# 第 3 章示例：猜数字（综合运用判断与循环）
导入 随机
令 答案 = 随机.随机整数(1, 100)
令 猜中 = 假
循环 10 次：
    令 猜测 = 整数(输入("猜一个 1 到 100 的数："))
    如果 猜测 < 答案：
        打印("太小了，再猜")
    否则如果 猜测 > 答案：
        打印("太大了，再猜")
    否则：
        打印("猜对了！答案就是", 答案)
        猜中 = 真
        中断
如果 非 猜中：
    打印("次数用完了，答案是", 答案)
