# 第 4 章示例：函数——把代码打包复用
函数 问候(名字)：
    返回 "你好，" + 名字 + "！"

打印(问候("小明"))
打印(问候("基石学员"))

函数 求平均(分数们)：
    返回 总和(分数们) / 长度(分数们)

打印("平均分：", 求平均([85, 92, 78]))

# 函数可以调用自己（递归）
函数 斐波那契(n)：
    如果 n <= 2：
        返回 1
    返回 斐波那契(n - 1) + 斐波那契(n - 2)

打印("斐波那契数列第 10 项：", 斐波那契(10))

# 不写「返回」的函数返回「空」
函数 打印横线()：
    打印("----------------")

打印横线()
打印("程序结束")
