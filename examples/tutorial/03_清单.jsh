# 第 2 章示例：购物清单
令 清单 = ["牛奶", "面包", "鸡蛋"]
清单.追加("苹果")
清单.移除("面包")
打印("清单：", 清单)
打印("共", 长度(清单), "样东西")
如果 清单.包含("牛奶")：
    打印("牛奶还在")
令 句子 = "  基石语言真好学  "
打印("去掉空白后：", 句子.去空白())
打印("以「基石」开头：", 句子.去空白().开头是("基石"))
