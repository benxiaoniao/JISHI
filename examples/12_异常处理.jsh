# 异常处理 —— 尝试 / 捕获 / 最终 / 抛出
# 演示：用中文异常让程序既健壮又可读

# 一个会「校验输入」的函数：不合法就抛出 值错误
函数 折扣价(原价, 折扣):
    如果 折扣 <= 0 或 折扣 > 1:
        抛出 值错误("折扣必须在 0 到 1 之间")
    返回 原价 * 折扣

# 逐个尝试，捕获不同类型的错误
尝试:
    打印("正常：", 折扣价(100, 0.8))
    打印("越界：", 折扣价(100, 2))
捕获 值错误 为 e:
    打印("捕获到", e.类型, "：", e.消息)

# 捕获基类能抓子类：运行期错误 > 值错误
尝试:
    抛出 键错误("找不到这个键")
捕获 运行期错误 为 e:
    打印("用基类接住：", e.类型)

# 最终块无论如何都执行（收尾资源）
尝试:
    令 数据 = [1, 2, 3]
    打印(数据[99])
捕获 索引错误 为 e:
    打印("越界了：", e.消息)
最终:
    打印("（清理完毕）")

# 抛出任意值：非异常对象会被包装成「异常」
尝试:
    抛出 "账户余额不足"
捕获 为 e:
    打印("接到：", e.类型, "：", e.消息)
