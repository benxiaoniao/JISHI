# 猜数字小游戏 —— 基石语言第一个完整程序
# 运行：jishi examples/01_猜数字.jsh

导入 随机

令 答案 = 随机.随机整数(1, 100)
打印("我想好了一个 1 到 100 之间的数，你来猜！")

循环 10 次：
    令 猜测 = 整数(输入("猜一个数："))

    如果 猜测 < 答案：
        打印("太小了，再大一点")
    否则如果 猜测 > 答案：
        打印("太大了，再小一点")
    否则：
        打印("恭喜你，猜对了！答案就是", 答案)
        中断

打印("游戏结束，答案是", 答案)
