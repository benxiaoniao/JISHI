# 基石技术路线（v1.0 之后）

> 前作 `docs/路线图.md` 记录 M0–M16（已完成），本文件规划 **1.0 之后**的演进。
> 延续既有纪律：**诚实不吹牛 + 度量指标防自嗨 + 明确记录边界**。

---

## 〇、战略定位（2026-09 重要调整）

经过 M0–M16 的完整闭环，重新审视方向后，对北极星做一次**务实调整**：

| | 旧定位 | 新定位 |
|---|--------|--------|
| 北极星 | 「最容易被大模型正确生成、安全执行、自我修复」 | **健全语言自身能力，让编程者方便地实现意图，出问题能快速定位** |
| 「AI 友好」 | 核心驱动 | **辅助线，带着走**（不放弃，但不作为当前主攻方向） |

**为什么调整**：

1. 「容易被大模型正确生成」的影响因素太复杂——涉及模型能力、训练语料、
   提示词工程、评测方法等多个不可控变量，作为唯一北极星会让路线被外部因素牵着走。
2. 基石当前**更本质的价值**，是给中文使用者一门「写起来顺、错了能看懂、能快速找到问题」
   的语言——这个价值不依赖任何模型，也永远成立。
3. 一个语言如果自身不够健壮、报错不够好用，AI 写得再准，用户也难上手。

> **结论**：把力气花在「语言自身」上。AI 友好相关的资产（语言卡 / 评测集 /
> few-shot / 容错解析）继续维护、随需求自然生长，但不再作为路线的主线牵引。

---

## 一、现状盘点（规划的事实基础）

### 1.1 已具备的能力

| 维度 | 现状 |
|------|------|
| 语言核心 | 变量/分支/循环/函数/类继承/异常/闭包，中文报错精确到行列 + 修正建议 |
| 表达力 | 多赋值解包、默认参数、列表字典推导式、文本插值、容错解析（英文关键字纠正） |
| 执行器 | 树遍历 / Python VM / C VM（三执行器 107 项对拍逐字节一致） |
| 性能 | C VM 提速 1.6x–47x（整数循环 47x、递归 17x）；字符串拼接为 HOST 句柄架构边界 |
| 标准库 | 14 个中文模块（随机/数学/时间/文件/表格/文本/json/日期/正则/网络/路径/加密/压缩/系统） |
| 生态桥 | `导入 x 从 python` 直用整个 Python 生态，关键字参数保中文，异常中文化 |
| AI 辅助 | 语言卡 / 机器可读语言规格 / 结构化报错（含 fix 自愈）/ 沙箱 / MCP Server / few-shot 45 对 |
| 包管理 | `jishi 安装/卸载/列表`，依赖递归解析 + 版本约束 + 循环检测 |
| 跨语言 | JSON 字节码 + Node 宿主（语义对齐）/ Rust 宿主（最小可运行）/ C ABI 嵌入 |
| 发行版 | PyInstaller 独立可执行（Windows x64，16MB）+ 安装卸载脚本 + `医生`/`新项目`/`教程` |
| 测试 | 559 项全绿 |
| 文档 | 语言规格 v1.0（冻结）+ 教程 10 章 + 设计决策 D1–D23 |

### 1.2 实测发现的真实不足（「找到自身不足」的第一手证据）

> 以下是 2026-09-05 逐场景实测的结果，是本次路线最重要的输入。
> 它们对应新定位的核心诉求「**出问题能快速找到**」。

#### 缺口 A：递归超限错误没有被中文化（最典型）

```jsh
函数 递归(n):
    返回 递归(n + 1)
递归(1)
```

实际输出：

```
错误：程序内部出现未预料的问题（RecursionError）：maximum recursion depth exceeded
```

**问题**：既没有中文解释，也没有「是不是忘了写结束条件？」的建议。
而 `jishi/errors.py` 里其实**已经写了**翻译逻辑
（`RecursionError → "递归层数太深了，是不是函数忘了写结束条件？"`），
但实际执行时这个异常没走到翻译分支，落到了 CLI 的兜底 `except Exception`。

> **根因**：`RecursionError` 在解释器递归调用 `exec_block` 时直接抛到顶层，
> 绕过了 `_as_jishi_error` 的翻译路径。这是一个**真实 bug，不是设计缺失**。

#### 缺口 B：沙箱/统一协议里的错误缺行列与建议

同样的递归超限，在沙箱里：

```json
{"ok": false, "error": {"code": "E2000", "message": "RecursionError: maximum recursion depth exceeded",
                        "line": null, "col": null, "hint": null}}
```

死循环超时：

```json
{"ok": false, "error": {"code": "E2000", "message": "执行超时（2 秒）", "line": null, "col": null, "hint": null}}
```

**问题**：`line`/`col`/`hint` 全是 `null`。对 agent 或用户来说，
「执行超时」没有告诉它**哪里**可能有问题、**怎么改**。

#### 缺口 C：没有调用栈回溯（深层调用链看不出「谁调用了谁」）

```jsh
函数 里层():
    令 x = [1, 2]
    打印(x[10])      # 这里越界
函数 中层():
    里层()
函数 外层():
    中层()
外层()
```

实际只输出越界那一行：

```
错误 E2003：索引越界
  ┌─ _t.jsh:3:9
  3 │     打印(x[10])
```

**问题**：看不到「`外层` → `中层` → `里层`」的调用链。用户定位问题时，
只能靠猜是哪个调用路径触发的。**这正是「出问题快速找到」的最大短板。**

#### 缺口 D：REPL 里错误不显示源码行

在 REPL 里 `打印(1/0)`，输出：

```
错误 E2001：不能除以零
  ┌─ <输入>:1:5
  │
  │          ← 这里本该显示源码行，但空了
```

**问题**：交互式输入没有缓存源码行，导致 `^` 指示和源码显示不出来。

#### 缺口 E：真实项目反哺的 5 个语法别扭点（M15 已记录未修）

无切片 / `最小`只收单列表 / `导入 文本`遮蔽内建 / 正则定界符 / 无 argv，
详见 `examples/projects/README.md`。

### 1.3 核心判断

> **新定位下的三条主线**，按重要性排序：
>
> 1. **出问题能快速找到** —— 缺口 A/B/C/D 全是这一条，是「语言自身健全」的最直接体现。
> 2. **方便实现意图** —— 缺口 E 的 5 个语法别扭点，真实写过项目才痛。
> 3. **拿得到手、跑得起来** —— 三平台发行、开发者工具、生态。
>
> 「AI 友好」作为辅助线，随这三条主线自然生长。

---

## 二、优先级框架（准入三关）

每个候选特性必须同时过三关，任一不过则降级：

```
┌─ 关 1：真实需求驱动
│    来自实测缺口（1.2 节）或真实项目反哺，而非「别的语言有所以我们也要有」
├─ 关 2：可度量验收
│    说得清「怎么算完成」，否则不开工
└─ 关 3：投入产出比
     用户是业余投入，优先「低成本高收益」，警惕「大工程情怀项目」
```

**排序原则**：先补缺口（排错体验）→ 再修痛点（表达便利）→ 再扩平台 → 再工具生态。

---

## 三、第一梯队：诊断与排错体验（最高优先级）

> 对应新定位诉求「**出问题能快速找到**」，直接修复 1.2 节的缺口 A/B/C/D。
> 这些是「语言自身健全」的底线，成本不高、收益立竿见影。

### M17 报错体验补全

#### 17.1 修复递归超限未翻译的 bug（缺口 A）

- 让 `RecursionError` 走通 `translate_python_exception` 的翻译路径
  （`errors.py` 已有翻译，修复传递链路即可）
- 预期输出：

```
错误 E2000：递归层数太深了，是不是函数忘了写结束条件？
  ┌─ _t.jsh:2:12
  2 │     返回 递归(n + 1)
    │            ^
```

- 同步修三执行器（树遍历 / Python VM / C VM），加对拍测试

#### 17.2 调用栈回溯（缺口 C，本梯队最有价值的一项）

- 给解释器维护一个**调用帧栈**：每次进入用户函数时压入
  `(函数名, 调用位置 line/col)`，出错时随错误一起渲染
- 预期输出形态：

```
错误 E2003：索引越界
  ┌─ _t.jsh:3:9
  3 │     打印(x[10])
    │         ^
调用链：
  外层 (第 7 行) → 中层 (第 5 行) → 里层 (第 3 行)
```

- **实现要点**：
  - `Interpreter` 增加 `self._call_stack: list[Frame]`
  - 进入 `_eval_call` 的用户函数分支时压栈，`finally` 里弹栈
  - `JishiError` 增加 `trace: list[Frame]` 字段，`render()` 里渲染
  - 控制深度（比如只显示最近 20 帧），避免深层递归刷屏
- **边界诚实**：三执行器都要做，C VM 需要额外的宿主回调传帧信息，工作量大，
  可分两阶段——先树遍历解释器（默认执行器），C VM 后续跟上

#### 17.3 沙箱/协议错误补全行列与建议（缺口 B）

- 超时错误：`line`/`col` 填「当前执行位置」（若可获取），
  `hint` 填「可能是有死循环，检查循环条件或递归结束条件」
- 递归超限：复用 17.1 的中文翻译，行列来自调用帧栈
- 让 `--json-result` 的错误对 agent 同样友好（不只是人看的报错）

#### 17.4 REPL 错误显示源码行（缺口 D）

- REPL 缓存最近输入的多行源码，出错时填入 `source_line`
- 让 REPL 的 `^` 指示和源码显示与文件模式一致

**验收**：四个缺口各配一个回归测试，报错质量肉眼可见地提升。
**验收脚本**：把 1.2 节的四段代码跑一遍，输出应包含中文解释 + 定位 + 建议。

---

## 四、第二梯队：表达便利（「方便实现意图」）

> 对应缺口 E，来自 M15 真实项目反哺，是「写过项目才知道的痛」。

### M18 语法体验补全

#### 18.1 切片语法（第一痛点）

```jsh
列表[1:3]      # 取子列表
列表[::-1]     # 倒序（当前要写 反转(列表)）
列表[:2]       # 前两个
列表[2:]       # 从第 3 个到末尾
```

- 实现难点：tokenizer/parser 要在方括号里区分「字典字面量冒号」与「切片冒号」
- 保守方案：先只支持「返回新列表」的切片，不支持切片赋值（降低复杂度）
- 三执行器同步 + 对拍

#### 18.2 命令行参数（argv）

```jsh
# jishi 分析.jsh 成绩.csv --排序 分数
导入 系统
打印(系统.参数())      # ["成绩.csv", "--排序", "分数"]
```

- 让真实项目告别「`输入()` 交互式读路径」
- 只给原始参数列表，复杂解析交给标准库或用户

#### 18.3 `最小`/`最大` 支持多参数

```jsh
最小(5, 3)        # 3（当前报错）
最小([5, 3])      # 3（现有行为，保持兼容）
最小(5, 3, 9)     # 3（变长）
```

#### 18.4 模块遮蔽治理

`导入 文本` 会遮蔽内建 `文本()`（M15 日志统计项目真实踩过）：

- 方案 A（推荐）：导入时若模块名与内建同名，打印一次中文提示
- 方案 B：改名（breaking change，留 2.0）
- 方案 C：只补文档（成本最低，但用户还会踩）

**验收**：每个特性配测试；用新语法重写 `examples/projects/` 三个项目，代码应变短。

---

## 五、第三梯队：平台与分发（「拿得到手」）

### M19 三平台发行 + 自动化

- GitHub Actions 构建矩阵：windows / ubuntu / macOS（x64 + arm64）
- 打 tag 即触发：构建 → 跑冒烟 → 上传 Release（复用 M16.4 已建能力）
- 冒烟测试自动化：把 `docs/冒烟清单.md` 里可自动化的步骤做成 CI 脚本
- **诚实边界**：macOS 未签名需在发布说明注明；CI 分钟数成本可接受

---

## 六、第四梯队：性能与开发者工具（按需推进）

### M20 性能（先验证 ROI，不达标就放弃）

- 短字符串内联：先做最小原型测收益，<1.5x 则明确放弃并记录
- 更多热路径下沉 C VM（参考 M11 的 `LIST_APPEND` 经验）
- Rust 宿主完整化（`Rc<RefCell>` 重构可变容器语义）
- JS VM 性能补测（`docs/embed.md` 里「待测」如实填上）

### M21 开发者工具（决定「写起来爽不爽」，按需挑选）

- **LSP 语言服务器**（最高价值）：补全/跳转/悬停文档/实时诊断
- 调试器：断点/单步/查看变量（M4 已带行列，基础具备）
- 格式化工具 `jishi 格式化`
- `jishi 测试` / `jishi 文档`

### M22 包生态（网络效应）

- 远端包索引静态托管（Gitee Pages / GitHub Pages），`jishi 安装 包名` 开箱即用
- 包发布工具 `jishi 发布`（打包 → 生成索引条目 → 提交）
- 依赖解析增强：钻石依赖冲突检测（A→B→D@1.0、A→C→D@2.0）
- 官方包（按需）：`网页`/`数据库`/`测试`（每个都要真实需求驱动）

---

## 七、AI 友好（辅助线，带着走）

> 不放弃，但不再作为主攻。随三条主线自然生长。

- **评测集持续维护**：每修一个语法点，同步补评测题，防止回归
- **语言卡 / few-shot 随语法演进更新**：18.1 切片、18.3 多参数等落地后同步
- **容错解析**：继续作为「降低上手门槛」的常设能力，遇到高频英文写法就补
- **真实模型评测**：**有机会（有 API key、有预算）时做一次**，作为对外材料；
  但不再作为路线卡点

---

## 八、明确「不做」的事（克制清单）

| 不做 | 理由 |
|------|------|
| 自研垃圾回收 | Python 前端已有 GC，自研是情怀项目，ROI 极低 |
| JIT 编译 | 已有 C VM；工程量大且教学/工具场景性能足够 |
| 完整静态类型系统 | 与「简单易用」定位冲突；可选标注（待评估）而非强类型 |
| 多线程 / 异步并发 | 严重破坏简单性，GIL 限制难绕过 |
| 宏系统 | 中文编程语言受众不需要元编程复杂度 |
| 复刻 Python 全部标准库 | 已有 `导入 x 从 python` 桥接，重造轮子违背 D5 |
| 追求「完全自主」 | 木兰的教训；D1 已定：技术栈透明，不 fork/patch CPython |

---

## 九、度量体系（防自嗨）

### 9.1 核心指标（对应新定位）

| 指标 | 定义 | 现状 | 目标（M17 后） |
|------|------|------|---------------|
| **报错可定位率** | 报错含行列 + 中文解释的比例 | 部分场景缺（缺口 A/B/C/D） | 100% |
| **调用链可见率** | 深层错误能看到调用链的比例 | 0%（无调用栈） | 100% |
| **真实项目代码行数** | 三个项目用新语法重写后的总行数 | 基线 | 下降（越短越「方便实现意图」） |

### 9.2 健康指标

| 指标 | 现状 | 目标 |
|------|------|------|
| 测试数 | 559 | 随特性增长，保持全绿 |
| 三平台发行包 | 仅 Windows | 三平台齐备（M19） |
| 教程完成率 | 10 章 | 补齐 LSP/调试等新章节 |

### 9.3 纪律

- **每个里程碑必须有可度量验收**，说不出「怎么算完成」就不开工
- **未度量的指标一律标注「未度量」**，不用估计值充数
- **不利数据照实公布**，不做选择性呈现

---

## 十、风险与诚实边界

| 风险 | 说明 | 应对 |
|------|------|------|
| 调用栈回溯跨三执行器工作量大 | C VM 需宿主回调传帧 | 分两阶段：先树遍历（默认），C VM 后续 |
| 特性蔓延 | 加太多语法破坏「简单」 | 第八节「不做清单」+ 准入三关 |
| 用户精力有限 | 业余投入，路线需克制 | 按梯队推进，允许长期停在某一梯队 |
| 三平台 CI 成本 | macOS runner 分钟数消耗大 | 可接受；或 macOS 版手动构建 |
| 杀软误报 | PyInstaller 产物常见 | 已有 wheel/源码替代途径，不承诺绝对不报毒 |

---

## 十一、建议推进顺序（一句话版）

```
M17 排错体验（修递归翻译 bug + 调用栈 + 协议补全 + REPL 源码行）  ← 现在就做
  ↓
M18 表达便利（切片 / argv / 最小二参 / 遮蔽治理）
  ↓
M19 三平台发行
  ↓
M20 性能 → M21 开发工具 → M22 生态（按需）
  ↓
AI 友好：随三条主线自然生长，有机会再跑真实评测
```

**如果只能做一件事**：做 **M17 的 17.2 调用栈回溯**。
它是「出问题快速找到」的核心，也是当前最影响实际使用体验的短板——
没有它，用户在深层调用出错时只能靠猜。

---

_本路线随实践修订；每次调整在 `docs/设计决策.md` 补充决策记录。_
