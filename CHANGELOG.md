# 变更记录（CHANGELOG）

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)：
主版本（语言不兼容变更）/ 次版本（新特性）/ 修订（修 bug）。

## [0.1.4] - 2026-09-12

包生态（M22）：从「能装本地包」走到「装包不用带参数」。

### 新增特性

- **默认包索引（M22.1）**：`jishi 安装 包名` 现在开箱即用，不必再传
  `--from-index`。索引是静态 JSON，托管在本项目公开仓库里（无需服务端），
  可用 `--index` 或环境变量 `JISHI_INDEX` 指到自建索引；
  `--no-index` 可关闭联网行为。
- **发布工具（M22.2）**：
  - `jishi 发布 <包目录>`：校验 `包.json` → 打包 zip → 算 sha256 →
    生成索引条目；`--index` 可直接合并进索引文件
  - `jishi 索引 <目录>`：扫描多个包批量重建索引（`--base-url` 指定下载前缀）
  - 打包只收 `包.json` 与 `.jsh`，`__pycache__` 等噪音不会进包
  - `包.json` 字段名会校验：拼错（如把「依赖」写成「依赖项」）直接报错
- **安装校验和**：索引带 `校验和` 时逐字节校验，对不上即拒装。
- **钻石依赖冲突检测（M22.3）**：`jishi 检查` 遍历依赖图，报告
  「谁以什么约束要求了它」的完整链条；运行期约束失败时提示跑 `jishi 检查`。

### 修复

- **Windows 下中文源码经 stdin 传入变乱码**（发版前实测发现）：打包版不继承
  `PYTHONUTF8`，`sys.stdin` 按 GBK 解码，`打印` 变成 `鎵撳嵃`。修复：
  `cli.py` 改按「字节 → UTF-8」读 stdin，`mcp_server.py` 的 JSON-RPC 读取与
  子进程传参同理；入口脚本三个流全部显式 UTF-8 + `errors=replace`。
- **报错渲染遇非法码位会崩成英文堆栈**：源码里混进孤立代理字符时，
  渲染中文报错会 `UnicodeEncodeError`，最终用户看到 PyInstaller 的英文报错。
  修复：错误渲染统一做 `_safe_text` 兜底。
- **打包版 MCP 无法执行脚本**：打包后 `sys.executable` 是 `jishi-mcp.exe`
  自己，`-m jishi.cli` 直接失败。修复：frozen 模式下改用发行目录同级的
  `jishi` 可执行文件。
- 索引/下载地址含中文时未做 URL 编码、已编码地址被二次编码为 `%25`。
- `jishi 检查` 遇到循环依赖会无限递归。

### 测试

- 新增 55 项（M22 生态 36 项 + 编码健壮性 19 项，含真实 MCP 中文往返与
  本地 HTTP 服务闭环），全量 761 项通过。

## [0.1.3] - 2026-09-12

开发者工具（M21）：语言服务器 + 代码格式化，把「写起来爽不爽」补上。

### 新增特性

- **语言服务器 `jishi lsp`（M21.2）**：stdio 上的 LSP，编辑器接入后即可
  - **实时诊断**：打开/改动即重新分析，把中文报错（错误码 + 行列 + 修改提示）
    推给编辑器；另附保守的「名字没定义过」警告
  - **补全**：关键字 / 内建函数 / 标准库模块 / 文档内符号；
    `随机.` 之后给该模块的函数与签名
  - **悬停**：关键字、内建函数、标准库模块与函数、文档内符号的中文说明
  - **跳转定义**：跳到文档内定义的函数 / 类 / 变量 / 导入
  - **文档符号**：大纲视图
- **代码格式化 `jishi 格式化`（M21.1）**：
  - 统一缩进（默认 4 空格，`--indent` 可调）与词间间距
  - **保留注释与字符串原样**：`#` 行注释、`#- -#` 块注释、三引号字符串、
    中文引号与转义都不动
  - `--write` 原地写回、`--check` 供 CI 校验（未格式化退出码 1）、
    `--stdin` 管道输入
  - **幂等**且**语义不变**：格式化结果再格式化不变；全语料 token 流逐一相等

### 测试

- 新增 111 项测试（格式化 90 项 + LSP 21 项），全量 706 项通过。

## [0.1.2] - 2026-09-12

性能里程碑：用 benchmark + cProfile 定位并消除了 C VM 的主要瓶颈。

### 性能

- **批量 JVal 转换（M20）**：C VM 的迭代/函数调用返回值原先逐个跨 ctypes
  边界构造 `JVal`（≈1µs/值）。现在整批为整数时用一次 `struct.pack` + `memmove`
  写入 C 侧数组（转换快 ~9x；混合类型 / 大端 / 溢出自动回退逐个）。
- 相对树遍历解释器的提速变化（`tools/benchmark.py`）：

  | 场景 | 优化前 | 优化后 |
  |------|--------|--------|
  | 整数循环累加 | 1.9x | **8.4x** |
  | 算术混合运算 | 5.2x | **18.1x** |
  | 函数调用密集 | 4.5x | **10.7x** |
  | 列表构建与遍历 | 1.6x | **2.6x** |
  | 当循环倒计数 | 44.4x | 45.9x |

- 独立验证：`遍历 范围(20000)` 从 16.5ms 降到 3.8ms（4.4x）。

## [0.1.1] - 2026-09-12

「健全语言自身、方便实现意图、出问题能快速定位」——排错体验与语言表达力双升级，
并改进发行方式（提供 Windows 安装程序、三平台自动构建）。

### 新增特性

- **切片语法（M18）**：`列表[1:3]`、`[::-1]`、`[::2]`、`[:2]`、`[2:]`、`[:]`，
  字符串同样支持；三执行器（树遍历 / Python VM / C VM）语义一致
- **命令行参数（M18）**：`系统.参数()` 读取 `jishi 脚本.jsh 参数…`；
  `--` 之后的内容原样作为脚本参数
- **`最小`/`最大` 多参数（M18）**：支持 `最小(a, b)`、`最小(a, b, c)`，
  单列表调用保持兼容
- **导入别名与遮蔽治理（M18）**：普通导入支持 `为 别名`（`导入 文本 为 t`）；
  模块名与内建同名时导入会给出一次中文提示

### 排错体验（M17）

- **调用栈回溯**：运行期错误附带「调用链（从外到内）」，标明每层函数在第几行被调用；
  协议 JSON 同步携带 `trace` 字段
- **递归超限中文化**：修复 `RecursionError` 绕过中文翻译的问题，
  现在输出「递归层数太深了」+ 调用链 + 修改建议
- **协议错误补全**：沙箱超时给出「可能是有死循环」提示；错误不再缺 `line`/`col`/`hint`
- **REPL 报错显示源码行**：交互式报错与文件模式一致，显示源码行与 `^` 指示

### 发行改进（M19）

- **Windows 安装程序**：Inno Setup 生成 `setup.exe`（用户级安装、中文向导、
  开始菜单、可选 PATH、标准卸载），不再只提供压缩包
- **三平台自动构建**：GitHub Actions 打 tag 触发 windows / linux / macOS 构建，
  自动创建 Release 并附 SHA256SUMS

### 修复

- 递归超限错误未走中文翻译链路（落到英文兜底）

## [0.1.0] - 2026-09-05

首个对外发行版。语言已可用、AI 工具链齐备、三执行器语义一致、
可独立安装运行（免 Python 环境）。

### 新增特性

- **语言核心**：变量 / 分支 / 循环 / 函数 / 类继承 / 异常，中文报错精确到行列
- **表达力（M7）**：多赋值解包、默认参数、列表/字典推导式、文本插值
- **三执行器**：树遍历解释器（默认）/ Python 字节码 VM / C 字节码 VM，
  107 项对拍逐字节一致
- **Python 生态桥接（M3）**：`导入 x 从 python` 直用整个 Python 生态，
  关键字参数、异常自动翻译中文
- **14 个标准库**：随机 / 数学 / 时间 / 文件 / 表格 / 文本 / json / 日期 /
  正则 / 网络 / 路径 / 加密 / 压缩 / 系统（中文薄壳，零第三方依赖）
- **AI 原生工具链（M8/M12）**：语言卡（`--ai-card`）、机器可读语言规格
  （`--lang-spec`）、结构化报错（`--json-errors`，含 fix 自愈）、
  few-shot 45 对、50 题评测集
- **安全执行（M9）**：沙箱（超时/输出上限/模块白名单）、统一协议
  （`--json-result`）、MCP Server（`jishi-mcp`）、C VM 嵌入 ABI
- **跨语言字节码分发（M13）**：`--dump-bytecode` 产出 JSON 字节码，
  Node/Rust 宿主直嵌引擎
- **包管理（M14）**：`jishi 安装/卸载/列表`，包依赖解析 + 版本约束 + 循环检测
- **发行版（M16）**：独立可执行文件（PyInstaller），安装/卸载脚本，
  `jishi 医生` 环境自检，`jishi 新项目` 脚手架

### 已知限制

- 无切片语法（`[::-1]` 用 `反转()` 代替）、`最小`/`最大` 只收单列表、
  无命令行参数 argv（详见 `docs/语言规格.md`「已知边界」）
- Rust 宿主为「最小可运行」（可变容器语义待 Rc<RefCell> 重构）
- 发行包体积较大（内含 Python 运行时），PyInstaller 产物可能被杀软误报

### 升级指引

- 首个版本，无升级路径。
- 0.x 阶段允许 breaking change，每个版本在发布说明列清变更与迁移指引。

---

版本格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
