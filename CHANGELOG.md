# 变更记录（CHANGELOG）

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)：
主版本（语言不兼容变更）/ 次版本（新特性）/ 修订（修 bug）。

## [0.1.23] - 2026-09-19

M41：**方向一 · 语言能力第二轮**。标准库补 5 个模块（五路同宽）、内建
`带下标`/`配对`、`遍历` 多变量解包、错误归类修正、C VM 性能定位；
顺带做了一次 few-shot 示例体检（发现 4 段真错）。

### 新增 · 标准库（5 个模块，**五路同宽**：三执行器 + Node + Rust）

| 模块 | 内容 |
|------|------|
| `容器` | 计数 / 最多 / 按值排序 / 分组 / 取前 / 取后 / 分块 |
| `迭代` | 分组 / 滑窗 / 去重 / 展开 / 取前 / 取后 / 求和 / 计数 |
| `参数` | 新建 / 选项 / 标志 / 用法 / 解析（命令行**选项**解析） |
| `日志` | 级别们 / 设级别 / 取级别 / 设前缀 / 调试 / 信息 / 警告 / 错误 |
| `测试` | 用例 / 相等 / 不等 / 为真 / 为假 / 近似 / 包含 / 抛出 / 统计 / 汇总 / 重置 |

> **模块名是「容器」不是「集合」**：`集合` 是内建函数名（`集合([1,2,3])` 去重造
> 集合），同名模块一 `导入` 就把它遮蔽——正是 M40 修掉的 f-string 遮蔽坑同一类
> 问题。这条已写进设计决策（D35）与 AGENTS §2：**标准库模块名先查内建名与关键字**。

```jsh
导入 容器
令 词们 = ["甲", "乙", "甲", "丙", "甲", "乙"]
打印(容器.计数(词们))          # {'甲': 3, '乙': 2, '丙': 1}
打印(容器.最多(词们))          # 甲
打印(容器.按值排序(容器.计数(词们)))   # [['甲', 3], ['乙', 2], ['丙', 1]]

导入 测试
测试.用例("加法", 函数()：
    测试.相等(1 + 1, 2))
测试.汇总()                    # ✅ 全部通过（1 个用例）；有失败则退出码 1
```

### 新增 · 内建 `带下标` / `配对`（同 Python 的 `enumerate` / `zip`）

```jsh
打印(带下标(["甲", "乙"], 1))     # [[1, '甲'], [2, '乙']]
打印(配对([1, 2, 3], ["a", "b"]))  # 取最短 → [[1, 'a'], [2, 'b']]
```

### 新增 · `遍历` 支持多变量解包（解析期脱糖）

```jsh
遍历 名字, 年龄 在 [["小明", 8], ["小红", 9]]：
    打印(名字, 年龄)
```

走**解析期脱糖**（临时循环变量 + 循环体首句解包赋值），**三执行器与两宿主
一行都不用改**（D36）。`*余` 也可用，语义与 `令 甲, *余 = …` 一致。

### 新增 · `路径.递归列出(目录, 后缀)`

返回**相对路径**列表（已排序、只列文件）。原先写 `基石自述站` 时数
`examples/` 下的 .jsh 要手写递归（Python 一个 `rglob`）。

### 修正 · 错误归类（对齐 Python）

`整数("abc")` 原先抛「类型错误」，而 Python 是 `ValueError`——写
`捕获 值错误` **接不住**。现在：**文本转不动 → 值错误；类型本身不对
（列表/字典/集合）→ 类型错误**，与既有的 `"abc".转整数()` 归到同一类。
`待办清单` 里那句 `捕获 异常` 已收窄成 `捕获 值错误`。五路一致。

### 性能 · C VM 瓶颈定位（按 M20 的做法先 cProfile，不猜）

结论：瓶颈**仍是 ctypes 边界**（各场景占比 58%–87%，字典操作高达 86.8%）。
其中「批量迭代」回调里有一处**两趟遍历**（先 `all(...)` 预扫描、再构造 flat），
改成**一趟**（边判边装，遇非整数抛内部哨兵回退）：

| 场景 | 优化前 | 优化后 |
|------|--------|--------|
| 整数循环累加 | 9.5x | **10.5x** |
| 函数调用密集 | 13.3x | **15.7x** |
| 递归斐波那契 | 21.2x | 20.6x |
| 算术混合运算 | 24.2x | 23.4x |
| 字符串拼接 | 0.45x | **0.50x**（架构边界，如实记录） |

> **这条优化差点引入一个静默回归**：内部哨兵第一版写成**实例**而非类，
> `except (A, B, 哨兵)` 在运行时炸成「catching classes that do not inherit
> from BaseException」——而且**只在 C VM 遇到「批量里混了非整数」时触发**
> （`遍历 [[1,2],[3,4]]` 立刻中招，单层整数列表一路正常）。五路对拍抓住了它。

### 真实项目 · 用新模块替换绕道写法，行数比 1.27 → **1.21**

| 项目 | 场景 | 行数比 |
|------|------|--------|
| 网页采集 | 网页采集 | **1.02**（最接近） |
| 日志统计 | 数据处理 | 1.18 |
| 待办清单 | CLI 工具 | 1.19 |
| 成绩分析 | 数据处理 | 1.21 |
| 排序跑分 | 算法练习 | 1.24 |
| 基石自述站 | 教学演示 | **1.26**（差最多） |
| **加权平均** | | **1.21** |

- `日志统计`：手写的「有就加一、没有就置一」+ 自己排序 → `容器.计数` /
  `容器.按值排序` / `容器.取前`。
- `排序跑分`：手写的 `对齐()` 与 `小数位()` → `文本.左对齐` / `数学.四舍五入`。
- `待办清单`：`遍历 i 在 范围(长度(…))` + 按下标取 → `遍历 序号, 项 在 带下标(…)`。

### 体检 · `examples/ai/few-shot.md` 48 段示例（计划外）

这些示例是喂给 AI 的「正确写法」样本，但**从没人跑过**。逐段执行发现
**4 段是真错**：

| 示例 | 错在哪 |
|------|--------|
| 26 正则匹配 | 变量名叫 `文本`，遮蔽了内建 `文本(...)` |
| 29 闭包计数器 | 内层函数给外层变量赋值（M40 起算新建局部变量），改成可变容器写法 |
| 34 多值解包循环 | `遍历 名字, 年龄 在 …` 当时不支持（M41 起支持，示例现在真能跑） |
| 37 排序字典按值 | `频次.键值对()` 这个方法**不存在**，改用 `容器.按值排序` |

已全部修好，并加 `tests/test_m41_stdlib.py::test_fewshot_examples_are_valid`
**逐段钉住**（片段不自包含的按「环境缺失」放行，语法/API 错误必须报出来）。

### 文档

- `docs/真实项目.md`：别扭点清单收口复盘（10 条已收 7 条，另 2 条暴露
  **「能力早就有、只是不知道」**——`数学.四舍五入`、`文本.左对齐`、
  集合的交并差，这给 M42–M45 两个外功方向提供了新靶子）。
- `docs/语言规格.md`：循环节补解包遍历；内建表补 `带下标`/`配对`；
  标准库表补 5 个新模块与 `路径.递归列出`。
- `docs/embed.md`：标准库 14 → **19 个模块**。
- `docs/设计决策.md`：新增 **D35–D38**。
- `AGENTS.md §2`：新增 5 条硬约束（`except` 元组不能放实例、模块名查内建、
  有状态模块每次导入造一份、三执行器验证别用 CLI 的 `--执行器`、
  AI 示例也要进测试）。

### 测试

新增 `tests/test_m41_stdlib.py`（106 项）、`test_m21_lsp.py` 加 3 条解包钉子、
`test_m5_exceptions.py` 跟着错误归类改了 2 条。全量 **1779 → 1888 项**。

## [0.1.22] - 2026-09-19

**M40：三个穿插项收尾（C VM 调试器 / 「赋值即局部」/ f-string 遮蔽）+ 主线 C
五类真实项目与行数比基线**。M39 记下的「两个已记录待修项」这一版清零。

### 一、C VM 的调试器（B5 最后一格）

树遍历（M37）与 Python VM（M39）早已能调，C VM 一直是「暂不做」。这一版补上，
做法是给 C 侧加**四个只读入口**（`cvm/include/jsvm.h`）：

| 入口 | 作用 |
|------|------|
| `jsvm_set_stmt_marks` | 收下语句起始表（**单独一张 int32 数组**） |
| `jsvm_set_stmt_hook` | 装语句钩子（只在语句首指令回调） |
| `jsvm_frame_meta` | 问一帧的「代码下标 + 调用点」 |
| `jsvm_dump_locals` / `jsvm_dump_globals` | 把帧变量 / 全局交给宿主转换 |

三个刻意的决定：**语句起始表另发一张**（指令宽度是 Node / Rust 也按它读的公开
格式，为一个调试功能改契约不划算）；**钩子用独立 setter**（不动
`jsvm_load_module` 签名，不改已有 ABI）；**值的所有权用「借用」**（不为调试发明
第二套所有权规则）。

```bash
jishi 调试 脚本.jsh --执行器 cvm    # 三个执行器，命令完全一样
```

**验收**：`tests/test_m40_cvm_debugger.py` 让**三个执行器的会话记录逐字相同**
（变量表、调用栈、出错现场、`退出` 行为），覆盖函数 / 循环 / 闭包 / 类方法 /
`尝试·捕获·最终` / 空块 / 回调。

**「退出」是这里最贵的一处**：它要穿 `最终`、又不能被 `捕获` 接住。做法是把
`DebugQuit` 当**卸载类信号**（宿主 `is_signal` 返回 3）交给 C 侧异常分发：
跳过循环、直接跳 `finally_ip`、`END_FINALLY` 后继续向外。为此补了两件事：
`END_FINALLY` 要检查「有没有待重抛的异常」（否则信号被静静吞掉），
`make_error` 不许包装 `DebugQuit`（否则被用户 `捕获` 接住）。

**没做的一件事（明确说明，不给错值）**：**就地求值**（`查看 表达式`）——C 侧
没有「在任意帧上求一个表达式」的入口，要做得把编译好的求值代码塞进当前帧执行，
属另一件事。会话会直说「C 虚拟机暂不支持就地求值」。

**顺带补上的一个缺口**：C VM 的报错**从来没有「调用链」段**（M17 给树遍历与
Python VM 都做了，C VM 漏了——错误语料全是顶层报错，所以一直没暴露）。现在
C 侧在建帧时记调用点，出错时把帧栈拍成快照交给宿主，`errors.Frame` 渲染口径与
另两个执行器**逐字对齐**。新增两条错误语料（`34_函数内报错有调用链`、
`35_赋值前读局部`）钉住。

### 二、树遍历的「赋值即局部」补齐（M39 那条明确分歧）

函数里给**外层同名**变量赋值时，字节码执行器按 Python 的「赋值即局部」报错，
树遍历却按「先向外找、找到就用」静默算出另一个结果——「一边报错一边给值」。
现在进函数时给「本函数体里**被赋过值**的名字」占上未绑定哨兵：

- 赋值**之前**读它 → 报「名字「x」在赋值之前被用到了」（不再穿透到外层）；
- 外层有同名变量也不受影响（内层遮蔽）；
- **只读**外层变量（不赋值）照常闭包捕获——这条最容易改坏，专门有用例。

**两个关键选择**：① 判定「哪些名字是局部」**直接复用编译器的收集器**
（`compiler._collect_stmts` + `_Scope.bound`），不另写一遍——那份逻辑已经决定了
字节码执行器把哪些名字当局部变量，另写一遍迟早会漂；② 报错文案走**单一来源**
（`runtime.unbound_local_error`），三个执行器都调它——对拍比的是
`(输出, 报错全文)` 整对，文案差一个字就判成「执行器不一致」。

M39 用 `xfail(strict=True)` 钉住的那条测试，已按约定转成正常断言。

### 三、f-string 被 `导入 文本` 打断（M39 记录的待修项）

插值原先脱糖成内建 `文本(值)`——而 `文本` 是本项目**最常用的标准库名**，一句
`导入 文本` 就把它遮蔽，于是**所有 f-string 当场报「模块不能直接调用」**。
现在脱糖到 `$文本`：`$` 在词法阶段就是「无法识别的字符」，**用户写不出来**
这个名字，所以永远不会被遮蔽。三个执行器与 Node / Rust 两个宿主都注册它。

配套一条纪律：**内部名不许出现在用户可见的地方**。新增
`runtime.is_internal_builtin`（判据：`$` 开头，或说明以「内部用」开头），
`ai.build_lang_spec()` 与编辑器补全都按它过滤——否则 `$文本` 会被当成可写的
函数推荐给用户（顺带修掉老早就在语言规格里的 `检查实参`）。

### 四、主线 C：五类真实项目 + 行数比基线

`examples/projects/` 补齐五类场景，每个配一份**只用标准库**的 Python 参考实现：

| 类别 | 项目 | 有效行比（基石 ÷ Python） |
|------|------|--------------------------|
| 教学演示 | 基石自述站 | 1.26 |
| CLI 工具 | **待办清单**（新增，JSON 持久化 + 子命令 + 中文错误） | 1.24 |
| 数据处理 | 成绩分析 | 1.21 |
| 算法练习 | **排序跑分**（新增，五种排序 + 自校验 + 计时） | 1.39 |
| 数据处理 | 日志统计 | 1.56 |
| 网页采集 | 网页采集 | 1.02 |
| **加权平均**（合计 688 ÷ 542 有效行） | | **1.27** |

（逐项算术平均 1.28。**最接近的**是网页采集 1.02——它主要在做解析与字符串
处理，两边几乎同构；**差得最多的**是日志统计 1.56，卡在字符串拼接与聚合写法上。）

`tools/loc_ratio.py` 统计（有效行 = 去空行去注释，口径写在 `docs/真实项目.md`），
`--check` 在缺参考实现时退出码 1。文档里同时记了**别扭点清单**——写这五个项目
时真正卡住的地方（`整数("abc")` 抛「类型错误」而非「值错误」、字符串拼 HTML 的
样板多、`字典.获取` 的默认值写法绕…），作为下一轮补语言 / 标准库的输入。

### 五、测试与文档

- 新增 `tests/test_m40_cvm_debugger.py`（24 项，三执行器逐字对拍）、
  `tests/test_m40_scope.py`（22 项，作用域语义 + f-string + 内部名不外露 +
  「新项目本来就合规」的钉子）、
  `tests/test_m40_projects.py`（15 项，五类项目端到端 + 行数比可复现）；
  错误语料 +2；`test_cvm.py` 那条 `xfail` 转正常断言。全量 **1707 → 1888 项**。
- 新增 `docs/真实项目.md`；设计决策新增 **D32 / D33 / D34**；
  `docs/调试器.md` 全面更新（三执行器）；`docs/embed.md` 补 `$文本` 与调试能力两行；
  README / AGENTS / PROGRESS / 路线图同步（路线图 M40 标完成、度量加「行数比 1.27」）。
- 路线图新增「新阶段三方向」（语言能力 / IDE 可用性 / AI 可用性）与 M40–M45 顺序；
  本轮把 M40 的「正确性补课」做完，标准库第一批顺延到 M41。

## [0.1.21] - 2026-09-19

**M39：主线 B5（字节码执行器的调试器）+ 主线 C 开头（用基石写一个静态站生成器）**。

### 一、`jishi 调试 --执行器 vm`：调试器支持字节码执行器

树遍历那条路 M37 就通了；难的是栈机——它只知道指令，没有「语句边界」事件。

做法是**让编译器说话**：`compile_stmt` 在每个语句的**第一条指令**上打一个标记
（`Instr.stmt_line`），VM 主循环只在标记处回调调试器。标记只打首指令是刻意的：
循环的回边（`遍历`→FOR_ITER、`当`→条件、`循环 n 次`→计数器判断）都落在语句**中部**，
于是**循环头只停一次、循环体每轮都停**——与树遍历（一个语句一次 `exec_stmt`）
的暂停点严格对齐。

```bash
jishi 调试 脚本.jsh --执行器 vm       # 命令与默认执行器完全一样
```

**验收方式是「逐字对拍」**：同一份脚本、同一组断点、同一串命令，两个执行器的
会话记录**一字不差**（含变量表、调用栈、出错现场、`退出` 时 `最终` 照跑）。
另有一条全量不变式：**打标行的集合 == AST 语句行的集合**（拿 `examples/` 与
`tests/cases/` 全部语料对拍）。

代价：挂上调试器（哪怕从不暂停）比不挂慢约 13%；不挂时每条指令只多一次
`is not None` 判断。

### 二、C VM 的调试器：判定为暂不做（如实说明）

C VM 的栈帧与局部槽都在 C 侧。要做需要：C 侧在语句标记处回调宿主（`JsHost`
新增字段**必须追加在结构体末尾**，插中间会错位）、再开回调把帧的局部变量读出来、
再约定「退出」如何中断 C 侧执行。而收益只是「同样的体验再支持一个执行器」。
按「别为对称而做」，列为 B 线待办；设计草图写在 `docs/调试器.md`。

### 三、顺手修掉四个潜伏缺陷（共同点：不报错、文档没写、一写就踩）

1. **跨行三引号字符串整个特性原先不可用**：收尾 `"""` 独占一行时词法器不补
   NEWLINE，解析器把下一行并进同一句、报「这一行没写完」。语料里没人真跑过它
   （只有格式化的用例碰到过），所以一直没被发现。→ `tokenizer` 去掉一个多余的
   `and rest`，并补上四种写法的用例。
2. **类体里的方法定义行、空块 `:` 行两个执行器都停不住**：`statement_lines` 说
   它们可停、断点加在上面却从不停——一个**静默失效**的断点。→ 编译器给这些
   「不产生指令的语句」补一条空操作指令挂标记，树遍历也在类体循环里调
   `before_stmt`。
3. **类变量求值顺序与源码顺序不一致**：字节码执行器是「先造类、再按源码顺序求值」，
   树遍历是「按源码顺序求值」。两边都不报错，只有带副作用的类变量才露馅。→
   字节码侧改成「在源码位置求值、存临时槽，类造好后再挂上」，于是调试器看到的
   语句顺序也一致了。
4. **`导入 文本` 会打断 f-string**：f-string 插值脱糖成 `文本(值)`，而模块「文本」
   正好遮蔽这个内建，于是任何 f-string 都报「模块「文本」不能直接调用」。
   **尚未修**（正解要动 Python / Node / Rust 三个宿主，给插值一个不会被遮蔽的
   内部转换）——先用 `导入 文本 为 别名` 绕开，并写进路线图与 AGENTS。

### 四、一条「明确分歧」被对拍抓出来（如实标注，不静默）

函数里给**外层同名变量**赋值时，字节码执行器按「赋值即局部」报「找不到这个名字」，
树遍历却按「先向外找、找到就用」静默算出另一个结果。修树遍历要加一遍
「赋值名收集」（语义改动，得配全量对拍），所以这一版只标注不修：
`tests/test_cvm.py` 用 `xfail(strict=True)` 钉住现状——**哪天补齐了，这条测试会
失败并提醒改成正常断言**。

### 五、主线 C 开头：用基石写的静态站生成器

`examples/projects/基石自述站/生成.jsh`：读仓库里的真数据（版本号来自
`pyproject.toml`、测试数与能力探针来自 `README.md`、里程碑来自 `PROGRESS.md`、
模块规模与示例数是当场统计的），生成一套介绍基石的静态网页
（概览 / 里程碑 / 代码规模 / 上手 + 样式表）。

它一次验收三件事：真实项目能不能写、三个执行器产物是否一致（**逐字节相同**）、
调试器有没有靶子（B5 就是在它上面试的）。源码自己也守项目风格：
`jishi 格式化 --check` 全绿（用基石自己的格式化器改写过一遍）。

### 六、测试与文档

- 新增 `tests/test_m39_vm_debugger.py`（75 项，含两个执行器的逐字对拍）、
  `tests/test_m39_selfsite.py`（6 项，含产物跨执行器对比）、
  `tests/test_tokenizer.py` 加 4 项跨行三引号用例、`tests/test_cvm.py` 加 1 项
  xfail 钉住已知分歧。全量 1618 → **1707 项**（1706 通过 + 1 xfail）。
- 新增 `docs/设计决策.md` D30（怎么让栈机有语句边界 + C VM 判定）、
  D31（四个潜伏缺陷 + 一条分歧的处置）；`docs/调试器.md` 全面更新
  （两个执行器、对拍承诺、C VM 边界与草图、实现说明）；README / AGENTS /
  PROGRESS / 路线图同步；站点加「基石自述」并重建。

## [0.1.20] - 2026-09-19

**M38 主线 B 收尾：LSP 增量同步（B2）+ REPL 增强（B3）+ 格式化器边界（B4）**。
主线 B「可信赖的工具链」到这一版收齐（B1 调试器见 0.1.19）。

### 一、B2：LSP 增量同步（先量了再说）

以前每收到一次编辑就把整份文件 tokenize + parse 一遍。实测每敲一个字符的成本：
253 行 9.9ms、1253 行 44ms、5003 行 182ms、20003 行 759ms——**一千行以上就有
可感的卡顿**，路线图那句「大文件有卡顿」实测坐实。

三件事：

1. **协议层**：`textDocumentSync.change = 2`（增量），客户端只发改动的那一段，
   服务端按区间应用（行列按 LSP 默认的 **UTF-16 码元**换算，emoji 一类天文平面
   字符算 2）；不带 range 的全量替换仍然兼容。
2. **版本乱序保护**：收到的版本不比当前新时直接忽略——否则编辑器重发旧版本时
   诊断会「改完又变回去」。
3. **按行复用前缀的增量分词**（新模块 `jishi.incremental`）：编辑只影响改动行
   **之后**的内容，前缀的 token 与每行的入行状态（缩进栈 / 括号深度 / 跨行三引号
   状态 / 是否在多行注释里）都能复用（依据记在 `tokenizer.LineAnchor`）。
   实测改末行 18001 行从 359.7ms 降到 **8.5ms**（40 倍以上），改中间降约 1.9 倍。

**正确性靠对拍**：随机编辑下增量结果必须与全量分词逐项相同（含三引号内部、
括号续行内部、多行注释内部、缩进错等形状）。

### 二、B3：REPL 增强（历史 / Tab 补全 / 多行编辑）

- **历史**：↑/↓ 翻，**跨会话**（落盘 `~/.jishi/repl_history.txt`，`JISHI_HISTORY`
  可改位置）；相邻重复不重复记，上限 500 条；新增 `历史` 命令。
- **Tab 补全**：关键字 / 内建 / 标准库模块 / **本次会话定义过的变量**；
  `模块.` 给该模块函数，`对象.` 给方法；唯一命中直接替换，多个命中补到共同前缀。
  候选数据来自 `ai.build_lang_spec()`（经新的 `jishi.langdata` 提炼层），
  REPL **不另存一份**语言元数据。
- **多行编辑更准**：引号（含三引号、反引号插值）没写完也续行，判定交给**真词法器**
  而不是自己数引号。
- 平台差异收在最外层：Windows 自己实现逐键编辑（`msvcrt` + 整行重绘），
  POSIX 交给 `readline`，**非终端一律退回 `input()`**（脚本与既有测试行为不变）。

### 三、B4：格式化器边界（修掉四个真问题）

1. **字符串里的 `#` 被当注释**：`令 甲 = "含 # 号"` 被从中间劈开、直接报
   「字符串没有正常结束」（插值字符串同理）。注释识别改成状态机扫描，
   并把括号计数并进同一个扫描器。
2. **括号续行里的注释被吃掉**（与「保留注释」的承诺相悖）。
3. **带 BOM 的文件直接崩**：Windows 记事本存的 UTF-8 带 BOM 文件连跑都跑不起来。
   现在词法器只剥**开头那一个** BOM（中间的仍报错），格式化器保留它。
4. **`jishi 格式化 <目录>` 甩英文 traceback**：README 里写的 `--check examples/`
   一直抛 `PermissionError`。现在目录递归处理，并把「没格式化」与
   「处理不了（词法错 / 读不了）」分开报。

顺带修了一个潜伏很久的词法器 bug：`content_start` 在「括号内续行」分支没有赋值
（用上一轮残留值），**顶格的续行会被整行跳过**——是 B2 从 `bracket_depth > 0`
续跑时以 `UnboundLocalError` 暴露出来的。另外三引号字符串未闭合时词法器
**静默丢弃**整段内容（既不报错也不续行），现在明确报「字符串没有正常结束」。

### 四、文档

新增 `docs/工具链.md`（B2/B3/B4 的现状、实测数据与已知边界，含「parse 仍是全量」
这类不粉饰的标注）；README（工具链能力清单与进度表）、AGENTS（三条新踩坑 +
文档地图）、PROGRESS、路线图（B 表三项转 ✅）同步。
**`docs/语言规格.md` 不用动**——这一版没有改语法（语法面自 M36 起冻结）。

### 五、测试

新增 `tests/test_m38_lsp_incremental.py`（36 项，含随机编辑对拍）、
`tests/test_m38_repl.py`（45 项，含 Windows 逐键路径用假 `msvcrt` 直喂按键）、
`tests/test_m38_formatter_edges.py`（58 项，42 条边界语料 + 目录/CLI）。
全量 **1479 → 1618 项通过**。

## [0.1.19] - 2026-09-16

**M37 主线 B1：调试器**（`jishi 调试 脚本.jsh`）。北极星的后半句
「出问题能快速定位」到这里才算补齐：M17 给了报错与调用栈，现在还多了
断点、单步、看变量、看调用栈。

### 一、用法

```bash
jishi 调试 脚本.jsh                     # 停在第一句，之后听你指挥
jishi 调试 脚本.jsh --断点 12,20         # 直接跑到第 12 行
jishi 调试 脚本.jsh -b 12 --命令 "变量；查看 单价 * 数量；继续"   # 自动跑（演示/脚本化）
```

命令全中文：`继续` / `单步` / `下一步` / `跳出` / `断点 12` / `取消断点 12` /
`变量` / `查看 表达式` / `栈` / `源码` / `帮助` / `退出`；另给 gdb 习惯的
字母别名（`c`/`s`/`n`/`o`/`b`/`v`/`p`/`bt`/`l`/`h`/`q`）；直接回车 = 重复上一条。

### 二、三种单步，与「出错现场」

- `单步` 进函数、`下一步` 不进函数、`跳出` 回调用处——**三种语义全由一个钩子
  推出**（只按「帧深」区分），实现里没有第二处特判。
- **出错现场**：程序出错时先打「出错那一帧的变量 + 调用栈」，再打正式中文报错。
  「错在哪」与「**错的时候变量是什么**」两段互补——后者才是定位问题的关键信息。

### 三、诚实边界（不粉饰）

- **只做树遍历执行器**：字节码 VM 与 C VM 是栈机、没有「语句边界」事件，
  要另做一套（C VM 还得让宿主回调把帧传出来），列为 B 线待办（B5）。
- **块关键字行不能设断点**（`否则：` / `捕获：` / `最终：`）：它们是块的一部分，
  不是独立语句（`否则` 那一行连 AST 节点都没有）。加断点时会明确提示
  「这一行没有可停的语句」，**不静默无效**。
- **从 Python 侧回调进来的基石函数不会在其中暂停**（如 `列表.映射` 的回调），
  与 M17 错误调用栈同一限制。

### 四、实现要点（四处踩过的坑）

1. **钩子只挂一处**：`interpreter.exec_stmt` 开头的 `before_stmt`，加上
   `_eval_call` 里的 `push_frame`/`pop_frame`。它们**只读不写**，所以「带调试器跑」
   与「不带」的程序输出**逐字节相同**（有用例钉住）。
2. **「退出」必须是 `BaseException`**（`DebugQuit`）：`_do_try` 里那句是
   `except BaseException`（为把 Python 异常翻译成中文），若它继承 `JishiError`，
   用户程序里一句 `捕获：` 就能把「退出」吞掉。配套在 `_do_try` 的控制信号元组
   里显式放行它，于是「退出」时 `最终` 块照常执行（像正常控制流离开，不是硬杀）。
3. **出错现场要「执行语句前」留快照**：错误是语句执行到一半时冒出来的，等它传到
   `run()`，`_eval_call` 的 `finally` 已经把帧弹干净了——那时再问「当前帧」只会
   得到模块顶层、变量还是上一帧的（实测踩到）。另外「查看 表达式」第一版报
   「只能写一个表达式」：词法器会在末尾补一个 NEWLINE，`parse_expression`
   要吃掉它才判得到 EOF。
4. **管道输入按「字节 → UTF-8」解码，并自己回显命令**：中文命令走文本层在中文
   Windows 上会按 GBK 解码成乱码（与 M23 的源码 stdin 同一坑）；管道不替用户
   回显，记录里会读不出「哪句是用户敲的」。读到 EOF 视为「退出」，不空转。

### 五、测试与文档

- 新增 `tests/test_m37_debugger.py` **62 项**：行为（断点/三种单步/栈/变量）、
  命令细节（断点增删查、求值、源码、帮助、重复上一条、英文别名）、出错现场、
  「退出」不被 `尝试/捕获` 吞掉且照跑 `最终`、**「带不带调试器输出一致」的语义钉子**、
  AST 语句行遍历（嵌套块不漏）、管道 UTF-8 与 EOF、CLI 退出码与 `--命令`。
- 新增 `docs/调试器.md`（用法 + 命令表 + 已知边界 + 实现说明）；
  README / AGENTS / PROGRESS / 路线图同步；路线图 B1 标记完成，
  度量「**调试可见性**」由「无」变「有」。
- 顺手修正 `jishi/version.py` 兜底版本（打包后无包元数据时用）滞后一版的问题。
- 全量 **1417 → 1479 项通过**。

## [0.1.18] - 2026-09-14

**M36 第一批：语言面收敛（位运算内建 + `字典.合并`）+ 路线图合并成一个**。

### 一、位运算改「内建」而不是新语法（关 0 的又一次应用）

`& | ^ << >>` 日常业务代码极罕见，而加这五个运算符要动三执行器 + 两宿主 +
词法器。所以按「关 0」做成 6 个内建：

| 内建 | 语义 |
|------|------|
| `位与(a, b)` / `位或(a, b)` / `位异或(a, b)` / `位取反(a)` | 按位运算；`位取反` 等价 `-a - 1` |
| `左移(值, 位数)` / `右移(值, 位数)` | 位移；`右移` 对负数是算术右移（向下取整） |

两个「明确报错优于静默算错」的设计点：

- **只收整数**：`真`/`假` 是**独立类型**（Python 里 `True & 1` 会给 1），
  位运算里混进逻辑值只会让人困惑 → `位或(真, 1)` 直接报错。
- **位移位数有上限**（1000000）：`1 << 10**9` 在 Python 里会真的去申请 125 MB。

### 二、`字典.合并(别的)`

返回**新字典**（同名键以参数为准），与就地修改的 `更新` 分工清楚——
`令 全 = 默认配置.合并(用户配置)` 这种写法不再需要先拷贝一份。

### 三、探针升级：缺口必须带「结论」

`tools/probe_capabilities.py` 除了报 ❌，现在还会报**这事我们怎么定的**
（`VERDICT` 表）：不做 / 有替代 / 搁置，并给出替代方案。汇总行新增
**「无悬案」判据**，有未判定项时退出码为 1。当前 **50 项 / 可用 41 /
缺口 9 项全部已判定**。

> 为什么重要：只报「缺什么」的话，❌ 会一直挂着，读者分不清「还没轮到」和
> 「已决定不做」——那正是「用户以为有、其实没有」的陷阱。

### 四、路线图合并成一份

此前有三份路线图（M7–M15 / M17–M34 / M35+），内容重叠、容易各说各话。
现在只有 **`docs/路线图.md`**：北极星 + 已完成总表 + 准入标准 + M36 起的三条
主线 + 不做清单 + 度量体系 + 遗留项逐条结论。三份旧文**按原样归档**进
`PROGRESS.md`（不丢历史），全仓引用同步更新。

### 五、条件表达式 `甲 如果 条件 否则 乙`（M36 A1）

「在一行里二选一」。语序与 Python 的 `A if C else B` 一致，选它是因为
**零新增关键字**（`如果`/`否则` 本来就是语句关键字）——加关键字是有成本与
风险的（粘连检查、既有 API 撞名）。

语义：优先级比「与/或」低（与 Python 同级）、**只求值选中的那一边**、
**右结合**（`a 如果 c1 否则 b 如果 c2 否则 d` 往右嵌套）、少写「否则」明确报错。

实现是**解析期脱糖**成「匿名函数立即调用」（本项目第六次用这个手法），
所以三个执行器 + 两个宿主**一行都没改**。

⚠️ 实现时撞到**两处**真坑（同一个根因：`如果` 早就被别处用了），都是**全量回归
抓出来的**——绝不能只跑新特性的用例就收工：

1. **推导式的过滤子句**也写「如果」（`[x 遍历 x 在 y 如果 条件]`），且紧跟在
   **可迭代对象**后面 → 可迭代对象会把过滤子句吃掉，把既有代码解析错；
2. **匹配的守卫**也写「如果」（`情形 90 如果 加分：`），且紧跟在**情形值**后面
   → 值会把守卫吃掉。

修法一致：这两处解析时传 `allow_ternary=False`，让它们待在不含条件表达式的
层级——Python 的 `comp_for` 只允许到 `or_test`，是同一个道理。想让这些位置
用条件表达式就加括号。两条都补了回归用例钉住。

> **教训**：给「已有的关键字」新增一种表达式用法时，先
> `grep '_at_kw("那个关键字")'` 把它**所有既有位置**列出来，逐个问
> 「这里会不会被新用法抢走」。

### 六、Node / Rust 宿主同步（M36）

7 个新内建/方法在 JS 与 Rust 上补齐，**五路（三执行器 + Node + Rust）逐字节对拍**：

- 位运算在 JS 里**必须走 BigInt**：JS 的位运算符是 32 位，`2 ** 40 & 1` 会被
  截断，与 Python 的任意精度对不上；
- Rust 用 i128 + `checked_shl`：左移移出范围**明确报错**（不给静默回绕），
  右移位数 ≥ 128 时按 Python 语义给 0 / -1；
- **顺手补上 Rust 侧一直缺的 `字典.更新` / `字典.弹出`**（此前调用会报
  「方法调用未实现」——是明确报错，但确实是漏的）。

测试：新增 `tests/test_m36_converge.py` **42 项**（三执行器逐字节一致、
7 条错误路径、浅合并语义、与 `更新` 的分工、内建可被遮蔽）。
全量 **1375 → 1417 项通过**（M36 共 42 项）。

## [0.1.17] - 2026-09-13

> 本次发布 = **M34（模式匹配与枚举）+ M35（发布工程与收尾）**。
> 0.1.17 此前**未发布过**，所以两轮合并成一个版本。

**D 档第一批：模式匹配与枚举**（`匹配`/`情形`、`枚举`）。两个都走
「**解析期脱糖优先**」——这是本项目第四次、第五次用这个手法（前三次是
M23 的 `超()`、M24 的集合字面量、M26 的 `用 … 为`）。解析器把新语法改写成
既有语句的组合，**三个执行器一行都不用改**，行为必然一致。

### 一、模式匹配：脱糖成 if/elif 链

```
匹配 x：                      __匹配_1__ = x
    情形 1：                  如果 __匹配_1__ == 1：
        …                         …
    情形 2, 3：               否则如果 __匹配_1__ == 2 或 … == 3：
        …                         …
    情形 其他：               否则：
        …                         …
```

待匹配值先落到临时变量，所以 `<表达式>` **只求值一次**
（`匹配 取下一个()` 不会因为分支多就跑多次，测试里锁住了这一条）。

四种分支写法（含两种守卫，这是设计时补进去的）：`情形 值：` /
`情形 值1, 值2：` / `情形 值 如果 条件：` / `情形 如果 条件：` / `情形 其他：`。

**`情形 如果 条件：` 这条是写原型时才想清楚的**：一开始我把「通配」和
「只有条件」混在一起处理，结果「通配必须放最后」把最有价值的用法
（按条件一段段筛：`情形 如果 分数>=90` → `情形 如果 分数>=60` → `情形 其他`）
给堵死了。改法是区分「裸兜底」（必须最后）与「带守卫的通配」（普通条件分支，
可放中间）。

### 二、枚举：脱糖成「类 + 成员实例」

```
枚举 颜色：                   类 颜色：（初始化 / 文本 两个方法）
    红 = 1                    颜色.红 = 新建 颜色("红", 1)
    绿 = 2                    颜色.绿 = 新建 颜色("绿", 2)
                              颜色.全部 = [颜色.红, 颜色.绿]
```

**为什么成员是实例而不是裸值**：这样 `打印(颜色.红)` 给 `颜色.红`（靠
`文本()` 方法，M27 早有），还能取 `.名字`/`.值`。若成员直接是 `1`，
打印出来就是 `1`，枚举「用名字代替魔法数字」的意义没了一半。

`==` 按身份（与 Python 的 `Enum` 一致）、`类型()` 给 `颜色 实例`、
遍历用 `颜色.全部`（**类对象本身不可遍历**——`迭代()` 是实例方法，
所以给一份清单更顺手）。成员还能不写值自动编号（从 1 起，跳过写死的值接着数）。

枚举体**只认成员、不认方法**：想在枚举上挂方法就直接用「类」——
枚举是「一组有名字的值」，不是「简化的类」。

### 三、加关键字撞掉既有 API（这次踩到的真问题）

`匹配` 一关键字化，两处立刻炸：

1. **`正则.匹配`**（M33 刚写的标准库函数，真实项目在用）——`.` 后面本来
   只收 NAME，关键字进不来。
   → 修法：**`. 后面的属性名允许是关键字**（M34 起）。属性名位置本来就没有
   歧义，这个改动让「以后加关键字」不再需要担心撞既有 API。变量名/参数名/
   字典键那些位置**仍然不许**（与 Python 一致）。
2. **`匹配组`**（真实项目「网页采集」里的一个变量）——被「关键字粘连检查」
   拦下了（`匹配组` 看着像「匹配 组」漏空格）。
   → 修法：`匹配`/`枚举` 加进 `_GLUE_CHECK_SKIP`。它们和 `用`/`是` 一样是
   **常用词前缀**（匹配组/匹配器/匹配度、枚举值/枚举类），粘连提示是净损失。

> 教训：**加关键字要同时检查「既有标识符」和「既有 API 名」**。
> 前者靠 `_GLUE_CHECK_SKIP`，后者靠放开属性名位置。

### 四、撞出并修掉宿主侧三个缺口（M34 的另一个收获）

枚举脱糖要用「类体外给类设属性」，一写就撞出宿主的老缺口：

1. **Node / Rust 宿主都没有「类变量」**（M23.3 的特性一直没同步）——
   `类 颜色：` 里写 `x = 1` 报「不能给「<类 颜色>」的属性「x」赋值」。
   补法很省事：**Python 侧的类变量本来就是靠 `SET_ATTR` 给类对象设属性实现的**
   （编译器早已发射那条指令），所以宿主只需补 `getAttr`/`setAttr` 的类分支，
   字节码层面零改动。Rust 侧的类变量表要 `Rc<RefCell<…>>`——`Instance`
   存的是 `Class` 的值拷贝，用普通 HashMap 的话实例看不到类变量。
2. **Rust：`继续` 之后再也 `中断` 不了**（真 bug，M29 引入信号分发时带来的）。
   `dispatch_signal` 在「循环胜出」时**多弹了一次循环块**，而 Python 侧
   只 `del stack[depth:]` + 改 ip，弹的动作归 `POP_BLOCK`。表现是
   「`继续` 之后 `中断` 报「中断/继续没有对应的循环」」。
   这个 bug 藏得深：单支/两支 if 里看不出来，要有「`继续` 过了还要用循环」
   才暴露——M34 的 `情形` 脱糖恰好生成了那种形状。
3. **Rust 的 `类型()` 给错名字**：实例给「对象」、类给「类」，
   而 Python 给 `A 实例` / `类 A`。`类型(x) == "A 实例"` 这种判断会永远为假
   ——**说好的能力给了错答案**，比不实现更糟。顺带把三侧的绑定方法名
   统一成「内建方法」/「方法」（此前 Python 漏 `_BoundMethod`、Node 漏
   `BoundMethod`、Rust 给「内建方法」）。

### 测试

新增 `tests/test_m34_pattern_enum.py`（**37 项**）：三执行器 + Node + Rust
**五路逐字节对拍**，覆盖单值/多值/守卫/守卫链/嵌套/求值一次/容器模式/
控制流穿透、枚举的成员取值/身份/自动编号/混合编号/遍历/与匹配配合、
以及宿主类变量的三条、六个错误路径。

能力探针 `tools/probe_capabilities.py` **37/48 → 39/48**
（顺便把「模式匹配」「枚举」两项的用例改成真验证特性——原来的枚举用例
写的是 `打印(1)`，跟枚举毫无关系，那样「能跑」毫无意义）。
全量 **1315 → 1352 项通过**。

### 四、发布工程与收尾（M35）

**语言图标**：从作者提供的 `基石_logo.jpeg` 生成三平台图标资产
（`tools/make_logo.py`）——`assets/基石.ico`（16/24/32/48/64/128/256 七档，
小尺寸略抬对比）+ `assets/基石.icns`（到 1024）。接入点：

- `jishi.exe` / `jishi-mcp.exe`：PyInstaller `--icon`（Win 用 ico，macOS 用 icns；
  Linux 的 ELF 不内嵌图标，改由桌面项 `share/基石.desktop` 指定 PNG）；
- Windows 安装程序：`SetupIconFile`（向导/任务栏/控制面板）+
  开始菜单与桌面快捷方式、卸载项的 `IconFilename`；
- 发行目录带 `share/基石.ico`，Linux 的 `install.sh` 装桌面项、`uninstall.sh` 移除。

**类体校验统一到解析期**：实测发现三个执行器对「类体里写了不该写的语句」
给出的答案**各不相同**（树遍历报错，两个字节码执行器**静默忽略**，
其中 `x += 5` 被当成 `x = 5` **静默算错**）。现在在 `parser` 一处拦下：
类体只接受「方法 / 类变量（只认一次性的 `=`）/ 空类占位（单独一行 `:`）」，
并给出正确写法示例。**三个执行器自动一致，字节码零改动**（决策见 D27）。

**顺带澄清一条误记**：M32 记的「树遍历不支持类体里写 `通过`」是误判——
`通过` 从来不是关键字（它是个普通名字，`如果 真：\n    通过` 三个执行器都报
「找不到这个名字」），真正的问题是「未定义的名字被字节码执行器静默忽略了」。
**不把 `通过` 升级成关键字**（空块已有 `:`），但**报错时点名说明**。

**修两处测试脆弱性**（都是「单跑必过、全量偶发」的假失败形态）：

- 对拍测试写的**固定临时文件名**改成带进程号（曾造成三次假失败）；
- 本地 HTTP 服务 fixture 由「先探空端口再关掉重绑」改成**原子 bind 端口 0**
  （前者是 TOCTOU 竞态，并发抢临时端口时偶发 `WinError 10048`）。

**`installer.iss` 编码加固**：新增的图标路径是**编译期**路径
（`SetupIconFile` 要在编译时找到文件），而 `.iss` 原为 UTF-8 无 BOM，
在英文 Windows 上可能按 ANSI 解码 → 中文路径乱码 → **编译失败**。
处理：`.iss` 改为 **UTF-8 带 BOM**，且构建脚本以
`/DMyIconFile=<绝对路径>` 传入（不再依赖 .iss 自身编码）。
另补 `.gitattributes` 钉住行尾（`*.sh` 强制 LF，避免 Windows 检出后打的
Linux 包带 CRLF 导致 shebang 失效）。

**文档整理**：`AGENTS.md` 从 504 行瘦身到 306 行——它自己定的规矩是
「只放高频硬约束和当前任务」，却累积成了完整变更日志；里程碑明细**原样迁入
`PROGRESS.md`**。清理 README 路线图节的重复残段与旧数字（27/43、9/38）；
更正「本机 cargo 无法运行」这条过时记录（cargo 1.98.1 可用，M34 的 Rust
改动已实际编译并对拍）；新增路线图 v2（M35 起的三条主线；该文件后来与另外
两份路线图**合并成了唯一的 `docs/路线图.md`**，见 0.1.18）。

**测试**：新增 `tests/test_m35_robustness.py`（**22 项**：类体校验三执行器
一致、`通过` 的点名提示、临时文件并发安全、图标结构与接线、`.iss` BOM）。
全量 **1352 → 1374 项通过**。

### 五、把 CI 从「一直红」修到四平台全绿（M35）

打 v0.1.17 的 tag 后，Release 三个平台**全部**在「全量测试」失败，Release 没产出；
而本地同一份代码全量全绿。排查本身就卡住了一次：Actions 的 job 日志只能在网页上
看，公开 API 下载要仓库管理员权限（403）。

**先解决「看不见」**：两个工作流加「失败摘要 → `::error` 注解」（注解可由公开 API
读取，这次就是靠它拿到失败用例清单）+「环境诊断写文件、注解一并带出」。

**三个真根因，全是「Windows 本机永远看不到」的类型**：

1. **Rust 可执行名写死 `.exe`** —— cargo 在 Windows 产出 `jishi-rs.exe`，
   Linux/macOS 产出 `jishi-rs`（无扩展名）。四个测试文件都硬编码 `.exe`，
   于是**非 Windows 上所有 Rust 对拍全线 FileNotFoundError**；更阴的是夹具
   「路径不存在→cargo build→build 成功就返回该路径」，**编译成功却把用例指向
   不存在的文件**。→ `conftest.rust_exe_path()` 按平台取名。
2. **测试往 `build/` 写文件，而 CI 全新检出没有 `build/`**（被 gitignore）
   ——本地因为跑过构建、目录一直在，永远看不到。→ conftest 里 `mkdir`。
3. **三处把开发机的平台/时区值写死**：`== "Windows…"`、时间戳期望写死 UTC+8 的
   `08:00:00`（runner 是 UTC）。→ 改成按本机实际情况推导。注意 `_agree` 本身
   已经在做跨宿主逐字节对拍，那句写死的 golden 只是多余的开发机快照。

**踩到一处 bash 坑**：注解步骤没写 `set +e`，而 pytest 被**中断**（退出码 2）时
日志里没有 `FAILED` 行，`grep` 返回 1 在 `-e` 下把这步本身弄挂了，注解反而写不出来
（白等一轮 CI）。

结果：四平台（ubuntu / windows × Python 3.10 / 3.13）**全绿**，随后重打 tag。

## [0.1.16] - 2026-09-13

**Rust 宿主的标准库补齐到与 Python 侧同宽**。此前它只有 `数学`/`文本`/`随机`
三个模块（而且函数不全：数学缺 `绝对值`、文本缺 `居中`/`格式化` 等），
这一轮把 **14 个模块、约 130 个函数**全补上。零第三方依赖——重活全是自己写的。

### 一、零依赖带来的四块硬骨头

1. **正则**（`regex.rs`）：自写**回溯引擎**。字面量 / `.` / 字符类（含 `[^…]`、
   区间、类内 `\d\w\s`）/ 量词（`* + ? {m,n}` 与非贪婪）/ 分组（`(…)`、`(?:…)`、
   `(?P<名>…)`）/ 前后查看 / 锚点 / `\b` / 反向引用。`查找全部` 的形状照
   Python 的 `findall`（0 组给整段、1 组给分组、多组给列表）。
2. **加密**（`crypto.rs`）：自写 md5 / sha1 / sha256 / sha512。这几个算法规范很死
   （常数表都在标准里），自己写反而好对拍——`abc`、空串这些公开向量与
   `hashlib` 逐字节一致，`摘要(真)` 用的也是「值→文本」后的 `真` 的字节。
3. **压缩**（`zip.rs` + `inflate.rs`）：**Node 有 zlib、Rust 什么都没有**。
   - 打包写 **`stored`（不压缩）** 条目——零依赖下没有 DEFLATE 压缩器
     （LZ77 + 两套霍夫曼树），而 `stored` 在 zip 规范里本来就合法。
     手拼本地文件头（30B）/ 中央目录项（46B）/ EOCD（22B）/ CRC-32。
   - 解压**躲不掉**：别人打的包默认都是 deflate。所以 inflate 是完整实现的
     （stored / 固定霍夫曼 / 动态霍夫曼三种块都支持）。
   - 测试不只对拍输出，还**互操作性双向验**：Python 打的 deflate 包 Rust 解得开，
     Rust 打的包 Python 的 `zipfile` 读得出来且 `testzip()` 校验通过。
4. **网络**（`http.rs`）：基石的网络是**同步**的（没有 `await`）。
   Windows 上直接绑 `winhttp.dll`（`extern "system"`，**是系统库、不是 crate**）
   —— 好处是 **https 也能用**，TLS 是 WinHTTP 内部的事，一行加密都不用写。
   其余平台走裸 `TcpStream`：`http://` 完全可用，`https://` 需要 TLS、
   手写不现实，所以**明确报错**并说明原因（绝不静默降级成明文请求）。

其余模块是薄封装 `std`（fs / path / env / process），但也有两处要绕：
`日期`/`时间` 要**本地时区**（std 没有这个概念，绑 `GetLocalTime` / `localtime_r`）
与 **`strftime` 子集**（`%U`/`%W` 照 CPython 公式）；`系统版本` 在 Windows 上
绑 `RtlGetVersion`（`GetVersionEx` 会撒谎说自己是 6.2）。

### 二、端到端验收撞出来的三个缺口（探针没覆盖到）

补完 14 个模块、113 例对拍全绿之后，按 M30 定下的标准拿
`examples/projects/` 的真实项目跑，**又撞出三块**：

1. **切片指令（`BUILD_SLICE`）Rust 侧压根没有** —— M30 只补了 Node，
   于是 `排名[::-1]` 直接报「未知指令 48」。顺手写脚本把 Python / JS / Rust
   三份指令表求差集，确认**只差这一条**。
2. **`输入()` 内建不存在** —— 三个真实项目里两个要读标准输入。
3. **`列表.排序` 静默失效** —— `Val::partial_cmp` 只处理数字与精确小数，
   字符串/列表返回 `None`，而排序把 `None` 当成「相等」→ 排了等于没排。
   表现是成绩分析的**总分排名顺序全乱**、日志统计的「访问最多的路径」
   顺序反了。这条最隐蔽：不报错、不错值，只是顺序不对。
   顺带修了 `表格.排序按` 的倒序——Python 的 `sorted(..., reverse=True)`
   对**并列项保持原顺序**，而「排完再整体反转」会把并列项也倒过来。

### 三、顺带修掉三处「跨宿主不一致」

对拍时发现 Python 侧有几处在 Windows 上做**换行转换**，导致同一份程序
在 Python 侧输出 CRLF、在 Node / Rust 宿主输出 LF：

- **CLI 输出**：`_setup_io()` 显式 `reconfigure(newline="\n")`。
- **`系统.执行`**：改成按字节收再显式解码（`text=True` 会做通用换行）。
- **`json.读文件`/`写文件`**：显式 `newline=""`（与 M31 的文件模块一致）。

还有两处「内部实现细节漏给用户」，也一并修了：

- **`正则.查找全部` 多分组时给的是 Python 的元组** → 基石没有元组类型
  （用户看到 `('a', '1')` 也没法自己构造），统一转成列表（Node / Rust 本来就是列表）。
- **`类型(表格)` 给 `_Table`** → 加了类型名钩子，三个宿主统一显示「表格」。

### 四、验收

- 新增 `tests/test_m33_rust_stdlib.py`（**58 项**）：14 个模块与 Python VM
  逐字节对拍；加密拿公开向量当裁判；zip 与 Python 的 `zipfile` **双向互操作**；
  网络用本地 `http.server`（不依赖外网）；正则覆盖贪婪/非贪婪、分组形状、
  替换模板、真实项目的爬虫模式。
- **`examples/projects/` 三个真实项目**（成绩分析 / 日志统计 / 网页采集）
  在 Rust 宿主上原样跑出与 Python **逐字节相同**的输出。
- 全量 **1257 → 1315 项通过**。

### 五、仍然缺的（明确报错，不是静默算错）

整数到 `i128` 就到边界；`是`/`不是` 对绑定方法明确报错；精确小数的 `**`
只支持整数指数；跨语言宿主不支持 `导入 … 从 python`；`压缩.打包` 写 stored
（文件大一点但合规）；非 Windows 平台 `https://` 明确报错。

## [0.1.15] - 2026-09-13

**核实并修掉「剩余边界」里的静默算错**。做法照旧：把 `docs/embed.md` 的边界
清单**逐条横向实测**，因为文档会过时。探测结果分三类，两类都动手了。

### 一、两条过时描述（删掉）

- 「JS 的 `1` 与 `1.0` 无法区分」——字节码里的小数常量本来就带
  `t: "float"` 标记，`decodeConst` 早就按它包成 `FloatBox` 了。
- 「JS 的错误渲染没有源码行」——M30 已规范化成中文。

> 教训：**边界清单也会腐烂**。上一轮 embed.md 把 JS 的成员测试标了 ✅
> 而实际没实现；这一轮的探测又反过来发现「标着 ❌ 的其实早就好了」。

### 二、真还在的「静默算错」（本轮修掉）

1. **JS 大整数**：`9223372036854775807` 显示成 `...776000`、`2 ** 64` 算错。
   JS 的 number 是双精度。**方案**：小整数仍走 number（性能不变），只在必要时
   切 `BigInt` —— ① 任一边已经是 BigInt；② number 算出来的整数结果**超出
   安全范围**。`BigInt` 是任意精度，与 Python 完全对齐。
   配套：`serialize.py` 把超出宿主安全范围的 int 常量按**十进制字符串**编码
   （JSON 的数字在 JS 里会被静默丢精度），宿主按需还原，旧产物照样能读。
2. **Rust 整数溢出**：`9223372036854775807 + 1` 给 **`-9223372036854775808`**
   （回绕成负数，最危险的一类错）。`i64` 提到 `i128` + `checked_*`，
   到边界报中文错。顺带把 `**` 的整数幂从浮点改成整数快速幂
   （`2 ** 64` 以前给 `1.8446744073709552e19`，Python 给整数）。
3. **C 虚拟机的大整数**：`2 ** 64` 给 **`0`**、`阶乘 25` 给完全不着边的数。
   根因很有意思——C 侧本来就用 `__builtin_*_overflow` 检测溢出并**回落宿主**
   计算，但回落算完之后，结果超出 i64 时在 **ctypes 边界被静默截断**。
   `JVal` 刻意保持 16 字节布局（性能路径依赖它），所以不扩宽，
   改为在 `_jint` 里拦下并报中文错（提示可以换 Python 虚拟机）。
4. **JS 的 `打印(类)` / `打印(函数)`** 给 `[object Object]`。
   给那七个类补 `toString()` —— 与 Python 侧「靠 `__repr__` 兜底」是同一个手法，
   一处定义、`display`/`pyStr`/`pyRepr` 三处自动生效。
5. **精确小数 28 位精度**：Python 的 `Decimal` 每次运算都走 context
   （`prec=28`, `ROUND_HALF_EVEN`），而 JS/Rust 只给 `div` 加了舍入——
   于是「加减乘超 28 位」时三个宿主给出**不同的数**。
   补齐 `add/sub/mul` 的舍入；并按 CPython 的 `__str__` 规则实现科学计数法
   （`exp <= 0 且 leftdigits > -6` 才用定点）。顺带修 `舍入(n)`：
   正数要**补足小数位**（`1234` → `1234.00`）、负数要换回定点写法
   （`1.2E+3` → `1200`）—— 后者是 Python 侧用 `Decimal(format(r, "f"))`
   做的，以前漏了。

### 三、明确报错（本轮补上）

- **Rust 的类型标注校验**：编译器早已把校验发射成函数的前导字节码
  （`检查实参`），Rust 侧没实现这个内建，于是带标注的函数报「找不到名字
  「检查实参」」——完全没法用。照 JS 侧实现，含「不认识的名字只作文档」。
- **Rust 的 `是`/`不是` 对类与函数**：它们在 Rust 里是值类型、没有身份。
  创建时分配递增序号（`Clone` 跟着复制），等价于 Python 的 `id()`。
  **绑定方法仍明确报错**：Python 里 `a.方法 是 a.方法` 为假，而 Rust 侧
  按「实例指针 + 方法名」判会得到真——宁可明确不支持，也不给反直觉的答案。

### 四、探测顺带发现的两件事（本轮不动，如实记下）

- **树遍历不支持类体里写 `通过`**（只认方法和类变量）。
- **JS 宿主没同步 M23 的「类变量」**：`类 A：` 里写 `x = 1` 会报
  「不能给「<类 A>」的属性「x」赋值」。类的**方法**与继承都正常。
- 另外 `类型标注` 表里的 `空` / `函数` 两条**语法上写不出来**（是关键字），
  三个侧都如此——无害的死条目。

### 测试

新增 `tests/test_m32_boundaries.py`（**40 项**）：大整数、精确小数精度与显示、
类与函数显示、Rust 类型标注、Rust 同一性、C 虚拟机的整数边界，关键语义走
树遍历 / Python VM / C VM / Node / Rust 多路对拍。
全量 **1217 → 1257 项通过**。

## [0.1.14] - 2026-09-13

**JS（Node）宿主的标准库补齐到与 Python 侧同宽**。M30 补到「5 个常用模块」，
这一轮把剩下 7 个（路径 / 文件 / 日期 / 时间 / 加密 / 压缩 / 网络）一次补完。
补完的直接结果：`examples/projects/` 里的**全部**项目都能在 JS 宿主上跑，
包括上轮还明确记为「跑不了」的「网页采集」。

### 一、三块各有各的坑

1. **压缩**：Node 没有内置 zip。`zlib.deflateRawSync` / `inflateRawSync` 只管
   deflate 流，本地文件头（30 字节）、中央目录项（46 字节）、EOCD（22 字节）、
   CRC-32 都得按 PKWARE APPNOTE 自己拼。所以这一块的测试**不只对拍输出**，
   还拿 Python 的 `zipfile` 当裁判：Node 打的 zip 要被 `zipfile` 读得出来
   （`zf.testzip()` 校验 CRC），Python 打的 zip 要被 Node 解得开。
   `crc32` 自己写，不依赖 `zlib.crc32` 的版本可用性。
2. **网络**：Node 没有同步 HTTP —— `fetch` 是异步的，而基石没有 `await`，
   标准库不能变成异步函数。方案是 `spawnSync(process.execPath, ['-e', 脚本])`
   起一个**自身的子进程**跑异步请求，结果经 stdout 回传。零第三方依赖，
   也不要求系统装了 `curl`。代价是每次请求多一次进程启动（本机约 40ms），
   换来与 Python 侧 `urllib` 一样的**同步**调用形态。
3. **日期时间**：JS 没有 `strftime`，`toLocaleString` 又依赖系统 locale，
   于是自己实现了一个子集（`%Y %y %m %d %H %I %M %S %j %w %u %a %A %b %B %p
   %U %W %c %x %X %%` 与 `%-d`）。`%U`/`%W` 的周号照 CPython 的公式
   （新年第一个周日/周一之前的日期算第 0 周）。
   `时间.睡眠` 用 `Atomics.wait` 同步阻塞 —— Node 主线程可用，这是基石没有
   `await` 时唯一的办法。

### 二、顺手改掉 Python 侧两个「跨平台不一致」

- **文件读写改成「换行保真」**（显式 `newline=""`）：Python 的文本模式在
  Windows 上会把 `
` 悄悄写成 `
`，于是「同一份程序在 Windows 与 Linux
  上产出的文件字节不同」，也与 Node 的 `fs`（不转换）对不上。
  基石的定位是「同一份程序到哪都一样」，所以标准库统一按字节保真来。
- **值→文本统一**：`文件.写文本` / `写行`、`加密.摘要` 之前走 Python 的 `str()`，
  于是 `写文本(p, 真)` 写出的是 `True`（而 `文本(真)` 给 `真`）。改成
  `jishi_repr`，与 M30 修的 `文本.拼接` / `格式化` 一致。

### 三、路径模块的两个细节

- **`后缀` 不能直接用 `path.extname`**：pathlib 对 `a.` 与 `.bashrc` 都算无后缀，
  而 `path.extname('a.')` 给 `'.'`。得自己按 pathlib 的规则算
  （最后一个点既不在开头、也不在末尾）。
- **Windows 上要规范化分隔符**：Python 的 `Path.__str__` 总吐 `\`
  （`str(Path("a/b"))` 给 `a\b`），而 JS 的 `path` 会保留输入里的分隔符。
  所以 `连接` / `父目录` / `创建目录` 的返回值都要转一次。

### 四、验收

- 新增 `tests/test_m31_node_stdlib.py`（**43 项**）：7 个模块与 Python VM
  逐字节对拍；网络用本地 `http.server`（不依赖外网）；压缩另测跨宿主互操作。
- **真实项目「网页采集」**（网络 + 正则 + 表格）在 JS 宿主上跑出与 Python
  逐字节相同的输出，连产出的 `链接.csv` 也一样。至此 `examples/projects/`
  里的**全部**项目都能在 JS 宿主上原样跑。
- 全量 **1174 → 1217 项通过**。

## [0.1.13] - 2026-09-13

**JS（Node）宿主补齐到 Python 侧**：把缺的七块一次补完，全部有对拍测试。

先做了一次**横向探测**（`build/_probe_node.py`，跑完即删）：39 个用例逐条比对
Node 与 Python VM，实测出 37 处不一致——不是靠印象猜范围。

### 一、原来早就坏了，只是没人跑

1. **成员测试 `在` / `不在`、同一性 `是` / `不是`**：`vm.js` 的 `CMPOP` 只映射了
   0–5（`==`…`>=`），M23.2 的语义比较码 **6–9 根本没接**，于是
   `2 在 [1, 2]` 会报「不支持的比较「undefined」」。
   **这与 Rust 侧 M28 暴露的是同一个问题**（那边也是 `compare` 只处理 0–5）。
2. **容器相等**：`[1, 2] == [1, 2]` 给**假**——JS 的 `===` 是引用比较，
   而 Python 的 `==` 对容器是按值递归。
3. **空容器为假**：JS 的 `Boolean([])` 是 `true`、`Boolean({})` 也是 `true`，
   而 Python 的 `bool([])`/`bool({})` 是 `False`——不特判，
   `如果 列表：` 这种判断会**整个反过来**（静默算错级别）。
4. **类型标注的参数校验**：JS 侧完全没有。

> 上一版 `docs/embed.md` 的支持程度表把成员测试/同一性给 JS 标了 ✅。
> **那是错的**——不跑就不知道。这轮按实测改正，并在表里补了
> 「JS 宿主剩下的边界」一节。

### 二、新补齐的四块

- **集合类型**：`JishiSet` 自己维护「规范化键 → 原值」的 Map，而不是用内建
  `Set`——因为 `Set` 的元素相等是 SameValueZero，与 Python 的 `hash/eq`
  不一致（Python 里 `{1, 1.0, 真}` 只留一个元素，`new Set([1, 1.0, true])`
  会留三个）。所以 `集合([1, 1.0, 真, 2])` 在两边都是 `{1, 2}`。
  字面量/推导式在解析期已脱糖成 `集合([…])`，所以实现内建即自动可用。
  十一个方法、有序去重、空集合写 `集合()`、进出集合的不可哈希检查都与
  Python 侧一致。
- **列表高阶方法**：`映射` / `过滤` / `排序按` / `归约`。JS 侧比 Rust 侧省一层
  ——`VmFunction` 自带 `.vm`，不必把 VM 传进方法表。
- **类型标注**：编译器把校验发射成函数自己的前导字节码（`检查实参`），
  宿主实现那个内建就自动获得这项能力。
- **自定义可迭代对象**：`迭代(自身)` 可用于 `遍历` 与 `长度`/`最大`/`最小`。

### 三、顺带两处

- **`node/index.js` 的错误输出规范化**：此前基石错误直接抛给 Node，
  用户看到的是一段 JS 堆栈（`vm.js:407 / throw exc; / JishiError: …`），
  既看不出是哪种错误也看不到行列。现在打印
  `错误（类型错误）：消息` + `← 第 N 行第 M 列`，与 Rust 宿主同风格。
- **`最大`/`最小` 支持多参数**（`最大(1, 5)`）以及集合/字典/自定义可迭代，
  并把「混了不能比较的值」报成中文错。

### 四、补着补着又发现的三块（跑真实项目撞出来的）

前七块补完，我把 `examples/projects/` 里的真实项目拿来在 Node 上跑——
**又撞出三块缺口**。这说明「横向探测」也有盲区（探针里没写切片），
所以「拿真实项目当验收标准」这一条要固定下来。

- **切片（指令 `BUILD_SLICE`=48）**：JS 侧**根本没有这条指令**，
  于是 `对[::-1]` 直接报「未知指令 48」。实现时对齐了 CPython 的
  `PySlice_AdjustIndices`（负值先加长度、再按方向裁边界）——
  `a[-2:]`、`a[3:0:-1]`、`a[10:20]` 这些边界行为逐条对拍过。
  顺带把 JS 与 Rust 的指令表做了**系统性比对**（用脚本读两边的表求差集），
  确认「只差这一条」。
- **二元运算的类型分派**：`[1] + [2]` 在 JS 里是**字符串拼接**（`"12"`）、
  `1 + 空` 会把空当 0、`-7 % 3` 的符号与 Python 相反（JS 是截断语义、
  Python 跟除数一致）。这些都是「不报错但结果错」。现在按 Python 的类型
  规则显式分派：列表 `+`/`*` 造新列表、文本只和文本相加、数字/布尔才做算术。
- **标准库**：补了 `表格`（CSV 读写与查询）、`文本`（补齐到 18 个函数，
  含 `格式化("{:.1f}")` 这类格式说明符）、`json`、`正则`、`系统`。
  两个坑：① `json.转文本` **不能直接用 `JSON.stringify`**——Python 的
  `json.dumps` 默认分隔符是 `', '` 和 `': '`（带空格），JS 的不带；
  ② `输入()` 此前**永远返回空串**（不读 stdin），现在真读、且同步写输出
  （`process.stdout.write` 对管道是异步的，与同步读 stdin 混用会让进程退出时
  **丢掉开头几个字符**——实测吞掉了「请输」两个字）。

**验收标准变成了**：`examples/projects/` 里的真实项目能在 Node 宿主上
**原样跑出与 Python 逐字节相同的结果**（含退出码）。现在成绩分析、日志统计
两个项目都过了，对拍测试里各锁一条。

### 已知边界（JS 表示层的限制，不是漏做）

- **`1` 与 `1.0` 无法区分**：JS 的 number 只有一个类型，JSON 字节码也把
  `1.0` 解析成 `1`，所以类型标注 `函数 f(x: 小数)` 遇到字面量 `f(1.0)`
  会判成「整数」报错（Python 侧放行）。运算得到的小数会装箱成 `FloatBox`，
  判定正常。
- 标准库覆盖 5 个常用模块 + 数学/随机；仍缺日期、时间、路径、文件、网络、
  压缩、加密（网络要异步、日期时间涉及时区，需单独一轮对齐）。
- 大整数要 BigInt 扩展；`导入 … 从 python / 本地包` 不支持（无 Python 运行时）。

### 测试

新增 `tests/test_m30_node_host.py`（**62 项**，全部与 Python VM 逐字节对拍，
关键语义另加树遍历 / C VM 四路对拍，另含两个真实示例项目的端到端对比）。
**跑这个文件就能看出本轮补了多少东西**：集合 6 组、高阶方法 9 组、
成员/同一性 4 组、容器相等与真假值 2 组、切片 5 组、运算分派 5 组、
类型标注 4 组、自定义可迭代 4 组、标准库 7 组、输入 2 组、端到端 2 组。
全量 **1174 项通过**（上版 1153）。

## [0.1.12] - 2026-09-13


**Rust 宿主补齐到 Python 侧**：用户方法调用 / 列表高阶方法 / 集合 /
文件与上下文 / 精确小数。三块一起做的结果是——**这个宿主上不再有
「静默算错」**，剩下的缺口都是明确报错。

### 一、用户方法调用（`对象.方法()` / `初始化` / 继承覆写）

此前走到 `BoundMethod::User` 就报「需经 VM 帧」。根因有两层：

1. 调方法要把实例塞进第一个形参（`自身`）再压帧，而 `call_bound` 是个
   **自由函数**、拿不到 VM。现在 `CALL` 遇 `BoundMethod::User` 直接走
   与「函数」相同的压帧路径——方法不过是首参被预先绑定的函数。
2. 更隐蔽的是 `Instance` 是**值类型**：`自身.字段 = 值` 走 `SET_ATTR`，
   拿到的是 `Val` 的一份拷贝，于是方法里的赋值**全部静默失效**。
   实例也必须共享（`JInst = Rc<RefCell<Instance>>`）。顺带的好处是
   实例有了对象身份，`a 是 b` 可以正常工作了。

新增 `VM::call_value`：内建/method 能同步回调用户函数——这是第二块的基础。

### 二、列表高阶方法（数据流水线）

`映射` / `过滤` / `排序按` / `归约`。实现要点：回调前先**快照**容器内容，
否则「拿着 `RefCell` 的借用去回调、回调里又改同一个容器」会撞上运行时
借用冲突。`排序按` 的键两两不可比时报中文错，不悄悄按「相等」给出错顺序。

### 三、集合 / 文件与上下文 / 精确小数

- **集合**：`{1, 2, 3}` 字面量、`集合(…)`、推导式，11 个方法（添加/移除/
  丢弃/包含/并集/交集/差集/对称差/清空/转列表/复制）。有序去重，
  与 Python 侧一致。列表/字典/精确小数不能进集合（对齐「元素必须可哈希」）。
- **文件对象**：`打开(路径, 模式)` 返回**真句柄**（读缓冲 + 惰性按行遍历），
  模式认「读/写/追加/读写」与 r/w/a/r+/w+/a+，固定 UTF-8。
- **上下文管理器** `用 … 为`：解析期脱糖成「临时变量 + 绑定 + 尝试/最终」，
  所以 Rust 侧只需要 `进入上下文` / `退出上下文` 两个内建。
- **精确小数**：**零依赖手写 BigInt**（`rust/src/decimal.rs`，base 10^9
  小端 + 逐肢二分长除法），28 位有效数字、ROUND_HALF_EVEN，
  `精确("1") / 精确("3")` 与 Python 的 `Decimal` **逐字节一致**。
  `**` 只支持整数指数——非整数次幂是近似运算（Python 走 `context.power`），
  各执行器必然漂移，所以报中文错而不是给近似值。
- **顺带**：自定义对象打印走带 VM 的 `format_value`（容器里也不再掉回
  `<甲 实例>`）；`长度`/`最大`/`最小` 支持定义了「迭代」的对象。

### 四、写第三块时抓到 Rust 侧两个真 bug（都不是本次引入的）

1. **异常根本没被分发**：`execute` 收到 `Err(e)` 直接 `return Err(e)`，
   绕过了 `dispatch_exception`——于是 `尝试/捕获/最终` 对**内置错误**
   （除零、越界、类型不符）完全失效（`Flow::Error` 这个变体根本没人构造）。
2. **`返回` 不跑 finally**：缺 Python 侧的 `_return_via_finally`，所以
   「`用` 块里 `返回`」会让文件不关。

现在两条都按 Python 的块栈语义补齐：`step_return` 把返回值挂进
`frame.pending_return`、在 `END_FINALLY` 收尾；循环信号（`中断`/`继续`）
要穿过 finally 时借 `Val::LoopSignal` 走值栈，`CHECK_SIGNAL` 判它、
末尾的 `THROW` 还原成信号；「最近的循环 vs 最近的 try」谁先接，
由登记序号 `block_seq` 比较决定（对齐 Python 的 `_Handler.seq`）。

### 测试与度量

`tests/test_m27_rust_host.py` 从 36 条扩到 **101 条**（**不再有 xfail**），
新增覆盖：方法调用 10 项、数据流水线 9 项、集合 6 项、文件与上下文 9 项、
精确小数 26 项、异常/清理 3 项。
全量测试 **1112 项通过**（含 Rust 宿主对拍 101 项）。

### 仍然缺的（都是明确报错，不是静默算错）

- 类型标注的参数校验（标注只作文档）
- `是` / `不是` 对**类与函数**（值类型没有稳定身份，硬做会给出和 Python 相反的答案）
- 标准库只覆盖数学 / 文本 / 随机三个模块（子集）
- 精确小数不做上下文精度舍入的乘/加（Python 对超 28 位有效数字的结果会舍入，
  Rust/JS 给数学精确值；日常金额规模两者一致）

## [0.1.11] - 2026-09-13


**Rust 宿主：修掉可变容器**，这是上一版留的「静默算错」——不报错但结果是错的，
比直接报错危险得多。

### 根因

`Val::List(Vec<Val>)` 是**值类型**、`Clone` 是深拷贝，而 `get_attr` 拿到的
`obj` 又是拷贝，于是 `a.追加(3)` 改的是副本、原变量纹丝不动。
推导式（内部走 `列表.追加`）因此**返回空列表**。

### 改法

容器换成 `Rc<RefCell<Vec<…>>>`（`type JList` / `type JDict`），
`Val::clone` 只复制指针、同一份数据被共享，方法里用 `borrow_mut()` 改。
涉及 53 处使用点，由编译器逐个把关。

**一个必须做对的配套改动**（容易漏）：Rust 版把「列表自身」当迭代器
（`iter_next` 会 `remove(0)`）。以前靠值拷贝侥幸正确（`Val::List(l)`
天然是独立副本），共享之后如果直接把 `l` 递出去，`遍历 x 在 a` 会
**把 `a` 逐元素吃掉**。所以 `make_iterator` 改成 `list_new(l.borrow().clone())`；
`unpack_values` 同理取快照（解包是只读操作，不该搬空原列表）。

### 顺带补齐两处 Rust 侧完全没有的比较运算

补容器语义时写了「按首字母分组」这个真实惯用法，结果在
`如果 k 在 分组` 处就断了——`compare` 只处理 0–5，
`在`(6)/`不在`(7)/`是`(8)/`不是`(9) 根本没实现。

- **成员测试 `在` / `不在`**：列表按值、字典按**键**、文本找子串，
  与 Python 侧 `runtime.contains` 同一套语义（range 解出来的列表同理）。
- **同一性 `是` / `不是`**：空/真/假 是单例；数字与文本按**值**比
  （Python 侧也是这么定的，否则会有「有时真有时假」的随机感）；
  列表/字典按对象身份（`Rc::as_ptr` 正好就是身份）。
- **类 / 实例 / 函数明确报错**：它们在 Rust 里是值类型、**没有身份可谈**。
  硬按「同内容即同一」算，`甲 是 甲` 会给出和 Python 相反的答案——
  那又是一次静默算错。所以宁可直接报「Rust 宿主暂不支持」。

### 测试与度量

- Rust 宿主对拍从 21 项增到 **36 项**（+4 xfail 记录剩余缺口）：
  新增容器可变语义 12 项、成员测试与同一性 2 项。
- 新增 `test_rust_iteration_does_not_consume` —— 专门锁「遍历不消耗原列表」，
  这是本轮最容易悄悄写错的一处。
- 全量测试通过（见 README 的「测试现状」）。

## [0.1.10] - 2026-09-13

**Rust 宿主首次真正跑起来**，据此修掉两个阻塞级既有 bug。

### 起因：`cargo` 命令「执行成功但什么都不做」

`~/.cargo/bin/` 下 13 个文件（`cargo.exe` / `rustc.exe` / `rustdoc.exe` …）
都是 **0 字节**。rustup 在 Windows 上把这些做成指向 `rustup.exe` 的硬链接，
链接坏掉后退化成空文件，执行它没有输出且 `rc=0`——很容易被误判成
「环境限制 / 命令被沙箱吞了」（此前两轮就是这么记的）。

真身在 `~/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin/`（58 MB）。
重建硬链接后 `cargo 1.98.1` 正常工作。**上一版 CHANGELOG 里那句
「本机 cargo 无法运行」是误判，此处更正。**

### 修掉两个阻塞级 bug（都是既有的，不是新引入）

1. **`Flow::Return` 语义混淆 → 函数调用整个不可用**
   它同时表示「本帧结束，请弹帧」和「刚压了新帧，别弹」。于是每次函数调用后
   新帧立刻被 `execute` 弹掉，外层接着执行那个**还没拿到实参**的 `CALL`，
   `fn_base = len - (1+nargs+nkw)` 下溢成 `usize::MAX` → panic。
   修前连 `函数 加一(x)：返回 x + 1` + `打印(加一(1))` 都跑不了。
   修法：新增独立的 `Flow::Call`（「帧已压好，继续循环、不要弹」）。
2. **`MAKE_FUNCTION` 默认值对齐按全部形参算**
   有 `*参数` 时默认值不在全部形参的尾部，而在**普通形参**的尾部，
   于是默认值被算到「余」头上，`带默认(1)` 报「缺少参数：乙」。
   （Python 侧 `vm.py` 在 M25 修过同一个坑，Rust 侧漏了。）

### 顺带修正一处夸大表述

Rust 宿主自 M13.3 起写着「最小可运行」，但 `tests/` 里**只有 Node 的对拍**，
Rust 从来没有运行验证——第 1 个 bug 说明它连函数调用都跑不了。
现在补上 `tests/test_m27_rust_host.py`（21 条对拍 + 5 条 xfail），
并把 `docs/embed.md` 的支持程度表按**实测**重写。

### 实测后的 Rust 宿主能力（`docs/embed.md` 为唯一事实源）

- **可用**：显示规则（空/真/假）、变长参数 `*`/`**`、星号解包、函数/递归/
  闭包/匿名函数/默认参数、基础控制流、赋值解包、文本插值
- **报错**：用户方法调用（需经 VM 帧）、集合字面量、列表高阶方法、类型标注
- **⚠️ 静默算错**：**可变容器**（`列表.追加` 改的是副本、推导式返回空）。
  根因是 `Val` 是值类型、`get_attr` 拷一份再改；修它要把 `List`/`Dict`
  换成 `Rc<RefCell<…>>`，是贯穿全文件的重构。这条比「报错」严重——用户会
  拿到错结果却不知道，已在 `embed.md` 与测试的 xfail 里明确标出。

### 构建方式（记进 docs/embed.md）

```bash
cargo build --manifest-path rust/Cargo.toml --release
# windows-gnu target 用 gcc 当链接器：把提供 gcc 的目录加进 PATH
# （或用 JISHI_MINGW 环境变量指定，测试认这个变量、不写死本机路径）
```

### 测试与度量

- 新增 26 项（21 对拍 + 5 xfail 记录缺口），全量 **1032 项通过 + 5 xfail**。
- 探针可用率不变（37/48）——探针测的是语言面向用户的能力，宿主覆盖度
  由 `docs/embed.md` 的支持程度表负责。

## [0.1.9] - 2026-09-13

两件事：**值→文本的统一**（M27），以及**跨语言宿主的同步补课**。

### 值→文本的统一（M27）

同一个值以前有两种写法：`打印(空)` 给 `None`、布尔给 `True`/`False`，
而 REPL 回显与 `类型()` 给中文「空/真/假」——用户得猜哪个算数。
根因是这件事有**两份**几乎一样的实现（`runtime.jishi_repr` 与
`repl._repr_jishi`），日子久了就漂移（`文本()` 是第三个漂移源：它直接
`str()`，插值又脱糖成 `文本()`，所以插值也跟着给 `None`）。

现在收敛成一处 `runtime.jishi_repr(v, *, top)`：

- `打印(值)`、`文本(值)`、插值 `` `{值}` `` 用 `top=True`：文本**不加引号**
  （用户要的就是那串字）；空/真/假 一律中文。
- 容器内元素与 REPL 回显用 `top=False`：文本**加引号**，否则分不清
  `['甲']` 里的是文本还是变量——与 Python 的 `print`/`repr` 分工一致。
- 字典用半角 `: `，与源码字面量 `{"甲": 1}` 写法一致，打印结果能直接抄回代码。
- 标准库/桥接返回的原生 Python 值也走这套规则，所以 `路径.存在(...)` 与
  `日期.早于(...)` 显示「真/假」而不是 `True`/`False`。
- 连带消除规格里的一条「已知边界」（原生 bool 显示不一致）。

**影响面**：文档、教程、示例、测试快照里这三类字面值**全部**改到中文
（52 处测试期望 + 教程注释）。`repl._repr_jishi` 删掉，改为委托，
杜绝再次漂移。

### 跨语言宿主同步

- **Node（JS）**：补齐 M26 的四个内建——`精确`（自己实现十进制：系数用
  BigInt，加减乘精确、除法算到 28 位有效数字、舍入 half-even，因此
  `精确("1")/精确("3")` 与 Python **逐字节一致**）、`打开`（真文件句柄）、
  `进入上下文`/`退出上下文`，外加文件 6 方法、精确小数 3 方法、
  集合迭代与自定义对象打印。显示规则同步（空/真/假、半角冒号）。
  **全部由对拍测试验证**（同一份字节码，Node 与 Python VM 输出逐字节一致）。
- **Rust**：只做了**能人工确证正确**的部分——`py_repr` 中文化、
  `打印`/`文本` 改走 `display`（原来用 `py_repr`，导致 `打印("甲")` 会输出
  `'甲'` 带引号，与另两处宿主不一致）、`<实例>` 改成 `<类名 实例>`。
  **未做** `精确`/`打开`/上下文内建，理由如实记录在 `docs/embed.md`：
  ① `rust/` 零第三方依赖，精确小数需要任意精度整数（得手写 BigInt，数百行）；
  ② 新增 `Val` 枚举变体要补全部 `match` 分支；
  ③ **本机 cargo 无法运行**（`touch` 源文件后重新 build，产物时间戳不变），
  改动无法编译验证——写几百行编译不过的代码比明确标注未做更糟。

### 顺带修掉

- JS 侧 `JishiDecimal._alignTo` 的指数对齐方向写反（`e < target` 应为
  `e > target`），会让 `精确("19.99") - 精确("20")` 算成 `20.19`
  而不是 `-0.01`——只有「指数不同」的运算才暴露，已加专门的回归用例。

### 测试与度量

- 新增 19 项（显示统一 13 项 + Node 宿主对拍 6 项），全量 **1011 项**通过。
- 探针可用率 **36/47 → 37/48**（新增「空真假中文显示」，带预期输出校验）。

## [0.1.8] - 2026-09-13

语言功能扩展 C 档（M26）：让真实项目里高频的三件事变顺手——用完要关的资源、
打印自定义对象、金额计算。三项里只有 `用 … 为` 碰语法，而它在**解析期脱糖**，
所以三个执行器一行都没改。

### 新增特性

- **上下文管理器 `用 … 为`**：`用 打开("数据.txt") 为 f：` —— 离开代码块时
  自动调用对象的 `退出()`（没有就找 `关闭()`）。正常结束、`返回`、`中断`、
  `抛出` 都会走到。自定义类定义 `进入()` 可以决定绑定什么值。
  退出**不吞异常**（有意不做：吞了会让「出问题快速找到」变难）。
- **文件对象 `打开(路径, 模式 = "读")`**：返回真句柄，可 `遍历 行 在 f`；
  方法有 读 / 读行 / 读所有行 / 写 / 写行 / 关闭 / 刷新 / 位置 / 定位；
  模式 读 / 写 / 追加 / 读写（也认 `r` `w` `a` `r+` `w+` `a+`）；
  一律 UTF-8（Windows 默认 GBK 会把中文写乱）。
- **自定义对象打印 `文本()`**：类里定义 `函数 文本(自身)：` 后，
  `打印(对象)` 与 `文本(对象)` 都用它的返回值；放进列表/字典里也生效；
  没定义仍是 `<类名 实例>`。
- **精确小数 `精确(值)`**：十进制小数，四则运算、比较、`总和`/`最大`/`最小`/
  `排序` 都支持；方法 舍入（四舍五入，可给负位数舍到十位/百位）/ 绝对值 /
  转文本。`0.1 + 0.2 == 0.3` 是假，`精确("0.1") + 精确("0.2") == 精确("0.3")`
  是真。从普通小数转时走 `str`，`精确(0.1)` 就是 0.1，不带二进制尾巴。

### 顺带修掉

- `总和` / `最大` / `最小` 对精确小数可用（`sum` 从整数 0 起算会 TypeError）；
- 文件不存在的报错去掉 Python 英文原文（原来混着 `No such file or directory`）；
- 清掉树遍历侧 `_parse_try` 的重复定义（后一份覆盖前一份，前一份是死代码）。

### 明确边界

- 上下文管理器退出不吞异常；不支持 `用 … 为 a, b`（解包目标）。
- 普通小数 `0.1` **仍是二进制浮点**，与 Python 一致——不偷偷改语义。
- 跨语言宿主（Node / Rust）尚未同步 M26 的四个新内建（`精确` / `打开` /
  `进入上下文` / `退出上下文`），用到它们的程序在那边跑不了；
  `用 … 为` 脱糖出的正是后两个，所以在宿主上同样不可用。
- 已知不一致（**未改**）：`打印(空)` 显示 `None`、布尔显示 `True`/`False`，
  而 REPL 回显与 `类型()` 用中文「空/真/假」。统一会改掉所有既有输出
  （golden 快照、示例、教程对照结果），需单独确认后再做。

### 测试与度量

- 新增 51 项（每特性三执行器逐字节对拍，文件读写用真实临时目录），
  全量 **992 项**通过。
- 探针可用率 **31/44 → 36/47**。

## [0.1.7] - 2026-09-12

语言功能扩展 B 档收尾（M25）：补上 M24 明确留下的「需改 C」那一项。

### 新增特性

- **变长参数 `*参数` / `**选项`**：
  ```python
  函数 求和(*数)：           # 收下所有位置实参，是个列表
      返回 总和(数)
  函数 配置(**选项)：         # 收下未匹配的关键字，是个字典
      返回 选项
  函数 混合(甲, *余, **选)：   # 三者可组合
  ```
  没收到东西时是空列表 / 空字典（不报错）；规则与 Python 对齐：
  顺序固定为 `普通参数 → *参数 → **选项`，收集参数不能有默认值或类型标注。
  **没有「星号展开」实参**（`f(*列表)`）——这是明确的边界，已写入规格。
- **星号解包 `令 甲, *余 = …`**：
  ```python
  令 甲, *余 = [1, 2, 3, 4]        # 甲=1，余=[2, 3, 4]
  令 首, *中, 尾 = [1, 2, 3, 4, 5]  # 首=1，中=[2, 3, 4]，尾=5
  ```
  星号可在中间或末尾、只能有一个；长度不够时报中文错并提示「*余」这条路。
  右值支持列表、文本、集合、字典（取键）。

### 修复（都是本轮实测/对拍抓出来的既有缺陷）

- **`//=` 与 `**=` 复合赋值一直不可用**：词法器按 `OPERATORS` 列表顺序匹配，
  而 `**` 排在了 `**=` 前面，`**=` 被切成 `**` + `=`（`//=` 同理）。
  修复为按**长度降序**匹配。副作用是格式化器不再把 `甲 **= 3` 重写成
  `甲 ** = 3`（那等于改了语义）。
- **类方法的类型标注此前没有生效**：M24 加类型标注时，`_do_classdef` 里构造
  方法的 `UserFunction` 漏传 `annotations`，于是写在方法上的标注等于没写。
  已补上（`param_kind` 同批处理）。
- **`数据排列` 系列**：`长度` / `最大` / `最小` 对集合与自定义可迭代对象
  的支持补齐（写迭代协议时发现的缺口）。
- **解包右值收紧了类型**：集合、字典此前不在允许之列，现在都能解包。
- **两条参数绑定路径统一**：树遍历的普通调用与 Python 侧回调
  （`列表.映射(基石函数)`）此前各写一份绑定逻辑，已抽成
  `UserFunction._bind_args` 共用，避免「直接调对、通过高阶函数调错」。

### 实现要点（这批为什么必须改 C）

参数绑定在 C VM 的 `bind_args()` 里实现，所以：

1. **扁平字节码 `code_params` 每个形参从 3 个 int32 变成 4 个**，
   第 4 个是「形参种类」（普通 / `*参数` / `**选项`），
   由 `cvm/build.py` 从 `jishi/opcodes.py` 的 `ParamKind` 生成；
2. **`JsHost` 末尾追加 `pack_kwargs` 回调**（把未匹配的关键字收成字典）——
   `*参数` 的打包则直接复用现成的 `build_list`，不必新增回调；
   追加在末尾保证前面所有字段偏移不变；
3. **`bind_args` 的所有权语义从「转移」改为「借用」**：
   同一个实参可能「既被打包进列表、又留在数组里等待统一释放」，
   借用语义下失败路径只需统一 release 一次，不会双释或漏释；
4. **`UNPACK` 指令的 `b` 操作数**（此前闲置）用来传星号位置，
   因此不需要新指令；
5. 字节码序列化格式版本 **1 → 2**（新增 `param_kind`，且 `UNPACK.b` 含义变了），
   旧产物会被明确拒绝。

### 测试

- 新增 59 项（每特性三执行器逐字节对拍 + Node 宿主对拍；C 侧改动最需要这层保护），
  全量 **941 项**通过。
- 探针可用率 **27/43 → 31/44**。

## [0.1.6] - 2026-09-12

语言功能扩展 B 档（M24）：补齐集合、迭代表达能力与数据流水线。

### 新增特性

- **集合类型（M24.1）**：`{1, 2, 2, 3}` 字面量、`集合(列表)` 构造、
  集合推导式 `{x % 2 遍历 x 在 范围(5)}`。
  - **有序**：按首次出现顺序排列，打印/遍历结果稳定可复现
    （刻意不用无序哈希集合——那会让同一程序两次运行输出不同，
    也会让三执行器对拍变成随机失败）。
  - 方法：`添加` / `移除` / `丢弃` / `包含` / `并集` / `交集` / `差集` /
    `对称差` / `清空` / `转列表` / `复制`；`长度` / `在` / `==`（与顺序无关）通用。
  - `{}` 仍是空字典（与 Python 一致），空集合写 `集合()`。
- **迭代协议（M24.1）**：自定义对象定义 `迭代(自身)` 后即可用于 `遍历`、
  推导式、`总和` / `最大` / `最小` / `长度`（后两者此前对自定义对象也有缺口，
  一并补齐）。返回自身会被拦下并报「会无限循环」。
- **切片赋值（M24.2）**：`分数[1:3] = [95, 96]`、`分数[1:] = []`、
  带步长的 `分数[::2] = [...]`。读切片与写切片现在共用同一条求值路径
  （原先只有读路径支持，写会报「还不支持的表达式：Slice」）。
  长度不匹配时报中文错（此前会漏出英文 `extended slice`）。
- **可选类型标注（M24.3）**：`函数 折扣(价格: 小数) -> 小数：`。
  参数标注会**运行时校验**，三执行器一致，报错指出函数名/参数名/期望/实际；
  认不出的标注名（自定义类型）只作文档、不误报；返回标注目前只作文档。
  定位仍是「运行时校验 + 文档」，**不是静态类型系统**。
- **匿名函数（M24.4）**：`函数(x)：x * 2`，支持默认值、类型标注、闭包捕获、
  柯里化 `函数(x)：函数(y)：x + y`。
- **数据流水线（M24.4）**：列表新增 `映射` / `过滤` / `排序按` / `归约`，
  具名函数与匿名函数都能传，可链式组合。

### 实现要点（为什么这样切）

- 集合字面量与集合推导式在**解析期脱糖**成 `集合([…])` 调用，
  复用既有指令（与 M23 的 `超()` 同一手法）——三执行器零改动。
- 类型标注校验编译成**函数自己的前导字节码**：
  参数绑定分别在 Python 与 C 里完成，只有把校验放进函数体，三个执行器
  才走同一条路径，既不必改 C，也不会出现「某个执行器不检查」。
- 匿名函数把 `_c_funcdef` 里「构造函数对象」的部分抽成共享逻辑，
  与具名函数共用闭包单元、默认值求值、MAKE_FUNCTION 全过程。

### 已知边界

- **变长参数 `*参数` / `**选项` 与星号解包 `令 a, *余 = …` 本轮未做**。
  这两项的参数绑定在 C VM 的 `bind_args` 里实现，且要同步扁平化字节码
  格式（`code_meta`/`code_params`）与 Python 侧编码器，属于「改 C + 重编
  DLL + 两侧锁步」的改动，风险与收益不匹配，留到用户确认语法后单独做。

### 测试

- 新增 64 项（每特性三执行器逐字节对拍），全量 **882 项**通过。
- 探针可用率 **16/39 → 27/43**（新增项均带预期输出校验）。

## [0.1.5] - 2026-09-12

语言功能扩展 A 档（M23）：六个高频缺口一次补上，重心回到语言自身。

### 新增特性

- **成员测试 `在` / `不在`（M23.2）**：`名字 在 名单`、`9 不在 范围(5)`。
  列表按值、字典按**键**、文本找子串，与 `列表.包含()` 是同一套语义。
- **同一性 `是` / `不是`（M23.2）**：`x 是 空`、`结果 不是 假`。
  空/真/假按单例，数字与文本按值（避免 Python `is` 那种「有时真有时假」），
  列表/字典/实例/函数按对象身份。
- **`超()` 调父类方法（M23.4）**：
  ```python
  类 狗 继承 动物：
      函数 初始化(自身, 名字, 品种)：
          超().初始化(名字)
  ```
  解析期脱糖成「(自身, 定义类名)」，因此**多级继承不会绕回自己**
  （与 Python 零参数 `super()` 一致）。
- **`断言(条件, 消息)`（M23.1）**：条件不成立抛「断言错误」（E2008），
  可被 `捕获 断言错误` 精确接住、也能被 `捕获 运行期错误` 兜住。
- **`是实例(值, 类)`（M23.1）**：判断实例是否属于某类（含子类）。
- **类变量（M23.3）**：类体里可写 `名字 = 值`，所有实例共享；
  查找顺序 `实例字段 → 类变量（含基类）→ 方法`，与 Python 一致。
- 内建/标准库抛出的错误现在会补上**调用点行列**，报错能指回用户写的那一行。

### 修复

- **`非` 的优先级（重要）**：原先 `非` 比比较运算更紧，导致
  `非 年龄 >= 18` 被解析成 `(非 年龄) >= 18` —— **恒为假、静默给错答案**
  （项目教程 `docs/tutorial/03` 就踩了这个坑，「未成年人」分支永不触发）。
  现修正为「比比较松、比与或紧」，与 Python 的 `not` 一致：
  `非 年龄 >= 18` = `非 (年龄 >= 18)`。
- **C VM 函数同一性**：同一函数每次跨 C/Python 边界都新建包装对象，
  导致 `f 是 g` 在 C VM 下与另两个执行器结果相反。现按底层 `JsFunc*`
  指针缓存包装，三执行器语义一致。
- 容错解析新增英文运算符提示：`not in` → 「不在」、`is not` → 「不是」、
  `is` → 「是」、`and`/`or` → 「与」/「或」。

### 测试

- 新增 57 项（每个特性都做三执行器逐字节对拍），全量 818 项通过。
- 新增 `tools/probe_capabilities.py` 能力缺口探测器：实测 39 项候选特性，
  可用率从 9/38 升到 **16/39**（探测「能跑通」之外还校验预期输出，防自嗨）。

## [0.1.4] - 2026-09-12

包生态（M22）：从「能装本地包」走到「装包不用带参数」。

### 新增特性

- **默认包索引（M22.1）**：`jishi 安装 包名` 现在开箱即用，不必再传
  `--from-index`。索引是静态 JSON，托管在本项目公开仓库里（无需服务端），
  可用 `--index` 或环境变量 `JISHI_INDEX` 指到自建索引；
  `--no-index` 可关闭联网行为。
- **发布工具（M22.2）**：
  - `jishi 发布 <包目录>`：校验 `包.json` → 打包 zip → 算 sha256 →
    生成索引条目；`--index` 可直接合并进索引文件
  - `jishi 索引 <目录>`：扫描多个包批量重建索引（`--base-url` 指定下载前缀）
  - 打包只收 `包.json` 与 `.jsh`，`__pycache__` 等噪音不会进包
  - `包.json` 字段名会校验：拼错（如把「依赖」写成「依赖项」）直接报错
- **安装校验和**：索引带 `校验和` 时逐字节校验，对不上即拒装。
- **钻石依赖冲突检测（M22.3）**：`jishi 检查` 遍历依赖图，报告
  「谁以什么约束要求了它」的完整链条；运行期约束失败时提示跑 `jishi 检查`。

### 修复

- **Windows 下中文源码经 stdin 传入变乱码**（发版前实测发现）：打包版不继承
  `PYTHONUTF8`，`sys.stdin` 按 GBK 解码，`打印` 变成 `鎵撳嵃`。修复：
  `cli.py` 改按「字节 → UTF-8」读 stdin，`mcp_server.py` 的 JSON-RPC 读取与
  子进程传参同理；入口脚本三个流全部显式 UTF-8 + `errors=replace`。
- **报错渲染遇非法码位会崩成英文堆栈**：源码里混进孤立代理字符时，
  渲染中文报错会 `UnicodeEncodeError`，最终用户看到 PyInstaller 的英文报错。
  修复：错误渲染统一做 `_safe_text` 兜底。
- **打包版 MCP 无法执行脚本**：打包后 `sys.executable` 是 `jishi-mcp.exe`
  自己，`-m jishi.cli` 直接失败。修复：frozen 模式下改用发行目录同级的
  `jishi` 可执行文件。
- 索引/下载地址含中文时未做 URL 编码、已编码地址被二次编码为 `%25`。
- `jishi 检查` 遇到循环依赖会无限递归。

### 测试

- 新增 55 项（M22 生态 36 项 + 编码健壮性 19 项，含真实 MCP 中文往返与
  本地 HTTP 服务闭环），全量 761 项通过。

## [0.1.3] - 2026-09-12

开发者工具（M21）：语言服务器 + 代码格式化，把「写起来爽不爽」补上。

### 新增特性

- **语言服务器 `jishi lsp`（M21.2）**：stdio 上的 LSP，编辑器接入后即可
  - **实时诊断**：打开/改动即重新分析，把中文报错（错误码 + 行列 + 修改提示）
    推给编辑器；另附保守的「名字没定义过」警告
  - **补全**：关键字 / 内建函数 / 标准库模块 / 文档内符号；
    `随机.` 之后给该模块的函数与签名
  - **悬停**：关键字、内建函数、标准库模块与函数、文档内符号的中文说明
  - **跳转定义**：跳到文档内定义的函数 / 类 / 变量 / 导入
  - **文档符号**：大纲视图
- **代码格式化 `jishi 格式化`（M21.1）**：
  - 统一缩进（默认 4 空格，`--indent` 可调）与词间间距
  - **保留注释与字符串原样**：`#` 行注释、`#- -#` 块注释、三引号字符串、
    中文引号与转义都不动
  - `--write` 原地写回、`--check` 供 CI 校验（未格式化退出码 1）、
    `--stdin` 管道输入
  - **幂等**且**语义不变**：格式化结果再格式化不变；全语料 token 流逐一相等

### 测试

- 新增 111 项测试（格式化 90 项 + LSP 21 项），全量 706 项通过。

## [0.1.2] - 2026-09-12

性能里程碑：用 benchmark + cProfile 定位并消除了 C VM 的主要瓶颈。

### 性能

- **批量 JVal 转换（M20）**：C VM 的迭代/函数调用返回值原先逐个跨 ctypes
  边界构造 `JVal`（≈1µs/值）。现在整批为整数时用一次 `struct.pack` + `memmove`
  写入 C 侧数组（转换快 ~9x；混合类型 / 大端 / 溢出自动回退逐个）。
- 相对树遍历解释器的提速变化（`tools/benchmark.py`）：

  | 场景 | 优化前 | 优化后 |
  |------|--------|--------|
  | 整数循环累加 | 1.9x | **8.4x** |
  | 算术混合运算 | 5.2x | **18.1x** |
  | 函数调用密集 | 4.5x | **10.7x** |
  | 列表构建与遍历 | 1.6x | **2.6x** |
  | 当循环倒计数 | 44.4x | 45.9x |

- 独立验证：`遍历 范围(20000)` 从 16.5ms 降到 3.8ms（4.4x）。

## [0.1.1] - 2026-09-12

「健全语言自身、方便实现意图、出问题能快速定位」——排错体验与语言表达力双升级，
并改进发行方式（提供 Windows 安装程序、三平台自动构建）。

### 新增特性

- **切片语法（M18）**：`列表[1:3]`、`[::-1]`、`[::2]`、`[:2]`、`[2:]`、`[:]`，
  字符串同样支持；三执行器（树遍历 / Python VM / C VM）语义一致
- **命令行参数（M18）**：`系统.参数()` 读取 `jishi 脚本.jsh 参数…`；
  `--` 之后的内容原样作为脚本参数
- **`最小`/`最大` 多参数（M18）**：支持 `最小(a, b)`、`最小(a, b, c)`，
  单列表调用保持兼容
- **导入别名与遮蔽治理（M18）**：普通导入支持 `为 别名`（`导入 文本 为 t`）；
  模块名与内建同名时导入会给出一次中文提示

### 排错体验（M17）

- **调用栈回溯**：运行期错误附带「调用链（从外到内）」，标明每层函数在第几行被调用；
  协议 JSON 同步携带 `trace` 字段
- **递归超限中文化**：修复 `RecursionError` 绕过中文翻译的问题，
  现在输出「递归层数太深了」+ 调用链 + 修改建议
- **协议错误补全**：沙箱超时给出「可能是有死循环」提示；错误不再缺 `line`/`col`/`hint`
- **REPL 报错显示源码行**：交互式报错与文件模式一致，显示源码行与 `^` 指示

### 发行改进（M19）

- **Windows 安装程序**：Inno Setup 生成 `setup.exe`（用户级安装、中文向导、
  开始菜单、可选 PATH、标准卸载），不再只提供压缩包
- **三平台自动构建**：GitHub Actions 打 tag 触发 windows / linux / macOS 构建，
  自动创建 Release 并附 SHA256SUMS

### 修复

- 递归超限错误未走中文翻译链路（落到英文兜底）

## [0.1.0] - 2026-09-05

首个对外发行版。语言已可用、AI 工具链齐备、三执行器语义一致、
可独立安装运行（免 Python 环境）。

### 新增特性

- **语言核心**：变量 / 分支 / 循环 / 函数 / 类继承 / 异常，中文报错精确到行列
- **表达力（M7）**：多赋值解包、默认参数、列表/字典推导式、文本插值
- **三执行器**：树遍历解释器（默认）/ Python 字节码 VM / C 字节码 VM，
  107 项对拍逐字节一致
- **Python 生态桥接（M3）**：`导入 x 从 python` 直用整个 Python 生态，
  关键字参数、异常自动翻译中文
- **14 个标准库**：随机 / 数学 / 时间 / 文件 / 表格 / 文本 / json / 日期 /
  正则 / 网络 / 路径 / 加密 / 压缩 / 系统（中文薄壳，零第三方依赖）
- **AI 原生工具链（M8/M12）**：语言卡（`--ai-card`）、机器可读语言规格
  （`--lang-spec`）、结构化报错（`--json-errors`，含 fix 自愈）、
  few-shot 45 对、50 题评测集
- **安全执行（M9）**：沙箱（超时/输出上限/模块白名单）、统一协议
  （`--json-result`）、MCP Server（`jishi-mcp`）、C VM 嵌入 ABI
- **跨语言字节码分发（M13）**：`--dump-bytecode` 产出 JSON 字节码，
  Node/Rust 宿主直嵌引擎
- **包管理（M14）**：`jishi 安装/卸载/列表`，包依赖解析 + 版本约束 + 循环检测
- **发行版（M16）**：独立可执行文件（PyInstaller），安装/卸载脚本，
  `jishi 医生` 环境自检，`jishi 新项目` 脚手架

### 已知限制

- 无切片语法（`[::-1]` 用 `反转()` 代替）、`最小`/`最大` 只收单列表、
  无命令行参数 argv（详见 `docs/语言规格.md`「已知边界」）
- Rust 宿主为「最小可运行」（可变容器语义待 Rc<RefCell> 重构）
- 发行包体积较大（内含 Python 运行时），PyInstaller 产物可能被杀软误报

### 升级指引

- 首个版本，无升级路径。
- 0.x 阶段允许 breaking change，每个版本在发布说明列清变更与迁移指引。

---

版本格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
