# 进度日志 —— 跨工具的编程主轴

> 这是基石项目的**进度日志主轴**，任何 AI 编程工具都能读、都能写。
> 约束规则见 `AGENTS.md`；详细论证见 README.md 与 docs/。
>
> 本文件记录**里程碑级**进度（不是逐条琐碎操作）。每完成一个里程碑，
> 在下方时间线追加一行，并更新「当前进行中」「下一步」两节。

---

## 里程碑时间线

| 里程碑 | 内容 | 完成时间 |
|--------|------|---------|
| M0 | 词法/语法/AST/中文报错/树遍历解释器/CLI+REPL | ✅ |
| M1 | 对象方法系统/链式比较/REPL 增强/教程前 3 章 | ✅ |
| M2 | 标准库 6 模块/教程全 7 章/尾随逗号 | ✅ |
| M3 | Python 生态桥接 + 关键字参数 | ✅ |
| M4 | 字节码编译器 + Python 参考 VM + C 字节码 VM | ✅ |
| M5 | 异常处理 + 面向对象（类/继承/新建） | ✅ |
| M6 | 工程化：CI/编辑器插件/文档站/打包 | ✅ |
| M7 | 表达力打磨（解包/默认参数/推导式/插值）+ 容错解析 | ✅ |
| M8 | AI 原生工具链（语言卡/JSON 报错/few-shot/评测集） | ✅ |
| M9 | 安全沙箱 + 统一协议 + MCP Server + C VM 嵌入 | ✅ |
| M10 | 生态扩展（4 标准库 + 本地包 + 浏览器试玩） | ✅ |
| M11 | 性能与分发（C VM 热路径下沉 + wheel 内带 DLL） | ✅ |
| M12 | 评测驱动闭环（50 题评测集 + --llm 框架） | ✅（待跑真实模型） |
| M13 | 跨语言字节码分发（JSON 字节码 + Node/Rust 绑定） | ✅（Rust 最小可运行） |
| M14 | 包管理与生态（安装命令 + 依赖解析 + 4 新标准库） | ✅ |
| M15 | 真实项目 + 语言收敛 1.0 | ✅ |
| M16 | 发行版与安装体验（PyInstaller + 安装脚本 + 首次体验） | ✅（Windows x64） |
| **M17** | **排错体验补全（递归翻译 bug + 调用栈 + 协议补全 + REPL 源码行）** | ✅ 2026-09-06 |
| **M18** | **表达便利（切片 / argv / 最小二参 / 遮蔽治理）** | ✅ 2026-09-06 |
| **M19** | **发行改进（Windows 安装程序 + 三平台自动构建）** | ✅ 2026-09-12 |
| **M20** | **性能优化：批量 JVal 转换（C VM 提速 2–4x）** | ✅ 2026-09-12 |
| **M21** | **开发者工具（LSP 语言服务器 + 代码格式化）** | ✅ 2026-09-12 |
| **M22** | **包生态（默认静态索引 + 发布工具 + 钻石依赖冲突检测）** | ✅ 2026-09-12 |
| **v0.1.4** | **首个三平台 Release（含 Windows 安装程序）** | ✅ 2026-09-12 |
| **M23** | **语言功能扩展 A 档（在·不在 / 是·不是 / 超() / 断言 / 是实例 / 类变量）** | ✅ 2026-09-12 |
| **M24** | **语言功能扩展 B 档（集合 / 迭代协议 / 切片赋值 / 类型标注 / 匿名函数 / 数据流水线）** | ✅ 2026-09-12 |
| **M25** | **变长参数 `*`/`**` + 星号解包（改 C：code_params 加 kind、JsHost 加回调、借用语义）** | ✅ 2026-09-12 |
| **M26** | **语言功能扩展 C 档（上下文管理器 `用 … 为` + 文件对象 / 自定义对象打印 / 精确小数）** | ✅ 2026-09-13 |
| **M27** | **值→文本统一（空/真/假 中文；打印·文本·插值·回显收敛到一处）+ Node 宿主同步到 M26** | ✅ 2026-09-13 |
| **M27 收尾** | **Rust 宿主首次真正跑起来：修 cargo shim（13 个 0 字节）+ 两个阻塞级 bug（帧弹出 / 默认值对齐）** | ✅ 2026-09-13 |
| **M28** | **Rust 宿主修可变容器（`Rc<RefCell>`，原为「静默算错」）+ 补齐 `在`/`不在`/`是`/`不是`** | ✅ 2026-09-13 |
| **M29** | **Rust 宿主补齐到 Python 侧：用户方法调用 / 列表高阶方法 / 集合 / 文件与上下文 / 精确小数（零依赖手写 BigInt）；顺带修掉「异常绕过分发」与「返回不跑 finally」两个真 bug** | ✅ 2026-09-13 |
| **M30** | **JS（Node）宿主补齐：集合 / 高阶方法 / 成员测试 / 同一性 / 容器相等 / 类型标注 / 自定义可迭代，外加切片 / 运算类型分派 / 5 个标准库模块 / 交互输入；修「空容器为假」「错误直甩 JS 堆栈」；真实示例项目可在宿主上原样跑** | ✅ 2026-09-13 |
| **M31** | **JS 宿主标准库补齐到与 Python 同宽（路径 / 文件 / 日期 / 时间 / 加密 / 压缩[手写 zip 容器]/ 网络[子进程同步封装]）；Python 侧文件读写改「换行保真」；`examples/projects/` 全部项目可在 JS 宿主原样跑** | ✅ 2026-09-13 |
| **M32** | **边界清单逐条实测核实：删两条过时描述；修五处静默算错（JS 大整数/BigInt、Rust i64 回绕→i128+checked、C VM 大整数被 ctypes 静默截断、JS 类与函数显示 [object Object]、精确小数 28 位精度与显示）；补两处明确报错（Rust 类型标注校验、Rust 是/不是 对类与函数）** | ✅ 2026-09-13 |
| **M33** | **Rust 宿主标准库补齐到与 Python 同宽（14 个模块 / 约 130 个函数，零依赖自写：回溯正则引擎、md5/sha1/sha256/sha512、zip 容器 + inflate、WinHTTP 网络）；端到端又撞出并修掉切片指令缺失 / `输入()` 缺失 / `列表.排序` 静默失效；顺带统一三处跨宿主换行不一致与两处内部类型名漏出** | ✅ 2026-09-13 |
| **M34** | **D 档第一批：模式匹配 `匹配`/`情形` + 枚举 `枚举`（都走解析期脱糖，三执行器 + 两宿主零改动）；加关键字撞掉 `正则.匹配` 与 `匹配组` → 放开属性名位置 + 补 skip 表；顺带修宿主三缺口（Node/Rust 类变量、Rust `继续` 后无法 `中断` 的真 bug、Rust `类型()` 给错名字）** | ✅ 2026-09-13 |
| **M35** | **发布前验证 + 语言图标进安装包与发行物（多尺寸 ico / icns / Linux 桌面项 / Inno Setup 图标）+ 三平台 Release + 遗留收尾（类体解析期统一校验、测试临时文件并发安全、文档一致性）+ 路线图 v2** | ✅ 2026-09-13 |

---

## 当前进行中

- （无。M35 已交付，见下方「下一步」与 `docs/路线图_v2.md` 的 M36 起规划。）

---

## 下一步（M36 起，完整规划见 `docs/路线图_v2.md`）

**主线转向**：语法面已基本收敛（探针 39/48 可用，剩余 9 项**已逐条判定**，
见 `docs/路线图_v2.md` §1.2），主线从「扩语法」转为
**收敛（把探针做成「无悬案」→ 语法冻结）+ 可信工具链 + 真实项目验证**。

1. **主线 A——语言面最后收敛**：A1 三元表达式（脱糖，唯一真需新语法的项）；
   A2 位运算/位移改内建；A3 `字典.合并()`；A4 `冻结(列表)`；
   A5 探针 48/48 全部有结论 → 宣布语法冻结。
2. **主线 B——可信工具链**：B1 调试器最小可用（断点/单步/看变量与栈，
   先树遍历）；B2 LSP 增量同步；B3 REPL 增强；B4 格式化器打磨。
3. **主线 C——真实项目说话**：`examples/projects/` 扩到 5 类场景，
   维护「别扭点清单」反哺 A/B；建立「基石版行数 ÷ Python 版行数」基线。
4. **宿主侧剩余边界**（都是明确报错，不阻塞；唯一事实源是 `docs/embed.md`）：
   C VM 整数 64 位；Rust 整数到 i128、绑定方法的 `是`/`不是`、
   精确小数 `**` 只支持整数指数、`压缩.打包` 写 stored、非 Windows 无 `https://`；
   JS `网络` 是子进程封装。

---

## 已解决的遗留项

- M16 发行版：Windows x64 已发布到 Gitee（v0.1.0，含 zip + SHA256SUMS）
- Linux/macOS 发行包：已由 GitHub Actions 三平台自动构建（M19 起）
- JS 宿主标准库：已补齐到与 Python 同宽（M31）
- 树遍历类体不支持空类占位：已在**解析期**统一校验时一并解决（M35）
- Node / Rust 缺「类变量」：已补（M34）
- 测试临时文件固定名导致并行「假失败」：已改成带进程号（M35）
- 「本机 cargo 无法运行」：**旧记录已更正**（cargo 1.98.1 可用，M27 修好
  shim 后即正常；M34 的 Rust 改动已实际编译并对拍）
- 仍未做（非阻塞）：M12 跑真实模型（需 API key）；
  短字符串内联（无实测瓶颈，暂不做）

---

## 关键决策索引（详见 docs/设计决策.md，**D1–D27**）

- D1 技术栈透明（Python 前端 + C 内核，不 fork CPython）
- D5 标准库薄封装不重造轮子
- D13 JSON 字节码（常量池只含标量）
- D14 Node/Rust 走纯解释器而非 ffi
- D16 包管理分两步走
- D21 版本号单一来源（0.1.0）
- D22 PyInstaller onedir + 包外入口脚本
- D23 用户目录安装 + `jishi 医生` + GBK 编码坑
- D24 大整数与整数溢出的取舍（M32）
- D25 「剩余边界」清单要按实测维护（M30/M32）
- D26 D 档四类特性的 ROI 结论（M34）
- D27 类体校验放解析期一处拦下（M35）

---

## 里程碑明细（历史归档，2026-09-13 从 AGENTS.md 迁入）

> AGENTS.md 只放「高频硬约束 + 当前任务」，这些逐轮的交付细节与教训
> 迁到这里保存；**每条都是当时踩过的坑，改相关代码前值得回看**。

- 📌 **M30 已交付（JS 宿主补齐）**：先横向探测（39 用例比对 Node 与 Python VM）
  实测出 37 处不一致，再按实测范围补——不靠印象猜。补的七块：
  1. **成员测试 `在`/`不在`、同一性 `是`/`不是`**：`vm.js` 的 `CMPOP` 只映射
     0–5，M23.2 的语义比较码 **6–9 根本没接**（`2 在 [1,2]` 报
     「不支持的比较「undefined」」）。**与 Rust 侧 M28 是同一个问题**。
  2. **容器相等按值**（`[1,2] == [1,2]` 原先给假，因为 JS 的 `===` 是引用比较）
  3. **空容器为假**（`Boolean([])` 是 true，而 Python `bool([])` 是 False
     → `如果 列表：` 判断会整个反过来，属**静默算错**）
  4. **集合类型**：`JishiSet` 自己维护「规范化键 → 原值」的 Map，不用内建 `Set`
     ——`Set` 的元素相等是 SameValueZero，与 Python 的 `hash/eq` 不一致
     （Python 里 `{1, 1.0, 真}` 只留一个）。字面量/推导式解析期已脱糖，实现内建即自动可用。
  5. **列表高阶方法** 映射/过滤/排序按/归约（`VmFunction` 自带 `.vm`，比 Rust 省一层）
  6. **类型标注校验**：编译器已把校验发射成前导字节码，实现 `检查实参` 内建即自动生效
  7. **自定义可迭代对象**：`迭代(自身)` 可用于遍历与 长度/最大/最小
  顺带把 `node/index.js` 的错误输出规范化（此前直接甩 JS 堆栈，看不出错误类型
  与行列），改成与 Rust 宿主同风格的 `错误（类型）：消息 ← 第 N 行第 M 列`。
- 📌 **M30 还顺手补了三块（跑真实项目撞出来的，探针没覆盖到）**：
  1. **切片**（`BUILD_SLICE`=48）：JS 侧**整条指令都没有**，`对[::-1]` 直接报
     「未知指令 48」。实现了 CPython 的 `PySlice_AdjustIndices` 语义。
     并把 JS/Rust/Python 三份指令表用脚本求差集，确认只差这一条。
  2. **二元运算的类型分派**：`[1] + [2]` 在 JS 里是**字符串拼接**、`1 + 空` 当 0、
     `-7 % 3` 符号与 Python 相反——全是「不报错但结果错」。
  3. **标准库**：表格（CSV）/ 文本（补到 18 个函数，含 `{:.1f}` 格式说明符）/
     json / 正则 / 系统；`输入()` 从「永远返回空串」改成真读 stdin。
     ⚠️ `json.转文本` 不能用 `JSON.stringify`（Python 的默认分隔符带空格）。
- 📌 **宿主补齐的验收标准（M30 起固定）**：`examples/projects/` 里的真实项目
  能在宿主上**原样跑出与 Python 逐字节相同的结果（含退出码）**。
  光跑「横向探测脚本」会有盲区——切片的缺口就是探测漏掉、跑真实项目才撞出来的。
- 📌 **教训（两轮各撞一次）**：**加语义比较运算符时，宿主侧的「码值映射表」
  是最容易漏的地方**——Rust 的 `compare(op)` 与 JS 的 `CMPOP` 都只写到 5，
  `在/不在/是/不是`(6–9) 静默落空，而 `docs/embed.md` 还给它标了 ✅。
  改这类表之后**必须跑一次横向探测**（`build/_probe_node.py` 那类脚本），
  别信文档。
- 📌 **M29 已交付（Rust 宿主补齐）**：三块一起做，从此**没有「静默算错」**，
  剩下的都是明确报错。
  1. **用户方法调用**：`对象.方法()` / `初始化` / 继承覆写 / 递归方法。
     前提是把 `Instance` 也换成 `Rc<RefCell<…>>`——否则 `自身.字段 = 值`
     改的是副本、**静默失效**（与 M28 的容器是同一类坑）。
     新增 `VM::call_value`：内建/method 能同步回调用户函数，这是第 2 项的基础。
  2. **列表高阶方法**：`映射` / `过滤` / `排序按` / `归约`（含具名/匿名函数、
     闭包、链式）。实现要点：先**快照**容器再回调，否则拿 `RefCell` 借用去回调
     会撞运行时借用冲突。
  3. **集合 / 文件与上下文 / 精确小数**：
     - 集合（有序去重，11 个方法 + `集合()` 内建 + 字面量 + 推导式）
     - `打开()` 真句柄（按行惰性遍历）+ `进入上下文`/`退出上下文`
     - **精确小数：零依赖手写 BigInt**（`rust/src/decimal.rs`），
       28 位有效数字、ROUND_HALF_EVEN，`精确("1")/精确("3")` 与 Python
       逐字节一致；`**` 只支持整数指数（非整数是近似运算，宁可报错）
     - 顺带：自定义对象打印走带 VM 的 `format_value`，容器里也不再掉回
       `<甲 实例>`；`长度`/`最大`/`最小` 支持定义了「迭代」的对象
- 📌 **写第 3 项时抓到 Rust 侧两个真 bug（都不是本次引入的）**：
  ① `execute` 收到 `Err(e)` 直接 `return Err(e)`，**绕过了异常分发**——
     于是 `尝试/捕获/最终` 对内置错误（除零/越界/类型不符）完全失效
     （`Flow::Error` 变体根本没人构造）；
  ② `返回` 不跑 finally（缺 Python 侧的 `_return_via_finally`），
     所以「`用` 块里 `返回`」会让文件不关。
  现在都按 Python 的块栈语义补齐（含 `中断` 穿过 `用` 块时先清理、
  信号登记序号 `block_seq` 决定「最近循环 vs 最近 try」谁先接）。
- 📌 **M28 已交付（Rust 可变容器）**：
  - 容器换成 `Rc<RefCell<Vec<…>>>`：`a.追加(3)` 现在真的改到 `a`，
    `令 b = a` 是别名、推导式不再返回空列表
  - 配套：`make_iterator` / `unpack_values` 必须**显式快照**（否则遍历/解包
    会把原列表搬空——以前靠值拷贝侥幸正确，共享后就成了 bug）
  - 顺带补齐 Rust 侧的 `在`/`不在`（列表按值·字典按键·文本找子串）
    与 `是`/`不是`（数字文本按值、列表字典按身份；类/实例**明确报错**，
    因为值类型没有身份，硬凑会静默算错）
- 📌 **M27 已交付（值→文本统一）**：
  - `打印(值)` / `文本(值)` / 插值 / 容器元素 / REPL 回显**收敛到一处**
    `runtime.jishi_repr(v, *, top)`。以前有两份实现（另一份在 `repl.py`），
    漂移出「`打印(空)` 给 `None`、REPL 回显给 `空`」的不一致
  - `top=True`（打印/文本/插值）：文本**不加引号**；空/真/假 一律中文
  - `top=False`（容器内/REPL）：文本**加引号**（与 Python print/repr 分工一致）
  - 字典用半角 `: `，与源码 `{"甲": 1}` 一致，打印结果能直接抄回代码
  - 桥接返回的原生 Python bool 也统一，`路径.存在()` 不再给 `True`
  - **排查这类问题时先搜「同一件事有几份实现」**——漂移几乎都从这里来
- 📌 **M26 已交付（语言功能 C 档）**：
  - 上下文管理器 `用 打开("f") 为 f：` —— **解析期脱糖**成
    「临时变量 + 绑定 + 尝试/最终」，退出时自动调 `退出()` 或 `关闭()`；
    三个执行器一行都没改（`进入上下文`/`退出上下文` 只是普通内建）
  - 文件对象：`打开(路径, 模式)` 返回真句柄（可 `遍历 行 在 f`、有
    读/读行/读所有行/写/写行/关闭/刷新/位置/定位），UTF-8 固定编码
  - 自定义对象打印：类里定义 `文本(自身)`，`打印(对象)` 与 `文本(对象)`
    都用它（容器里也生效；没定义仍是 `<类名 实例>`）
  - 精确小数：`精确(值)` → 十进制（四则/比较/总和/最大/最小/排序 都支持），
    带 `舍入`（ROUND_HALF_UP）/`绝对值`/`转文本`
- 📌 **脱糖方案的坑（M26 踩到）**：`退出上下文` 必须作用在**原来的对象**上，
  不能传「绑定后的名字」——`进入()` 可以返回别的对象（Python 的 `__enter__`
  也如此），拿返回值去退出会找错对象且**静默失效**。现在多留一个临时变量
  `__用_N__` 保存原对象。
- 📌 **明确边界**：① 上下文管理器退出**不吞异常**（有意不做）；
  ② 精确小数只在显式 `精确(...)` 时启用，普通 `0.1` 仍是二进制浮点。
- 📌 **跨语言宿主的同步状态**（唯一事实源是 `docs/embed.md` 的支持程度表）：
  - **Node（JS）**：与 Python 侧对齐到 M27（含 `精确`/`打开`/上下文内建/
    自定义打印/显示规则），**全部由 `tests/test_m27_display.py` 对拍验证**。
    JS 侧的精确小数是自己实现的十进制（BigInt 系数 + 28 位有效数字 +
    half-even），所以 `精确("1")/精确("3")` 与 Python 逐字节一致。
  - **JS（Node）**：已补齐到 M30（集合 / 高阶方法 / 成员测试 / 同一性 /
    容器相等 / 类型标注 / 自定义可迭代），**全部由 `tests/test_m30_node_host.py`
    对拍验证**（41 项）。仍然是子集：标准库只有数学/文本/随机，且
    `1` 与 `1.0` 无法区分（JS 表示层限制）。
  - **Rust**：cargo 已修好（13 个 0 字节 shim 重建硬链接），**首次真正跑起来**，
    由此修掉两个阻塞级既有 bug：
    ① `Flow::Return` 语义混淆（弹帧 vs 压帧）——修前**连函数调用都 panic**，
    即 M13.3 起「最小可运行」是夸大的（只有编译验证、没有运行验证）；
    ② `MAKE_FUNCTION` 默认值对齐按全部形参算，有 `*参数` 时错位。
    随后 M28 修可变容器、M29 补齐方法调用/高阶方法/集合/文件与上下文/
    精确小数。现在**除了下表列的边界，全部与 Python 逐字节对拍一致**
    （`tests/test_m27_rust_host.py`，**101 条，无 xfail**）。
    仍然缺的（**都是明确报错，不是静默算错**）：标准库只覆盖三个模块
    （且函数不全，M33 的主项）、整数到 i128 就到边界、
    绑定方法的 `是`/`不是`、精确小数的 `**` 只支持整数指数。
    （类型标注与「是/不是 对类与函数」已在 **M32** 补上。）
- 📌 **M31 已交付（JS 标准库补齐到与 Python 同宽）**：M30 补到 5 个模块，
  这轮把剩下 7 个补完——**路径 / 文件 / 日期 / 时间 / 加密 / 压缩 / 网络**。
  三块各有各的坑（都是「照着 Python 写反而会错」的类型）：
  1. **压缩**：Node **没有内置 zip**。`zlib` 只管 deflate 流，本地文件头 /
     中央目录 / EOCD / CRC-32 得按 PKWARE 规范手拼。所以测试**不只对拍输出**，
     还拿 Python 的 `zipfile` 当裁判（`zf.testzip()` 校验 CRC），双向都测。
  2. **网络**：Node 没有同步 HTTP（`fetch` 是异步的，而基石没有 `await`）→
     `spawnSync(process.execPath, ['-e', 脚本])` 起**自身的子进程**跑异步请求，
     结果经 stdout 回传。零依赖，也不要求装了 `curl`；代价是每次多约 40ms。
  3. **日期时间**：JS 没有 `strftime`，自己实现子集（`%U`/`%W` 照 CPython 公式）；
     `时间.睡眠` 用 `Atomics.wait` 同步阻塞。
  4. **路径**：`后缀` 不能直接用 `path.extname`（pathlib 对 `a.`/`.bashrc` 算无后缀，
     JS 给 `'.'`）；Windows 上 `Path.__str__` 总吐 `\`，而 JS 的 path 保留原分隔符
     → 要显式规范化。
- 📌 **M31 顺手改了 Python 侧两个跨平台不一致**：
  ① **文件读写改成「换行保真」**（显式 `newline=""`）——Python 文本模式在 Windows
  上把 `
` 悄悄写成 `
`，导致「同一份程序在两个平台产出不同字节」；
  ② `文件.写文本`/`写行`、`加密.摘要` 的值→文本改走 `jishi_repr`
  （原来 `写文本(p, 真)` 写的是 `True`）。
- 📌 **验收标准再进一步**：`examples/projects/` 里的**全部**项目
  （成绩分析 / 日志统计 / 网页采集）都能在 JS 宿主上原样跑，逐字节相同。
- 📌 **M32 已交付（边界清单核实 + 静默算错清零）**：做法是把 `docs/embed.md`
  的「剩余边界」**逐条横向实测**。三类结果：
  1. **两条文档过时**（删掉）：JS 的「`1`/`1.0` 无法区分」（字节码里小数常量
     本来就带 `t:"float"`，`decodeConst` 早按它包 `FloatBox`）；JS「错误渲染
     没源码行」（M30 已修）。**边界清单也会腐烂——要按实测更新。**
  2. **五处真静默算错（修掉）**：
     - **JS 大整数**：双精度 → 必要时切 `BigInt`（任一边是 BigInt，或 number
       结果超出安全范围）。配套：`serialize.py` 把超安全范围的 int 常量按
       十进制**字符串**编码（JSON 数字在 JS 里会静默丢精度）。
     - **Rust 整数回绕**：`i64_MAX + 1` 给负数 → `i128` + `checked_*`，
       到边界报错；`**` 的整数幂改成整数快速幂。
     - **C 虚拟机大整数**：`2 ** 64` 给 `0`。根因是 **ctypes 边界静默截断**
       （C 侧本来就有 `__builtin_*_overflow` 回落，是回落算完之后丢的）→
       在 `_jint` 里拦下报错（`JVal` 的 16 字节布局不能动）。
     - **JS 显示类/函数**给 `[object Object]` → 七个类补 `toString()`
       （一处定义、三处自动生效，对应 Python 靠 `__repr__` 兜底）。
     - **精确小数 28 位精度**：JS/Rust 只给 `div` 加了舍入，「加减乘超 28 位」
       三边给不同的数 → 补齐 + 按 CPython 的 `__str__` 实现科学计数法。
       顺带修 `舍入(n)`：正数补足小数位、负数换回定点写法（`1.2E+3` → `1200`）。
  3. **两处明确报错（补上）**：Rust 的**类型标注校验**（实现 `检查实参`，
     编译器早就把校验发射成前导字节码了）；Rust 的**`是`/`不是` 对类与函数**
     （创建时分配递增序号，`Clone` 跟着复制 ≈ Python 的 `id()`）。
     绑定方法仍明确报错（Python 里 `a.方法 是 a.方法` 为假，Rust 侧判会得真）。
- 📌 **M32 顺带发现（未动，已记进 docs/embed.md）**：
  - **树遍历不支持类体里写 `通过`**（只认方法和类变量）；
  - **JS 宿主没同步 M23 的「类变量」**（`类 A：` 里 `x = 1` 报错）；
  - 类型标注表里的 `空`/`函数` 两条**语法上写不出来**（关键字，三侧皆然）。
- 📌 **M33 已交付（Rust 标准库补齐到与 Python 同宽）**：从 3 个模块
  （数学/文本/随机，且函数不全）补到 **14 个模块、约 130 个函数**。
  **零第三方依赖**，重活全自己写：
  1. **正则**（`rust/src/regex.rs`）——自写回溯引擎（字符类/量词/非贪婪/
     分组/前后查看/锚点/`\b`/反向引用）；`查找全部` 的形状照 Python 的 `findall`。
  2. **加密**（`rust/src/crypto.rs`）——自写 md5/sha1/sha256/sha512，
     拿 `hashlib` 当裁判逐字节比（`abc`、空串等公开向量）。
  3. **压缩**（`rust/src/zip.rs` + `inflate.rs`）——手拼 zip 容器；
     **打包写 stored（没有 deflate 压缩器）**，但**解压必须支持 deflate**
     （别人打的包都是压过的），所以 inflate 是完整实现（三种块都支持）。
     测试还要**双向互操作**：Python 的 deflate 包 Rust 解得开、Rust 的包
     `zipfile.testzip()` 校验通过。
  4. **网络**（`rust/src/http.rs`）——Windows 绑 `winhttp.dll`
     （**系统库、不是 crate**）→ **https 也能用**；其余平台裸 `TcpStream`
     （`http://` 可用，`https://` 明确报错，不静默降级成明文）。
  另有两处 std 没有的东西要绕：**本地时区**（绑 `GetLocalTime`/`localtime_r`）
  与 **`strftime` 子集**；`系统版本` 在 Windows 上绑 `RtlGetVersion`。
- 📌 **M33 的教训（值得单独记）：探针全绿不等于做完了**。14 个模块补完、
  113 例对拍 0 不一致之后，按 M30 定下的标准拿 `examples/projects/`
  真实项目跑，**又撞出三块**：
  1. **`BUILD_SLICE`（切片）Rust 侧压根没实现**（M30 只补了 Node）；
  2. **`输入()` 内建不存在**；
  3. **`列表.排序` 静默失效**：`Val::partial_cmp` 只处理数字与精确小数，
     字符串/列表返回 `None`，排序把 `None` 当「相等」→ 排了等于没排
     （成绩分析的总分排名顺序全乱）。**不报错、不错值，只是顺序不对**，
     最隐蔽的一类。
  → **写完新模块一定要拿真实项目验一遍**，这是 M30 定下的标准，M33 再次验证。
- 📌 **M33 顺带修掉三处「跨宿主不一致」**（对拍才暴露）：
  ① **CLI 输出**在 Windows 上会把 `\n` 写成 `\r\n`（`_setup_io` 显式
  `newline="\n"`）；② **`系统.执行`** 用 `text=True` 做通用换行（改成按字节收
  再解码）；③ **`json.读文件`/`写文件`** 缺 `newline=""`。
  另有 ④ **`正则.查找全部` 多分组时漏出 Python 元组**（基石没有元组类型）
  → 统一成列表；⑤ **`类型(表格)` 给 `_Table`** → 加类型名钩子统一成「表格」。
- 📌 **M34 已交付（D 档第一批：模式匹配 + 枚举）**：两个都走「**解析期脱糖**」
  ——这是第四次、第五次（前三次：M23 `超()`、M24 集合字面量、M26 `用 … 为`）。
  解析器改写成既有语句的组合，**三执行器 + 两宿主全都不用改**。
  - **模式匹配** `匹配 x：` / `情形 值：` → `__匹配_1__ = x` + if/elif 链。
    四种分支：单值、多值 `情形 1, 2：`、守卫 `情形 值 如果 条件：`、
    纯条件 `情形 如果 条件：`、兜底 `情形 其他：`。待匹配值**只求值一次**。
    ⚠️ 「纯条件」那条是写原型时才想清楚的——最初把「通配」与「只有条件」
    混为一谈，「通配必须最后」把最有价值的守卫链写法堵死了。
  - **枚举** `枚举 颜色：红 = 1` → `类 颜色：` + `颜色.红 = 新建 颜色("红", 1)`
    + `颜色.全部 = [...]`。成员是**实例**（`打印` 给 `颜色.红`，还能 `.名字`/`.值`）；
    `==` 按身份（与 Python Enum 同）；不写值自动编号；只认成员不认方法。
- 📌 **M34 加关键字撞掉既有 API（重要教训，新增硬约束）**：`匹配` 一关键字化，
  两处立刻炸——① 标准库函数 **`正则.匹配`**（M33 刚写、真实项目在用）；
  ② 真实项目里的变量 **`匹配组`**（被「关键字粘连检查」拦下）。
  修法：① **`.` 后面的属性名允许关键字**（属性名位置没歧义；变量名/参数名/
  字典键仍不许，与 Python 一致）；② `匹配`/`枚举` 加进 `_GLUE_CHECK_SKIP`
  （它们和 `用`/`是` 一样是常用词前缀）。
  → **加关键字的检查清单：既有标识符前缀（skip 表）+ 既有 API 名（属性名）**。
- 📌 **M34 顺带撞出并修掉宿主侧三个缺口**：
  1. **Node / Rust 都没有「类变量」**（M23.3 一直没同步）——枚举脱糖要用
     「类体外给类设属性」才撞出来。补法省事：Python 的类变量本来就是靠
     `SET_ATTR` 实现的，宿主只需补 `getAttr`/`setAttr` 的类分支，字节码零改动。
     （Rust 的类变量表要 `Rc<RefCell<…>>`——`Instance` 存的是 `Class` 值拷贝。）
  2. **Rust `继续` 之后再也 `中断` 不了**（M29 引入信号分发时的真 bug）：
     `dispatch_signal` 里多弹了一次循环块，而 Python 侧弹的动作归 `POP_BLOCK`。
     单支/两支 if 看不出来，要「`继续` 过了还要用循环」才暴露——
     `情形` 脱糖恰好生成那种形状。
  3. **Rust 的 `类型()` 给错名字**（实例给「对象」、类给「类」，Python 给
     `A 实例`/`类 A`）→ 顺手把三侧的绑定方法名也统一（此前三个都不一样）。
- 📌 **D 档其余候选的 ROI 结论（M34 定的）**：**生成器 `产出`** 成本高
  （要状态机/协程，三执行器都得动）、**多继承**（MRO 复杂度高，单继承够用）、
  `全局`声明 / `删除`变量（成本低但需求弱）——都先不做，理由见
  `docs/设计决策.md` D26。
