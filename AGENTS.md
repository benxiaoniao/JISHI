# 基石（jishi）编程主轴 —— 所有 AI 编程工具共用

> 这是本项目的**单一编程主轴**。任何 AI 编程工具（WorkBuddy / Cursor /
> Claude Code / GitHub Copilot / CodeBuddy / Codex 等）在介入本项目前，
> **必读本文件**。
>
> 详细论证在 README.md（对外主文档）与 docs/ 下各文件，这里只放
> **工具最需要、最高频、必须遵守**的硬约束与当前任务。本文件是普通 Markdown，
> 任何工具都能读、都能写（尤其「当前任务」节由各工具协作更新）。

---

## 0. 一句话定位

基石 = 一门**中文编程语言**，对标 Python 的易用性。

**北极星（2026-09-05 调整）**：健全语言自身能力，让编程者**方便地实现意图**、
**出问题能快速定位**。

---

## 0.5 两条设计核心（2026-09-20 用户重申，一切取舍都回到这里）

> 这两条是**语言的立身之本**，比任何单条技术约束都优先。当两条冲突时，
> 先保「中文语法习惯」，再谈「便于模型调用」——因为语言自身立得住，
> 模型才会学得对。任何新特性、新内建、新错误文案，都要过这两条。

### 核心一：遵循中文语法习惯，发挥中文的优点

- **按中文母语的语序与搭配取名**，不照搬英文语序。正面例子：
  `遍历 项 在 列表`、`如果 甲 那么 … 否则 …`、`长度(列表)`、`包含`。
- **优先简短**：中文里天然短的词就写短（`长度` 而不是 `获取长度`，
  `打印` 而不是 `输出内容`）。冗余词一律砍掉。
- **用上中文的优势**：没有时态/单复数变化，就不要再造「复数形式」；
  助词、量词、连词灵活，可以让语法更贴近口语（`对每个 分数 在 分数们` 这类
  读起来像话的写法，胜过机械照译）。
- **底线是不产生歧义**：语法可以短，但**不能有歧义**；一旦某种简写会让
  人（或模型）读出两种意思，就退回写全。
- **不要「为中文而中文」**：不造生僻字词、不硬凑译名。读起来别扭的地方，
  宁可借常用词，也不搞花架子。

### 核心二：便于被大模型调用

- **语言事实必须机器可读**：`jishi --lang-spec`（JSON：关键字 / 运算符 /
  内建 / 标准库 / 异常）与 `jishi --ai-card`（贴进系统提示词的语言卡）。
  模型不该靠猜——本项目已把它做成**单一来源**（`jishi.ai.build_lang_spec()`）。
- **报错必须可操作**：错误码 + 行列位置 + **修法建议**（`errors.JishiError.fix`
  是机器可读的文本替换建议），让模型能自愈，不要让模型只看到一句「出错了」。
- **常见任务要有惯用写法**：能进内建/标准库的，不让用户手写绕道——
  绕道写法不仅人累，模型也更容易写错（M40–M41 的「别扭点清单」就是查这个）。
- **喂给模型的示例必须真能跑**：`examples/ai/few-shot.md` 的每段示例都进测试
  （M41 的教训：48 段里躺着 4 段真错，模型会照着错样本写）。
- **不要为「AI 友好」牺牲语言自身**：`--ai-card` 是**语言事实的投影**，
  不是另造一套说辞。投影可以调，事实不能编。

---

## 1. 硬约束（违反会坏事，必须遵守）

1. **两条设计核心优先**（见 §0.5）：遵循中文语法习惯、发挥中文优点；
   便于被大模型调用。取舍有疑问时回到这一条。
2. **三执行器语义必须一致**：树遍历 / Python VM / C VM。
   任何改动语义（语法/内建/方法/异常），**必须先跑 `tests/test_cvm.py` 对拍**。
3. **测试命令**：`python -m pytest tests/`（当前 2066 项全绿）。
   改完代码必须跑全量回归，不许留红。
4. **诚实不吹牛**：指标没度量就写「未度量」，绝不用估计值充数；
   架构边界（如字符串是 HOST 句柄）如实标注。
5. **README.md 是唯一对外主文档**：里程碑推进后，必须同步更新它的
   「技术栈声明 / 性能数据 / 能力清单 / 项目结构」四块，否则会「总结偏差」。
6. **README 不写个人路径**（用户名 / 本机 IDE 安装路径 / 受管 Python 路径），
   保持本地与公开版一致，发布时无需反复屏蔽。
7. **新特性准入三关**：真实需求驱动 / 可度量验收 / 投入产出比。
   说不出「怎么算完成」就不开工。
8. **发布走两条通道，规矩不同**：
   - **代码 → GitHub**：`tools/publish_github.py`（Gitee 已于 2026-09-20 弃用）。
     **每个里程碑提交后自动发布，不用问用户**——见 §4 的命令与两个坑。
   - **Python 包 → 自建分发**（2026-09-22 用户定：**放弃 PyPI**）。
     理由：分发链路全押在 Python 生态上不合适；我们已有自己的服务器（腾讯轻量云），
     包管理与分发自己做，并做自研的安装命令（暂名 `下载`）。
     **这条还没开工，属后续升级规划**（`docs/路线图.md`「方向四 · 自主分发」）。
     `tools/publish_pypi.py` 保留但**不再是发布通道**——它的三段逻辑（版本号
     一致性校验 / 构建 sdist+wheel / 隐私扫描）对自建分发照样有用，别删。
     ⚠️ **README 不许写 `pip install jishi`**——那个包永远不会出现在 PyPI 上；
     安装说明只写「从源码装」（`pip install git+…`）。

---

## 2. 关键技术备忘（踩过的坑，别重踩）

- **新增宿主回调时**：C 侧 `JsHost` struct 与 Python 侧 `JsHost._fields_`
  字段顺序必须**逐字段对齐**（曾因插错位置导致 ctypes 回调错位崩溃）。
- **异常穿透重入边界**：vm.py 用 `stop_at`、C VM 用 `stop_depth`，两边要一致。
- **Windows venv 路径**是 `Scripts/` 不是 `bin/`。
- **PyInstaller 打包**：入口必须用包外的 `tools/entry_cli.py` 绝对导入，
  直接打包 `jishi/cli.py` 会因相对导入报「no known parent package」。
- **改 C 代码后必须同步 DLL**：重新编译 `python cvm/build.py` 后，还要
  `cp cvm/bin/jsvm.dll jishi/_native/jsvm.dll`——运行时优先加载 `_native/`
  里的 DLL，否则跑的是旧 DLL（曾因此报「未知指令」）。
- **CI 的 Windows 上装 gcc**：不要用 `choco install mingw`（路径不稳定）；
  用 `msys2/setup-msys2@v2`（`msystem: MINGW64` + `install: mingw-w64-x86_64-gcc`
  + `path-type: inherit`）。GitHub runner 预装 MSYS2 但不在 PATH、且不含 gcc 包。
- **Windows 安装程序**：由 Inno Setup 编译（`tools/installer.iss`）；
  ISCC 查找覆盖 Program Files / %LOCALAPPDATA%\Programs（`/CURRENTUSER` 装法），
  优先用较新版本（7 优于 6）。
- **Windows 上打印中文**：CI/Cmd 的 Python stdout 默认非 UTF-8，会
  `UnicodeEncodeError`；脚本入口 `sys.stdout.reconfigure(encoding="utf-8")`，
  workflow 另设 `PYTHONUTF8=1`。同理 `strftime` 传含中文的格式串在 Windows
  Python 3.10 上会崩（见 `jishi/stdlib/日期.py` 的摘出/放回处理）。
- **C VM 性能优化思路（M20 经验）**：先 benchmark + cProfile 定位，别猜。
  真正的瓶颈往往在 **ctypes 边界的数据转换**（逐个构造 `JVal` ≈1µs/值），
  而不是 C 侧循环本身。批量优化手法：整批同类型时一次 `struct.pack` +
  `memmove` 写入 C 数组（见 `cvm_bind._cb_iter_next_batch`），比逐个快 ~9x。
- **推 tag 不要推本地仓库**：本地 git 历史含旧版个人路径，用
  `python tools/publish_github.py --tag vX.Y.Z` 在屏蔽版快照上打 tag。
- **词法器 Token 的 `end_col` 约定不一致（易踩）**：OP 类 token 的区间是
  `[col-1, end_col-1)`，而 NAME/STRING/NUMBER/FSTRING 是 `[col-1, end_col)`。
  按列切片取原文时必须分类处理（见 `jishi/formatter.py` 的 `_respacify`）。
- **格式化器/新工具尽量复用既有分析器**：格式化器用词法器算缩进层级，
  LSP 的关键字/内建/标准库数据取自 `ai.build_lang_spec()`——不要在工具里
  另抄一份语言元数据，否则会与语言本体漂移。
- **URL 里的中文要编码，但别二次编码**：包名是中文，下载地址生成时就得
  编码好（域名走 IDNA，路径走 `quote(path, safe="/%")`）。safe 里必须保留
  `%`，否则已编码的前缀（如 `Path.as_uri()` 的产出）会被再编成 `%25`。
- **`detect_conflicts` 判环只能看「链上是否出现过包名」**：用
  `(名字, 整条链)` 当访问键在环里永不重复，会无限递归（写 M22.3 时踩到）。
- **Windows 下读 stdin 必须按字节 + UTF-8**：打包版不继承 `PYTHONUTF8`，
  `sys.stdin.read()` 会按 GBK 解码，中文源码变乱码（`打印`→`鎵撳嵃`）。
  统一用 `cli._read_stdin()`（字节层读 + 显式 UTF-8 解码），MCP 的
  JSON-RPC 读取与子进程传参同理；入口脚本三个流都要 `reconfigure`。
- **打包版找同级可执行文件**：PyInstaller 冻结后 `sys.executable` 是自己，
  `-m jishi.cli` 会失败；MCP 需改调发行目录同级的 `jishi`（见
  `mcp_server._cli_command`）。发行版布局是 `bin/jishi/` 与 `bin/jishi-mcp/`
  互为兄弟目录。
- **`cvm/include/opcodes.h` 是自动生成的**（`cvm/build.py` 从
  `jishi/opcodes.py` 生成），**手改会被覆盖**。要加常量先加到 `opcodes.py`
  的对应类（如 `ParamKind` / `CmpOp`），再跑 `python cvm/build.py`。
- **改 C 代码的完整流程**：改 `cvm/src/jsvm.c` → `python cvm/build.py`
  → **务必** `cp cvm/bin/jsvm.dll jishi/_native/jsvm.dll`（运行时优先加载
  `_native/`，不拷就是跑旧 DLL）。
- **加语义比较运算符 / 新指令参数可以不动 C**：比较码放 6–9（0–5 是 C VM
  快路径，6 以上回落宿主 `host->compare`）；`UNPACK` 的 `b` 操作数闲置，
  直接拿来传星号位置（M25）。
- **`bind_args` 的所有权语义是「借用」**（M25 起）：实参由调用者释放，
  进槽位的值由 `bind_args` retain。因为 `*参数` 会出现「同一个实参既被打包
  进列表、又留在数组里等统一释放」，借用语义下只需统一 release 一次。
- **新增宿主回调必须追加在 `JsHost` 末尾**（C 与 Python 两侧逐字段对齐）：
  插在中间会让后面所有字段错位，ctypes 直接崩（项目历史上踩过）。
- **「三执行器语义分歧」的检查要主动做**（M35 踩到）：类体里写非法语句时，
  树遍历报错、而两个字节码执行器**静默忽略**（`x += 5` 甚至被当成 `x = 5`
  静默算错）。凡是「只在某一个执行器里才出现的行为」，都值得写个小脚本
  三路各跑一次对比。**修法是放到解析期**：三个执行器都要过 parser，
  一处校验就全一致（与「解析期脱糖」同一思路）。
- **空块在基石里是单独一行 `:`**，没有 Python 的 `pass` 关键字
  ——`通过` 只是个普通名字（曾把它误当关键字，见 M35）。
- **测试里不许出现「开发机事实」**（M35：CI 长期全红的唯一根因就是这个）：
  Rust 可执行名（Windows 是 `jishi-rs.exe`，别处是 `jishi-rs`——用
  `conftest.rust_exe_path()`）、`build/` 目录（CI 全新检出没有它，conftest 负责建）、
  平台名（别写 `"Windows"`）、**时区**（别写 UTC+8 的 `08:00:00`，runner 是 UTC）。
  写测试时凡遇「我这台机器上是什么」，都要问一句「CI 上是什么」。
- **「本地能装 ≠ CI 能装」**（2026-09-23 M45 收尾抓到）：`pip install -e .`
  这类**打包/安装**问题，凡本地能过、CI 过不了的，先 `git archive HEAD | tar -x`
  导出一棵干净树再装——开发机的构建产物会掩盖「干净检出缺文件」。
  真实案例：`packages` 列了 `jishi._native`，但目录里只有被 `.gitignore` 排除的
  `*.dll`，干净检出时目录整个消失 → 四平台全倒（`package directory does not exist`）。
  修法是 `.gitkeep` 让目录入库；测试用 `git ls-files` 判「目录是否被跟踪」，
  而不是看本地磁盘。已钉进 `tests/test_m16_release.py`。
- **CI 失败怎么查**：两个工作流里有「失败摘要 → `::error` 注解」+「环境诊断」，
  注解可由公开 API 读（`check-runs/annotations`），不必登录网页下 job 日志
  （那要管理员权限）。注意注解步骤需 `set +e`，且要抓 pytest 的 `E ` 行才有
  完整消息。
- **解析期脱糖优先**（M26.2）：新语法如果能写成「既有语句的组合」，就让它
  在 parser 里脱糖、返回**多条语句**（`_parse_statement` 可以返回 list，
  `_parse_statements` 会展平），三执行器零改动、行为必然一致。
  `超()`（M23）、集合字面量（M24）、`用 … 为`（M26）都是这个手法。
- **脱糖时别把「绑定后的值」当成原对象**（M26 踩到）：`用 X 为 v：` 里
  `v` 是 `进入()` 的返回值，可能不是 `X`；清理必须针对 `X`，所以要留一个
  临时变量 `__用_N__`。这类错误**静默**（清理不报错，只是没生效），
  只能靠「对照 Python 语义写测试」发现。
- **`cargo` 什么都不做 → 先看 shim 是不是 0 字节**（2026-09-13 查明）：
  rustup 在 Windows 上把 `~/.cargo/bin/cargo.exe` / `rustc.exe` 等做成指向
  `rustup.exe` 的**硬链接**。链接一旦坏掉就退化成 **0 字节空文件**，
  执行它「成功且无输出且 `rc=0`」——极像「命令被沙箱吞了」，很容易误判成环境限制。
  诊断：`ls -la ~/.cargo/bin/`（真身 14MB，坏的 0 字节）；
  真身在 `~/.rustup/toolchains/<toolchain>/bin/`。
  修复：`ln -f rustup.exe cargo.exe`（每个名字来一次，等价于复制）。
  另外 `cargo build` 的产物时间戳是判断「到底编没编」的硬依据：
  `touch` 源文件后重新 build，exe 时间戳不变就说明根本没编译。
- **Rust 宿主（`rust/`）的验证方式**：`windows-gnu` target 用 gcc 当链接器，
  构建前把提供 gcc 的目录加进 PATH（或用 `JISHI_MINGW` 指定）。
  跑 `tests/test_m27_rust_host.py` 做跨宿主对拍（无 cargo 时自动跳过）。
  **不写死本机路径**——沿用「README/文档不写个人路径」的约定，
  否则 `tools/publish_github.py` 的隐私终检会拦下发布。
- **Rust 侧可变容器已修（M28）**：容器是 `Rc<RefCell<Vec<…>>>`，方法里用
  `borrow_mut()` 改共享数据（以前是裸 `Vec`、改的是副本 → **静默算错**）。
  配套要注意两点：① 「列表自身当迭代器」的实现必须**显式快照**
  （`make_iterator` 里 `list_new(l.borrow().clone())`），否则 `遍历` 会把
  原列表搬空；② 类/函数是值类型、**没有对象身份**，对它们用
  `是`/`不是` 会明确报错（不做「按内容相等」的静默降级）。
  改动渲染前先看 `docs/embed.md` 的支持程度表。
- **Rust 宿主：凡是「改了就希望调用方看见」的东西都要 `Rc<RefCell<…>>`**（M29）。
  实例也不例外——`Instance` 一开始是值类型，于是 `自身.字段 = 值` 走 `SET_ATTR`
  改的是**副本**，方法里的赋值全部**静默失效**。换成 `JInst = Rc<RefCell<Instance>>`
  之后，`是`/`不是` 也顺带有了对象身份。
- **Rust 宿主：内建要回调用户函数，靠 `VM::call_value`**（M29）。
  做法是压一个帧、在**自己的帧深**上停下（`execute(Some(depth))`），返回值留在栈顶。
  两个必须注意的点：① 回调前先**快照**容器内容——拿着 `RefCell` 的借用去回调，
  回调里又改同一个容器就会撞上运行时借用冲突；② 异常不会漏到外层帧，
  因为 `dispatch_exception` 见到 `stop_at` 就返回。
- **Rust 宿主：异常与 finally 的三处关键实现**（M29 补齐，此前全是坏的）：
  ① `execute` 里 `Err(e)` 必须走 `dispatch_exception`——直接 `return Err(e)`
     会让 `尝试/捕获/最终` 对**内置错误**完全失效；
  ② `返回` 要经过 `step_return` 先跑 finally（返回值挂进 `frame.pending_return`，
     在 `END_FINALLY` 收尾），对应 Python 侧的 `_return_via_finally`；
  ③ 循环信号（`中断`/`继续`）要穿过 finally 时，用 `Val::LoopSignal` 借道值栈，
     `CHECK_SIGNAL` 判它、末尾的 `THROW` 还原成信号；「最近的循环 vs 最近的 try」
     谁先接，由登记序号 `block_seq` 比较决定（对齐 Python 的 `_Handler.seq`）。
- **加关键字的检查清单**（M34）：① 它是不是常用词**前缀**？是的话加进
  `tokenizer._GLUE_CHECK_SKIP`（否则 `匹配组`、`枚举值` 这类标识符会被
  粘连检查拦下）；② 有没有**既有 API 用了这个词当属性名**？（`正则.匹配`
  被撞过——`.` 后面的属性名已放开接受关键字，但新写的库函数仍要留心）。
- **「脱糖」时不要把两种语义不同的写法塞进同一个分支**（M34）：模式匹配里
  「通配兜底」（必须最后）与「只有条件的守卫」（可放中间）长得像、语义不同，
  混在一起就堵死了守卫链写法。**写原型时先把每种写法的语义分开列一遍**。
- **宿主侧「值→文本」这类映射表是漏点重灾区**（M33/M34 各一次）：
  `类型()` 的名字在三个宿主里三个样（`_BoundMethod` / `BoundMethod` /
  「内建方法」）。加新类型时三处都要对一遍。
- **`Val` 多一支就要到处补**（M33）：Rust 侧新增 `Date`/`Table`/`Slice`
  三支时，每支都得在 `display` / `type_label` / `identity_key` / `PartialEq`
  里各接一次——漏一处就是「静默给错」。
- **`partial_cmp` 返回 `None` 会被排序当成「相等」**（M33）：
  `列表.排序` 用的是 `partial_cmp().unwrap_or(Equal)`，所以**漏实现一种类型的
  比较 = 那种类型的排序静默失效**（字符串、列表都栽过）。
  新增类型时要同时想「它能不能比大小」。
- **对拍脚本里不要用 `text=True` 收子进程输出**（M33）：它默认做通用换行，
  会把 `\r\n` 悄悄改成 `\n`，于是「输出换行不一致」这类问题**永远看不出来**
  （M30/M31 的端到端对拍就是这样漏掉的）。要按字节收再显式解码。
- **不要在 ctypes 边界上相信「赋给 c_int64 会自动检查范围」**（M32）：
  ctypes 是**静默截断**——`2 ** 64` 变成 `0`、`i64_MAX + 1` 变成负数。
  C 侧即使做了 `__builtin_*_overflow` 回落，回落算完之后仍会在边界丢。
  所有 Python→ctypes 的整数转换都要自己检查范围。
- **JS 双精度是「静默算错」的大户**（M32）：`2 ** 64` 给
  `18446744073709552000`、`9223372036854775807` 显示成 `...776000`。
  对策是**按需切 BigInt**（任一边是 BigInt，或 number 结果超出
  `Number.isSafeInteger`），而不是「一律用 BigInt」——后者会拖慢所有整数运算。
  另外字节码里的 int 常量超出安全范围时要按**字符串**编码（JSON 数字在 JS 里
  解析时就丢精度，reviver 也救不回来）。
- **跨宿主的三条「平台默认行为」坑**（M31 一次踩齐）：
  ① **Node 没有内置 zip**——`zlib` 只有 deflate 流，容器要手拼（见 vm.js 的
     `writeZipEntry`/`centralEntry`/`readZipEntries`）；zip 条目名必须用 `/`
     （Python 的 zipfile 也会把 `\` 换掉）。
  ② **Node 没有同步 HTTP**——用 `spawnSync(process.execPath, ['-e', 脚本])`
     起自身子进程跑 `fetch`，stdout 回传；这是基石没有 `await` 时的唯一解法。
  ③ **Python 的文本模式会转换换行**（Windows 上 `
`→`\r\n`）——
     基石标准库统一 `newline=""` 保真，否则「同一份程序到哪都一样」就成了空话。
- **跨宿主的「码值映射表」是漏点重灾区**（M28/M30 各撞一次）：加了一个语法
  特性后，Python 侧改了 `opcodes.py`，但宿主侧的表往往只写到旧范围——
  Rust 的 `compare(op)`、JS 的 `CMPOP` 都只处理 0–5，于是 `在/不在/是/不是`
  （6–9）静默落空。**改完这类表要跑横向探测脚本**，别信文档里的 ✅。
- **JS 宿主的三个「看着能跑其实不对」**（M30 实测出来的）：
  ① `Boolean([])` 是 `true`，而 Python `bool([])` 是 `False` → 空容器判断反了；
  ② `===` 是引用比较，容器 `==` 得自己递归按值比；
  ③ 内建 `Set` 的相等是 SameValueZero，与 Python 的 `hash/eq` 不同
     （`{1, 1.0, 真}` 在 Python 里只留一个）→ 集合要自己维护规范化键。
- **Rust 精确小数是零依赖手写 BigInt**（`rust/src/decimal.rs`，M29）：
  系数 base 10^9 小端、28 位有效数字、ROUND_HALF_EVEN。**`**` 只支持整数指数**——
  非整数次幂是近似运算（Python 的 `Decimal` 走 `context.power`），各执行器必然漂移，
  所以报中文错而不是给近似值。
- 更多见 README「已踩过的坑」一节。

- **调试器的钩子只有一个位置**（M37）：树遍历里挂在 `interpreter.exec_stmt`
  开头的 `before_stmt` 上，断点、单步（进/过/出）、看变量、看调用栈**全都由它推出**
  ——三种单步只靠「帧深」区分（`下一步` = 深度 ≤ 暂停时深度、`跳出` = 深度 <）。
  字节码 VM / C VM 没有「语句边界」事件（栈机只知道指令），所以调试器目前
  只在树遍历上工作（见 `docs/调试器.md`）。
- **调试器的「退出」必须是 `BaseException`**（M37）：`_do_try` 里那句是
  `except BaseException`（为把 Python 异常翻译成中文），如果 `DebugQuit` 是
  `JishiError`，用户程序里一句 `捕获：` 就能把「退出」吞掉。配套要在
  `_do_try` 的「离开控制信号」元组里显式放行它（与 中断/继续/返回 并列）。
- **一行里的多个分支共用局部变量要当心**（M38 B4 踩到）：格式化器把
  `body = raw[len(lead):]` 只写在「普通代码行」分支里，续行分支要用它时拿到的是
  **上一轮循环的旧值** → 续行被整段替换成上一行的内容、输出直接语法错。
  Python 不会报未定义（函数作用域），**只在多行结构上暴露**，单行用例永远看不见。
- **注释识别不许用 `line.find("#")`**（M38 B4）：字符串里的 `#` 不是注释，
  朴素查找会把 `令 甲 = "含 # 号"` 从中间劈开、报「字符串没有正常结束」。
  一律走状态机扫（`formatter._scan_code`），并让「括号计数」复用同一个扫描器。
- **增量/续跑类改动会把老 bug 照出来**（M38 B2 一次挖出两个）：词法器的
  `content_start` 在「括号内续行」分支从没赋值（靠上一轮残留值侥幸可用，
  顶格续行会被整行跳过）；三引号未闭合时**静默丢弃**整段内容。前者以
  `UnboundLocalError` 暴露，后者靠 REPL 续行判定才发现。
  **从状态中间开始跑，等于给状态机做了一次「全路径体检」**。
- **栈机也能有「语句边界」，但要让编译器说话**（M39 B5）：别去猜行号表
  （行号一变的地方远多于语句边界，循环回边会让循环头每轮都停），
  让编译器在每个语句的**首指令**上打标记，VM 主循环一次属性读取就够。
  配套不变式：**打标行的集合 == AST 语句行的集合**（拿全部语料对拍）——
  这条不变式一上线就抓出「类体方法定义行」「空块 `:` 行」两个原先**两边都停不住**
  的静默失效断点。另外，「一开始就停不住」的语句（空块、类定义开头）要占用一条
  空操作指令来挂标记，否则标记无处安放。
- **两个执行器的一致性，用「会话记录逐字对拍」来验收**（M39）：
  同一脚本 + 同一组断点 + 同一串命令，输出必须一字不差（含变量表、调用栈、
  出错现场）。这比「各自看起来对」强得多——它与做 B1 时踩的所有坑都兼容。
- **有副作用的「隐式顺序」也要对拍**（M39）：类变量在字节码执行器里是「先造类、
  再按源码顺序求值」，在树遍历里是「按源码顺序求值」——两边**都不报错**，
  只有带副作用的类变量才露馅。**凡是「多个语句/表达式的执行顺序」，都在
  一致性范围内**。
- **`导入 文本` 曾打断 f-string**（M39 记录 → **M40 已修**）：f-string 原先脱糖成
  `文本(值)`，模块「文本」正好遮蔽这个内建 → 报「模块「文本」不能直接调用」。
  现在脱糖到**用户写不出的内部保留名** `$文本`（`$` 在词法阶段就是「无法识别的
  字符」），三个执行器与 Node / Rust 两个宿主都注册它。
  **判据**：内部名一律走 `runtime.is_internal_builtin`（`$` 开头，或说明以
  「内部用」开头），且**不许出现在语言规格 / 补全里**——否则会被推荐给用户。
- **多行三引号字符串的收尾引号独占一行时，词法器必须补 NEWLINE**（M39 修）：
  少了它，解析器会把下一行并进同一句、报「这一行没写完」——
  整个特性不可用，而语料里没人真跑过。
- **出错现场要「执行语句前」留快照**（M37）：错误是在语句执行到一半时冒出来的，
  等它传到 `debugger.run()` 时 `_eval_call` 的 `finally` 已经把帧弹干净了，
  那时再问「当前帧」只会得到模块顶层、变量还是上一帧的（实测踩到过）。
  **变量也要当场复制**（M40）：C VM 的帧按「第几层」编号，弹帧后那个编号指向
  别的帧或越界，事后问只会得到空表。
- **`END_FINALLY` 里不能用 `SYNC_FRAME`**（M40）：它会先 `SAVE_IP()`，把异常分发
  刚设好的跳转目标冲掉（此时 `ip` 还指着 `END_FINALLY` 的下一条）——表现是
  `最终` 里的打印执行了、程序却「正常结束」。要手动重新同步帧。
- **进了退出流程就不能再请求一次中止**（M40）：`最终` 块的第一条语句会再次触发
  调试钩子，若又返回「请求中止」，`最终` 一行都不会执行。会话要记一个标记。
- **新增的 `.jsh` 语料要过两道自动检查**（M40 踩到）：`jishi 格式化 --check`
  （风格统一）与 LSP 语料误报检查（`tests/test_m21_lsp.py` 扫 `examples/**`）。
  手改过文件后**必须重跑格式化器**，否则下一次全量回归才报、很难定位到是哪个文件。
- **报错文案要放单一来源**（M40）：对拍比的是 `(输出, 报错全文)` **整对**，
  文案差一个字就判成「执行器不一致」。同一个错误别在三个执行器里各写一遍
  （`runtime.unbound_local_error` 是正面例子）。
- **`except` 的元组里不能放异常的「实例」**（M41 踩到）：`except (A, B, 哨兵)`
  里 `哨兵` 必须是**类**，写成实例会在运行时炸成
  「catching classes that do not inherit from BaseException」。危险之处在于
  **只在某一路才触发**：M41 把 C VM 批量迭代的两趟遍历改成一趟，用内部异常
  回退，写成实例后**只有「批量里混了非整数」才炸**（`遍历 [[1,2],[3,4]]`
  立刻中招，单层整数列表一路正常）。**对拍是唯一抓得住它的手段**。
- **标准库模块名先查一遍内建名与关键字**（M41）：`集合` 是内建函数名
  （`集合([1,2,3])` 去重造集合），路线图原先计划的标准库模块也叫「集合」——
  一 `导入` 就把内建遮蔽（同 M40 的 f-string 坑）。已改名**「容器」**（D35）。
- **有状态的标准库模块要「每次导入造一份」**（M41）：`日志`（级别/前缀）与
  `测试`（用例记录）若把状态放**模块级全局**，同一进程里跑第二个执行器会串
  （五路对拍必炸）。约定钩子 `__基石新建__()`，`do_import` 每次调它拿一份新状态
  （D37）。**配套**：`ai._stdlib_api` 的规格扫描要**往工厂函数里再看一层**，
  否则这些模块的函数会从语言规格里消失。
- **验证「三执行器一致」不能用 CLI 的 `--执行器`**（M41 踩到）：那个选项只属于
  **`jishi 调试`** 子命令；普通运行脚本时它会被当成**脚本参数**吃掉，于是
  「三路输出一致」是假象（实际只跑了树遍历）。要三路对拍就**直接调 API**
  （`Interpreter.run` / `VM.run` / `cvm_bind.run_source_c`），测试里已有现成脚手架。
- **喂给 AI 的示例本身也是代码，必须进测试**（M41）：`examples/ai/few-shot.md`
  48 段示例从没人跑过，体检发现 **4 段真错**（变量名撞内建、闭包计数器撞
  「赋值即局部」、用了不存在的解包遍历与 `字典.键值对()`）。AI 会照着错样本写，
  危险度高于普通文档。逐段测试见 `tests/test_m41_stdlib.py`。
- **测试不许污染工作区**（M41 踩到）：把 `examples/ai/few-shot.md` 的示例逐段
  跑起来时，有几段会写文件（`文件.写文本("输出.txt", …)`、`json.写文件("数据.json", …)`、
  `压缩.打包("备份.zip", …)`）——在仓库根目录跑，回归结束后工作区就多了三个文件。
  跑这类「会写相对路径」的代码要在 `tmp_path` 里跑（`monkeypatch.chdir(tmp_path)`）。
- **测试全绿不等于功能真的对**（M43 踩到，这条代价最大）：调试适配器的
  `initialize` 响应把能力声明平铺在 `body` 顶层（DAP 规范要求 `body.capabilities`），
  编辑器读不到于是所有开关退回默认值——而**34 项协议测试全绿**，因为那条断言写的
  `caps = init["body"]` 是**照着实现写的**：实现错、断言就跟着错，两边一起漂移。
  纪律：**协议 / 契约类代码，除了「按自己的理解写断言」，还要拿一个独立客户端把
  典型场景真的用一遍**。M43 这两个 bug 都是给用户写演示清单时才撞出来的——
  用 `DapClient` 起一个真会话，从「下断点」一路走到「跑完」。
- **写「已有某个交付物」之前，先确认它在发布快照里存不存在**（M43）：`.vsix` 在
  扩展的 `.gitignore` 里（构建产物不入库），而我先在 README 写了「打包好的
  `vscode-jishi-0.3.0.vsix` 可直接安装」——**发布出去的仓库里根本没有那个文件**。
  本地磁盘上有 ≠ 读者拿得到；要写就写「怎么打出来」（给出一行可复制的命令）。
- **给用户的操作清单，自己要先用一遍**（M43）：清单里每一步（哪个目录、按哪个键、
  填什么、看到什么算成功）都得真跑过；演示用的样本文件也要先确认它真能跑、
  跑得快、不读输入——否则用户按着做会卡在一个我根本没验证过的地方。

---

## 3. 当前任务与状态（各工具协作更新这一段）

- ✅ **M0–M37 全部完成**（语言核心 → AI 工具链 → 沙箱/MCP → 生态 → 性能分发 →
  评测闭环 → 跨语言分发 → 包管理 → 语言收敛 1.0 → 发行版 → 排错体验补全 →
  表达便利 → 发行改进 → 性能优化 → 开发者工具 → 包生态 → 语言功能 A/B/C 档 →
  变长参数 → 值→文本统一 → Rust / JS 宿主补齐（含标准库同宽）→ 边界清单核实 →
  模式匹配与枚举 → 发布工程与三平台 Release → **语法冻结** → **调试器**）。
  里程碑明细与各轮教训见 `PROGRESS.md`（末尾「里程碑明细 / 路线图历史归档」）。
- 📌 **M35（发布工程与收尾）**：语言图标进发行物（多尺寸 ico / icns / Linux
  桌面项 / Inno Setup 图标）；类体校验统一到解析期（原先三执行器各不相同，
  甚至 `x += 5` 静默算错）；修掉「测试里写死开发机事实」这条根因，
  把长期全红的 CI **修到四平台全绿**。
- 📌 **M36（主线 A 收敛 → 语法冻结）**：条件表达式 `甲 如果 条件 否则 乙`
  （脱糖，三执行器 + 两宿主零改动）+ 位运算内建 6 个 + `字典.合并` +
  Node/Rust 宿主同步（顺手补上 Rust 一直缺的 `字典.更新/弹出`）+
  探针「无悬案」判据；三份路线图合并成一份。**语法面就此冻结**。
- 📌 **M39（主线 B 收尾 + 主线 C 开头）**：
  - **B5 字节码执行器的调试器**：`jishi 调试 --执行器 vm`。栈机没有「语句边界」
    事件，于是让**编译器在每条语句的首指令上打标**（`Instr.stmt_line`），VM 主循环
    只在标记处回调调试器。循环回边落在语句中部，所以循环头只停一次——**与树遍历的
    暂停点严格对齐**，两个执行器的会话记录逐字相同（`tests/test_m39_vm_debugger.py`
    逐项对拍）。C VM 当时判为暂不做（帧在 C 侧，要结构体 + 宿主回调）——
    **M40 已补齐**（见下）；
  - **顺手修掉四个潜伏缺陷**（都不报错、只是你以为有）：跨行三引号字符串的
    收尾引号独占一行时不补 NEWLINE（**多行字符串整个特性不可用**）、类体里的方法
    定义行两个执行器都停不住、类变量求值顺序与源码顺序不一致、
    `导入 文本` 会打断 f-string（最后一个**尚未修**，已列入路线图）；
  - **主线 C 开头**：`examples/projects/基石自述站/` —— 用基石写的静态站生成器，
    读仓库真数据（版本号 / 测试数 / 探针 / 里程碑 / 模块规模）生成一套介绍基石的
    网页；三个执行器跑出来的产物**逐字节相同**。
- 📌 **M38（主线 B）**：**工具链收尾**（主线 B 就此收齐）：
  - **B2 LSP 增量同步**：声明增量同步（客户端只发改动的那一段）+ **按行复用前缀的
    增量分词**（`jishi.incremental`，依据是 `tokenizer.LineAnchor` 记的七个状态值）。
    实测每键分词成本：18001 行改末行 359.7ms → **8.5ms**；1801 行 33.4 → 0.5ms。
    **`parse` 仍是全量的**（如实标注在 `docs/工具链.md`）；编辑范围按 LSP 默认的
    **UTF-16 码元**换算；版本乱序直接忽略。
  - **B3 REPL 增强**：↑/↓ 跨会话历史（`~/.jishi/repl_history.txt`，`JISHI_HISTORY`
    可改）、Tab 补全（数据来自 `ai.build_lang_spec()`，不另抄一份）、多行编辑
    判定交给真词法器。Windows 自实现逐键编辑 / POSIX 交给 `readline` /
    **非终端退回 `input()`**。
  - **B4 格式化器边界**：修掉「字符串里的 `#` 被当注释」「括号续行里的注释被吃掉」
    「带 BOM 的文件直接崩」「`格式化 <目录>` 甩英文 traceback」四个真问题。
- 📌 **M37（主线 B1）**：**调试器** —— `jishi 调试 脚本.jsh`：
  断点 / 单步（进·过·出）/ 看变量 / 看调用栈 / `查看 表达式`；出错时先打
  「出错那一帧的变量 + 调用栈」的现场，再打正式中文报错。
  **目前只支持树遍历执行器**（字节码 VM / C VM 是栈机、没有语句边界事件，
  要另做一套）；钩子**只读**，带调试器跑与不带，程序输出逐字节相同。
  用法与边界见 `docs/调试器.md`。
- 📌 **M43（本轮）**：**方向二 · IDE 可用性第二轮**：
  - **新增 `jishi dap`**：把三个执行器的调试器接到 **Debug Adapter Protocol**。
    覆盖 initialize / launch / setBreakpoints / configurationDone / threads /
    stackTrace / scopes / variables / evaluate / continue / next / stepIn /
    stepOut / pause / disconnect 与 initialized、stopped、continued、output、
    exited、terminated 六个事件。**三执行器逐项一致**（`tests/test_m43_dap.py`
    34 项）；唯一差异是 C 虚拟机不支持就地求值——明说原因并给替代方案，不给错值。
  - **线程模型是这轮唯一要动脑的地方**（D42）：主线程跑 DAP 消息循环，脚本线程
    跑程序；暂停时脚本线程**阻塞在命令队列**上，所以主线程读栈与变量时执行状态
    是静止的。`stopped` 事件必须在**暂停判定一成立**就发（编辑器收到就来问栈）。
  - **扩展接调试适配器**（D43）：用 VS Code 内置的 `DebugAdapterExecutable`，
    **没有自己写 DAP 传输层**——语言服务那条路要自己写是因为要在编辑器外也能跑
    同一份代码，调试这条路没有这个需求。
  - **打包 `.vsix`**（26KB）：装完在**任何窗口、任何文件夹**打开 `.jsh` 都有效，
    不必再走「F5 + 扩展宿主窗口」（从宿主窗口打开新文件夹会另开一个普通窗口，
    那个窗口不加载开发中的扩展）。`.vscodeignore` 已排掉 99MB 的 `node_modules`。
  - **高亮与片段对齐语言事实**（D44）：内建 15 → **28**、新增标准库模块名 19 个
    与异常类型 9 个着色、关键字补 7 个、片段补 7 条；
    `tests/test_m43_highlight.py` 钉住「新增内建忘了同步高亮表就红」。
- 📌 **M42**：**方向二 · IDE 可用性第一轮**：
  - **语言服务补六项能力**：`formatting`（接上已有格式化器）/ `rename` +
    `prepareRename` / `codeAction`（报错里的修法建议→可点快速修复）/
    `signatureHelp`（文档解析不了时走 token 兜底）/ `workspace/symbol` + 跨文件
    跳转 / `semanticTokens`（关键字/标准库/用户符号/形参/属性五类）。
  - **VS Code 扩展升级为语言客户端**（此前**连 `main` 都没有**，只有语法高亮）：
    自己写 `lsp-transport.js`（约 150 行：分帧/请求应答/通知分发），
    **零运行时依赖**（没引 `vscode-languageclient`，见 D39），11 项能力逐条接上。
  - **`editors/vscode-jishi/verify-lsp.js` 端到端自检**（25 项断言）：用扩展**同一份
    传输代码**真的起一个 `jishi lsp`，把每项能力点一遍——脱离 VSCode 就能验
    「点了有没有反应」。已进回归（`tests/test_m42_vscode_client.py`）。
  - **三条有意的边界**（宁可少做也不改坏用户代码，见 D40）：
    ① 重命名**只做模块级符号**（AST 不记形参位置，且没有作用域分析会误伤同名
    局部变量 → 返回 `null` 让编辑器明确拒绝）② 跨文件跳转**只在唯一命中时生效**
    ③ 工作区符号索引**有封顶**（400 文件 / 512KB / 跳过 `.jishi` 与隐藏目录）。
- 📌 **M41**：**方向一 · 语言能力第二轮**：
  - **标准库补 5 个模块**（`容器` / `迭代` / `参数` / `日志` / `测试`），
    **五路同宽**（三执行器 + Node + Rust）。**模块名是「容器」不是路线图里写的
    「集合」**——`集合` 是内建函数名，同名模块会把内建遮蔽（D35）。
  - **内建 `带下标` / `配对`**（同 Python 的 `enumerate` / `zip`）。
  - **`遍历` 支持多变量解包**（`遍历 名字, 年龄 在 对们：`）：解析期脱糖，
    **三执行器与两宿主零改动**（D36）。
  - **错误归类修正**：`整数("abc")` 从「类型错误」改「值错误」（对齐 Python
    与既有的 `"abc".转整数()`）；`待办清单` 的 `捕获 异常` 已收窄。
  - **`路径.递归列出(目录, 后缀)`**；真实项目替换绕道写法后 **行数比 1.27 → 1.21**。
  - **C VM 性能定位**：瓶颈仍是 ctypes 边界（58%–87%）；批量迭代回调由
    **两趟遍历改一趟**，整数循环 9.5x → **10.5x**、函数调用 13.3x → **15.7x**。
- 📌 **M40**：**穿插项收尾 + 主线 C 铺开**：
  - **C VM 的调试器**（B5 最后一格）：C 侧加四个**只读**入口（语句起始表 /
    语句钩子 / 帧元信息 / 帧变量导出），**三执行器会话记录逐字对拍**
    （`tests/test_m40_cvm_debugger.py`）。唯一没做的是 C VM 的**就地求值**
    （C 侧没有「在任意帧上求表达式」的入口），会话明确说明而不是给错值。
    顺带补上 C VM 一直缺的**报错调用链**（M17 给另两个执行器做了，C 侧漏了）。
  - **树遍历「赋值即局部」补齐**：进函数时给「本函数赋过值的名字」占未绑定
    哨兵；判定**复用编译器的收集器**，报错文案走**单一来源**
    （`runtime.unbound_local_error`）。M39 那条 `xfail(strict=True)` 已转正常断言。
  - **f-string 遮蔽修复**：脱糖目标从内建 `文本` 改成用户写不出的 `$文本`；
    内部名从语言规格 / 补全里过滤掉（`runtime.is_internal_builtin`）。
  - **主线 C：五类真实项目 + 行数比基线 1.27**（有效行加权，合计 688 ÷ 542）。
    口径、逐项数据与「别扭点清单」见 `docs/真实项目.md`。
- 📌 **下一步（新阶段三方向）**：路线图 §3.6 定的是「语言能力 / IDE 可用性 /
  AI 可用性」三个方向，M40–M45 顺序推进。
  **M42 / M43 已完成**（语言服务六项能力 + VS Code 语言客户端；`jishi dap` 调试
  适配器 + 扩展打包）。**M44 / M45 已完成**（机器可读语言事实 + 静态检查；
  AI 跑分表 + MCP 工具面 7 个）。**方向三就此收齐**。
  **M41 的复盘给 M42–M45 提供了新靶子**：别扭点清单里 2 条其实是
  **「能力早就有、只是不知道」**（`数学.四舍五入`、`文本.左对齐`、集合交并差）
  ——治它靠补全与机器可读事实，不靠加能力。
- ✅ **M44 已交付**（方向三 · AI 可用性第一轮，2026-09-22）：`--lang-spec` 补
  **错误码表 / 静态检查码表 / 等价 Python 写法**；新增 **`jishi 源码静态检查`**
  （四类问题，真实语料零误报）；作用域分析抽成 `jishi/scope.py` 供编辑器与命令行
  共用。顺手修掉 `E2008` 重码（自定义异常改 `E2009`）。
- ✅ **M45 已交付**（方向三 · AI 可用性第二轮，2026-09-22）：
  ① **`tools/eval_ai.py` AI 跑分表**——45 题（语法 12 / 标准库 15 / 调试 10 /
  重构 8）在 `evals/tasks/*.json`；模型只看语言规格，写完**真跑基石**判分
  （输出逐字相同**且**退出码 0），跑错回灌中文报错最多改 3 轮。
  **题集先自检**（`--自检`）：调试题的缺陷代码必须真失败、重构题原始代码必须
  真跑通——题目错了跑分表就是假的。限流按 3/6/12/24 秒退避重试。
  ② **MCP 工具面 3 → 7 个**：`check_source` / `lookup_error` / `lookup_stdlib` /
  `format_source`，事实全从语言规格与既有实现取，不另抄一份。
  ③ 顺带修掉**树遍历复合下标赋值漏裸 `KeyError`**
  （`字典[新键] += 1` → 原来报「程序内部出现未预料的问题（KeyError）」，
  现报 `E2004 找不到这个键`）——**写调试题集时抓到的**。
- 📌 **M45 的收尾动作**：打包 GitHub Releases。
  （**不再含「PyPI 首次上传」**——2026-09-22 用户决定放弃 PyPI，改自建分发。）
- 📌 **当前任务：方向四 · 自主分发（M46 起）**——用自有服务器（腾讯轻量云）
  托管包索引与发行包，并做自研安装命令（暂名 **`下载`**），目标是不依赖 PyPI /
  Python 工具链。**服务器已连通**（上海 `lhins-46ox1t3g`，公网 `111.229.166.185`），
  三项已定：**SSH 密钥对登录 / 纯静态 Nginx / 域名 ICP 备案中、先跑 HTTP**。
  细节见 `docs/路线图.md`「方向四」与 `.workbuddy/memory/操作细节.md` §七。
- ✅ **两个「已记录待修项」已清零（M40）**：① f-string 改用内部保留名 `$文本`；
  ② 树遍历补齐「赋值即局部」。当前**没有已知的未修分歧**——`tests/test_cvm.py`
  里那条 `xfail` 也按约定转成了正常断言。
  下一批待办（M41 起）见 `docs/路线图.md` §3.6 与 `docs/真实项目.md` 的别扭点清单。
- 📌 **遗留项已逐条判定**（做 / 有替代 / 不做，没有含糊搁置），清单见
  `docs/路线图.md`「遗留项清单」。
- **铁律**：能用内建/标准库实现的，不碰语法；语法特性要三执行器对拍。
- **度量**：`python tools/probe_capabilities.py`（当前 **42/50 可用**；缺口 8 项
  **全部已判定**，探针汇总行会直接说「无悬案」）。
  **语法面已冻结**（M36，2026-09-14）：新增语法要走准入三关 + 关 0，
  默认先问「能不能用内建或标准库实现」。
- 可按需补的遗留项（不阻塞）：短字符串内联、字节码执行器的调试器、
  LSP 增量同步、非 Windows 平台的 https。
- 当前版本 **0.1.27**；全量测试 **2066 项**。

---

## 4. 常用命令

```bash
# 运行脚本 / REPL
python -m jishi.cli 文件.jsh
python -m jishi.cli -i

# 测试（必须全绿）
python -m pytest tests/

# 构建 C 虚拟机动态库（改 C 代码后）
python cvm/build.py

# 性能基准
python tools/benchmark.py

# 一键全检
python tools/ci.py

# 打发行包（PyInstaller；Windows 上会自动调 Inno Setup 出 setup.exe）
python tools/build_release.py

# 发布到 GitHub —— 代码的发布通道（2026-09-20 起 Gitee 已弃用）
python tools/publish_github.py           # 同步代码（屏蔽版快照）
python tools/publish_github.py --dry-run # 只导出+屏蔽+本地提交，不推送
python tools/publish_github.py --tag v0.2.0  # 同步 + 打 tag（触发三平台构建）

# 【已弃用】PyPI 通道（2026-09-22 用户决定放弃 PyPI，改走自建分发）
# 脚本留着不是因为还要发，而是这三段逻辑对自建分发照样有用：
#   版本号一致性校验 / 构建 sdist+wheel / 隐私扫描（包内不许出现本机路径）
python tools/publish_pypi.py                        # 只检查，产物落在 dist/

**这条不用问用户，直接执行**（2026-09-20 定的规矩）：每个里程碑提交后，
自动跑一次 `publish_github.py` 把屏蔽版快照同步到 GitHub。

**PyPI 通道已于 2026-09-22 弃用**（用户决定：太依赖 Python，改用自己的服务器做
包管理与分发，见 §1 第 8 条与 §3 的「方向四」）。所以现在：

- **别再去 PyPI 建账号 / 建 token / 上传**，那条路已经作罢；
- README 的安装说明保持「从源码装」（`pip install git+…`），
  并且**永远不写 `pip install jishi`**；
- 自建通道开工后，索引与下载地址那套另写——服务端是自有服务器，
  客户端是自研命令（暂名 `下载`），**别把服务器地址或凭据写进交付物**。

两个坑：
- **别用 `git push origin`**：本仓库的 `origin`（Gitee）已废弃移除；而且
  普通 push 会把**未屏蔽**的本地历史推上去（公告里明说了只发屏蔽版快照）。
- **`--tag` 会触发 GitHub Actions 三平台构建**，只在真的要发版时加。
```

---

## 5. 文档地图（需要深入时按图索骥）

| 文件 | 用途 |
|------|------|
| `README.md` | 对外主文档：进度 / 能力 / 架构 / 决策 / 踩坑 |
| `docs/语言规格.md` | 语法语义规格（v1.0 已冻结） |
| `docs/设计决策.md` | 设计决策 D1–D34 |
| `docs/路线图.md` | **唯一的路线图**（M0–M37 总表 + 主线 A/B/C 规划 + 不做清单 + 度量） |
| `docs/调试器.md` | 调试器的用法、命令表与**已知边界**（只有树遍历能调试） |
| `docs/工具链.md` | LSP 增量同步 / REPL 增强 / 格式化器的实测数据与已知边界（B2/B3/B4） |
| `docs/真实项目.md` | 五类真实项目 + **行数比基线**（口径、逐项数据）+ 「别扭点清单」 |
| `docs/真实项目.md` | 五类真实项目 + **行数比基线**（口径、逐项数据）+ 「别扭点清单」 |
| `docs/tutorial/` | 教程 10 章 |
| `docs/embed.md` | 跨语言嵌入（C/Node/Rust）指南 |
| `examples/projects/` | 真实项目 + 反哺的语法别扭点记录 |
| `PROGRESS.md` | **进度日志主轴**（跨工具，里程碑级进度；末尾是里程碑明细与三份旧路线图的历史归档） |

---

## 6. 协作约定

- 本文件是「约束主轴」，只放高频硬约束和当前任务，不重复 README 细节。
- **进度日志写进 `PROGRESS.md`**（里程碑级，跨工具共享）；详细决策写入
  `docs/设计决策.md`；琐碎过程不写进 PROGRESS.md。
- 「当前任务」节与 PROGRESS.md 的「当前进行中/下一步」在每完成一个里程碑后同步更新。
- 各工具的入口文件（CLAUDE.md / .cursorrules / copilot-instructions.md）
  只是一行指针，**不要**在里面另起炉灶写约束，避免多处维护产生偏差。
